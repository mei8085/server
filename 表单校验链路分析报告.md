# 前端表单状态容器承接后端校验结果并精准回填链路分析报告

## 一、整体架构概览

本项目是 **Gotify Server** —— 一个 Go (Gin) 后端 + React (MobX + MUI) 前端的消息推送服务系统。表单校验链路贯穿前后端，整体数据流如下：

```
用户输入 → 前端本地简易校验（按钮禁用+Tooltip）
         → Store 层发起请求（两条路径分流）
             ├─ 路径A：默认 axios 实例 → 全局拦截器 → Snackbar 全局提示
             └─ 路径B：axios.create() 新实例 → 局部 catch → 组件自行处理
         → 后端 Gin Bind + validator 校验 → 业务层校验
         → Gin 错误中间件格式化 → model.Error JSON 返回
```

---

## 二、后端校验体系

### 2.1 Model 层：Struct Tag 声明式校验

后端通过 Go 的 `validator/v10` 库配合 Gin 的 `ctx.Bind()` 机制，在 Struct Tag 中声明校验规则。

**文件：[model/user.go](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/model/user.go#L52-L68)**

```go
type CreateUserExternal struct {
    Name  string `binding:"required" json:"name" form:"name"` // 必填
    Admin bool   `json:"admin" form:"admin"`
    Pass  string `json:"pass,omitempty" form:"pass" binding:"required"` // 必填
}
```

**文件：[model/application.go](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/model/application.go#L28-L51)**

```go
type Application struct {
    Name            string `form:"name" json:"name" binding:"required"` // 必填
    Description     string `form:"description" json:"description"`
    DefaultPriority int    `form:"defaultPriority" json:"defaultPriority"`
    SortKey         string `form:"sortKey" json:"sortKey"`
}
```

**支持的校验标签（见 error/handler.go）：**
- `binding:"required"` — 字段必填
- `binding:"max=n"` — 数值/长度上限
- `binding:"min=n"` — 数值/长度下限

### 2.2 API 层：参数绑定与业务校验

**文件：[api/application.go](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/api/application.go#L92-L111)**

```go
func (a *ApplicationAPI) CreateApplication(ctx *gin.Context) {
    applicationParams := ApplicationParams{}
    // 1) ctx.Bind 触发 validator 校验，失败时自动将错误写入 c.Errors
    if err := ctx.Bind(&applicationParams); err == nil {
        // 2) 业务层校验（数据库唯一约束等）
        app := model.Application{...}
        if err := a.DB.CreateApplication(&app); err != nil {
            handleApplicationError(ctx, err)
            return
        }
        ctx.JSON(200, withResolvedImage(&app))
    }
}
```

**文件：[api/user.go](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/api/user.go#L188-L239)**

```go
func (a *UserAPI) CreateUser(ctx *gin.Context) {
    user := model.CreateUserExternal{}
    if err := ctx.Bind(&user); err == nil {
        // 业务校验：用户名已存在
        existingUser, err := a.DB.GetUserByName(internal.Name)
        if existingUser == nil {
            // 创建成功
        } else {
            ctx.AbortWithError(400, errors.New("username already exists"))
        }
    }
}
```

### 2.3 错误中间件：统一格式化

**文件：[error/handler.go](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/error/handler.go#L14-L64)**

这是整个后端错误体系的核心中间件，**在所有 handler 执行完毕之后** 统一处理 `c.Errors`：

```go
func Handler() gin.HandlerFunc {
    return func(c *gin.Context) {
        c.Next() // 先执行业务 handler

        if len(c.Errors) > 0 {
            for _, e := range c.Errors {
                switch e.Type {
                case gin.ErrorTypeBind: // 参数绑定/校验错误
                    errs, ok := e.Err.(validator.ValidationErrors)
                    if !ok {
                        writeError(c, e.Error())
                        return
                    }
                    var stringErrors []string
                    for _, err := range errs {
                        stringErrors = append(stringErrors, validationErrorToText(err))
                    }
                    writeError(c, strings.Join(stringErrors, "; "))
                default: // 其他业务错误（AbortWithError）
                    writeError(c, e.Err.Error())
                }
            }
        }
    }
}
```

### 2.4 字段名映射：后端校验字段 → 可读文本

**文件：[error/handler.go](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/error/handler.go#L43-L56)**

```go
func validationErrorToText(e validator.FieldError) string {
    // 取 Go 结构体字段名，首字母转小写（与 json tag 一致）
    runes := []rune(e.Field())
    runes[0] = unicode.ToLower(runes[0])
    fieldName := string(runes)

    switch e.Tag() {
    case "required":
        return fmt.Sprintf("Field '%s' is required", fieldName)
    case "max":
        return fmt.Sprintf("Field '%s' must be less or equal to %s", fieldName, e.Param())
    case "min":
        return fmt.Sprintf("Field '%s' must be more or equal to %s", fieldName, e.Param())
    }
    return fmt.Sprintf("Field '%s' is not valid", fieldName)
}
```

### 2.5 统一错误响应结构

**文件：[model/error.go](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/model/error.go)**

```go
type Error struct {
    Error            string `json:"error"`            // 如 "Bad Request"
    ErrorCode        int    `json:"errorCode"`        // HTTP 状态码 400/401/403/500
    ErrorDescription string `json:"errorDescription"` // 错误描述，多字段用 "; " 拼接
}
```

**示例响应（参数绑定时 name 和 pass 都为空）：**
```json
{
  "error": "Bad Request",
  "errorCode": 400,
  "errorDescription": "Field 'name' is required; Field 'pass' is required"
}
```

**示例响应（业务校验错误，用户名已存在）：**
```json
{
  "error": "Bad Request",
  "errorCode": 400,
  "errorDescription": "username already exists"
}
```

> **关键观察**：后端返回的是**拼接字符串**，不是 `{field: message}` 结构。前端无法直接解析出"哪个字段出错了"，只能看到一整段文字。

---

## 三、前端请求的两条路径：全局拦截器 vs 局部 catch

### 3.1 分流的根源：默认 axios 实例 vs axios.create()

项目中有**两种**发起 HTTP 请求的方式，对应**两条完全独立**的错误处理链路：

| 路径 | 发起方式 | 拦截器 | 错误处理 | 典型使用场景 |
|------|---------|--------|---------|------------|
| **路径A：全局拦截器路径** | 直接 `axios.get/post/put/delete`（使用默认实例） | ✅ 走 `initAxios` 注册的全局响应拦截器 | 拦截器 → Snackbar 全局通知 → Promise.reject | 大多数 CRUD：AppStore、UserStore、ClientStore 等 |
| **路径B：局部 catch 路径** | `axios.create()` 新建实例 → `.get/.post` | ❌ **不走全局拦截器**（新实例独立） | 各方法自行 catch，或继续向外抛出 | 认证相关：login、register、tryAuthenticate、elevate |

**为什么要分流？** 认证相关请求通常需要设置特殊 headers（如 Basic Auth），或者有独特的错误处理逻辑，因此使用独立的 axios 实例避免影响全局默认配置。

### 3.2 路径A：全局拦截器路径（占绝大多数）

**拦截器注册点：[ui/src/apiAuth.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/apiAuth.ts)**

```typescript
export const initAxios = (currentUser: CurrentUser, snack: SnackReporter) => {
    axios.interceptors.response.use(undefined, (error) => {
        // 1) 无响应 - 网络错误
        if (!error.response) {
            snack('Gotify server is not reachable, try refreshing the page.');
            return Promise.reject(error);
        }

        const status = error.response.status;

        // 2) 401 Unauthorized - 会话失效，触发重新认证
        if (status === 401) {
            currentUser.tryAuthenticate().then(() => snack('Could not complete request.'));
        }

        // 3) 400 / 403 / 500 - 业务错误，通过 snack 全局展示
        if (status === 400 || status === 403 || status === 500) {
            snack(error.response.data.error + ': ' + error.response.data.errorDescription);
        }

        return Promise.reject(error); // 继续向上抛出，让 await 抛异常
    });
};
```

**使用此路径的 Store（直接用 axios 全局实例）：**

- [AppStore](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/application/AppStore.ts) — create、update、remove、reorder、uploadImage 等
- [UserStore](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/user/UserStore.ts) — create、update、remove
- [ClientStore](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/client/ClientStore.ts) — create、update、remove、elevate
- [PluginStore](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/plugin/PluginStore.ts) — 相关操作
- [MessagesStore](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/message/MessagesStore.ts) — 消息相关

**错误传播效果**：
1. axios 拦截器捕获 → Snackbar 顶部弹出错误提示
2. `return Promise.reject(error)` → 调用方的 `await` 会抛出异常
3. 如果调用方没有 try-catch，异常会继续向上冒泡到 React 渲染层（一般表现为"静默失败"，只有 Snackbar 提示）

### 3.3 路径B：局部 catch 路径（认证相关）

使用 `axios.create()` 新建**独立实例**，不走全局拦截器。共 4 处：

| 方法 | 所在文件 | 是否自己 catch | 错误如何呈现 |
|------|---------|---------------|------------|
| `CurrentUser.register` | [CurrentUser.ts L24-L44](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/CurrentUser.ts#L24-L44) | ✅ 自己 catch | 返回 `Promise<boolean>`，失败时内部 snack 提示 + return false |
| `CurrentUser.login` | [CurrentUser.ts L51-L81](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/CurrentUser.ts#L51-L81) | ✅ 自己 catch | 设置 `authenticating=false` + snack 提示 "Login failed" |
| `CurrentUser.tryAuthenticate` | [CurrentUser.ts L83-L120](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/CurrentUser.ts#L83-L120) | ✅ 自己 catch | 处理连接错误、500、401登出等多种情况 |
| `ElevateStore.localElevate` | [ElevateStore.ts L34-L45](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/ElevateStore.ts#L34-L45) | ❌ 不 catch | 异常向外抛出，由调用组件自行处理 |

**示例：CurrentUser.register（局部 catch + 返回布尔值）**

```typescript
public register = async (name: string, pass: string): Promise<boolean> =>
    axios.create()  // 新建实例，不走全局拦截器
        .post(config.get('url') + 'user', {name, pass})
        .then(() => {
            this.snack('User Created. Logging in...');
            this.login(name, pass);
            return true;
        })
        .catch((error: AxiosError<{error?: string; errorDescription?: string}>) => {
            if (!error || !error.response) {
                this.snack('No network connection or server unavailable.');
                return false;
            }
            const {data} = error.response;
            this.snack(
                `Register failed: ${data?.error ?? 'unknown'}: ${data?.errorDescription ?? ''}`
            );
            return false;
        });
```

**示例：ElevateStore.localElevate（不 catch，向外抛出）**

```typescript
public localElevate = async (password: string, durationSeconds: number): Promise<void> => {
    await axios.create().request({  // 新建实例，不走全局拦截器
        url: `${config.get('url')}client/${this.currentUser.user.clientId}/elevate`,
        method: 'POST',
        data: {durationSeconds},
        headers: {
            Authorization: 'Basic ' + btoa(this.currentUser.user.name + ':' + password),
        },
    });
    await this.currentUser.tryAuthenticate();
    this.cleanupOidcElevate();
};
// 没有 catch，异常会冒泡到调用组件
```

---

## 四、前端状态容器（Store）体系

### 4.1 基础抽象：BaseStore

**文件：[ui/src/common/BaseStore.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/common/BaseStore.ts)**

```typescript
export abstract class BaseStore<T extends HasID> implements IClearable {
    @observable protected accessor items: T[] = [];

    protected abstract requestItems(): Promise<T[]>;
    protected abstract requestDelete(id: number): Promise<void>;

    @action
    public remove = async (id: number): Promise<void> => {
        await this.requestDelete(id);  // 无 catch，异常向上抛出
        await this.refresh();
    };

    @action
    public refresh = (): Promise<void> =>
        this.requestItems().then(action((items) => { this.items = items || []; }));
}
```

**设计特点**：BaseStore **完全不处理错误**，也**不保存错误状态**。异常直接向上抛出。

### 4.2 具体 Store 实现（路径A：全局拦截器）

**文件：[ui/src/user/UserStore.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/user/UserStore.ts)**

```typescript
@action
public create = async (name: string, pass: string, admin: boolean) => {
    await axios.post(`${config.get('url')}user`, {name, pass, admin});
    await this.refresh();
    this.snack('User created'); // 只有成功才会走到这里
};

@action
public update = async (id: number, name: string, pass: string | null, admin: boolean) => {
    await axios.post(config.get('url') + 'user/' + id, {name, pass, admin});
    await this.refresh();
    this.snack('User updated');
};
```

**文件：[ui/src/application/AppStore.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/application/AppStore.ts)**

```typescript
@action
public create = async (name: string, description: string, defaultPriority: number): Promise<void> => {
    await axios.post(`${config.get('url')}application`, {name, description, defaultPriority});
    await this.refresh();
    this.snack('Application created');
};
```

**共同特征**：
- 使用默认 `axios` 实例 → 走全局拦截器
- 无 try-catch → 异常直接抛出
- 成功后调用 `refresh()` 刷新列表 + `snack()` 提示

### 4.3 CurrentUser（路径B：局部 catch）

**文件：[ui/src/CurrentUser.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/CurrentUser.ts)**

```typescript
export class CurrentUser {
    @observable accessor loggedIn = false;
    @observable accessor authenticating = true;
    @observable accessor user: ICurrentUser = { name: 'unknown', admin: false, id: -1, createdAt: '' };
    @observable accessor connectionErrorMessage: string | null = null;

    // register / login / tryAuthenticate 都用 axios.create() 新实例
    // 各自有独立的错误处理逻辑
}
```

### 4.4 Store 注入与初始化

**文件：[ui/src/index.tsx](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/index.tsx#L28-L60)**

```typescript
const initStores = (): StoreMapping => {
    const snackManager = new SnackManager();
    const currentUser = new CurrentUser(snackManager.snack);
    const elevateStore = new ElevateStore(snackManager.snack, currentUser);
    const appStore = new AppStore(snackManager.snack);
    const userStore = new UserStore(snackManager.snack);
    // ...
    return { appStore, snackManager, userStore, currentUser, elevateStore, ... };
};

(function clientJS() {
    const stores = initStores();
    initAxios(stores.currentUser, stores.snackManager.snack); // 注册全局 axios 拦截器
    // ...
})();
```

---

## 五、表单组件层：提交失败后弹窗留原处的确认

### 5.1 三种 Dialog 模式的行为对比

项目中的表单 Dialog 分为三种模式，**提交失败后全部都留在原处**，但机制不同：

| Dialog | submitAndClose 写法 | 失败后是否关闭 | 机制说明 |
|--------|-------------------|---------------|---------|
| AddEditUserDialog | `async/await` 后调 `fClose()` | ❌ 不关闭 | await 抛异常 → fClose() 不执行 |
| AddApplicationDialog | 同上 | ❌ 不关闭 | 同上 |
| UpdateApplicationDialog | 同上 | ❌ 不关闭 | 同上 |
| RegistrationDialog | `.then(success => { if (success) fClose() })` | ❌ 不关闭 | 失败返回 false → 条件判断不通过 |
| ElevationForm | 组件内 try-catch | ❌ 不关闭 | catch 到错误 → 设置本地 error state |

### 5.2 模式一：async/await 型（占多数）

**代表：[AddEditUserDialog.tsx](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/user/AddEditUserDialog.tsx#L33-L36)**

```typescript
const submitAndClose = async () => {
    await fOnSubmit(name, pass, admin);  // 后端返回400 → Promise.reject → await抛异常
    fClose();                             // 这行不会执行！
};
```

**行为确认**：
- 成功：`await` 正常返回 → 执行 `fClose()` → 弹窗关闭
- 失败：`await` 抛出异常 → `fClose()` 被跳过 → **弹窗保持打开**
- 副作用：异常会冒泡到 React 事件处理层（通常被静默吞噬，用户只能看到 Snackbar）

**同模式的组件**：
- [AddApplicationDialog](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/application/AddApplicationDialog.tsx#L23-L26)
- [UpdateApplicationDialog](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/application/UpdateApplicationDialog.tsx#L32-L35)
- 以及 client 相关的 Dialog 等

### 5.3 模式二：Promise.then + success 标志型

**代表：[Register.tsx](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/user/Register.tsx#L30-L36)**

```typescript
const submitAndClose = (): void => {
    fOnSubmit(name, pass).then((success) => {
        if (success) {
            fClose(); // 只有成功才关闭
        }
        // 失败时不调用 fClose，弹窗留在原处
    });
};
```

**行为确认**：
- 成功：`success = true` → 调用 `fClose()` → 弹窗关闭
- 失败：`success = false` → 条件不通过 → **弹窗保持打开**
- 错误通过 `CurrentUser.register` 内部的 `snack()` 提示给用户

> 注意：此模式不使用 `async/await`，而是 `.then()` 回调风格。

### 5.4 模式三：组件内 try-catch + 本地 error state（唯一精准回填）

**代表：[ElevationForm.tsx](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/common/ElevationForm.tsx#L21-L27)**

```typescript
const [error, setError] = useState('');

const handleLocalElevate = async () => {
    try {
        await elevateStore.localElevate(password, ElevateDuration);
    } catch {
        // 捕获异常 → 设置字段级错误 → 弹窗当然留在原处
        setError('Elevation failed. Check your password.');
    }
};
```

**配套的 TextField 绑定**：
```tsx
<TextField
    type="password"
    label="Password"
    value={password}
    onChange={(e) => {
        setPassword(e.target.value);
        setError(''); // 输入变化时清除错误
    }}
    fullWidth
    error={!!error}    // ← 精准回填：错误时输入框变红
    helperText={error} // ← 精准回填：错误提示文字
/>
```

**行为确认**：
- 成功：正常继续
- 失败：catch 捕获 → 设置本地 error state → **弹窗保持打开** + 输入框高亮 + 错误文字

这是项目中**唯一**实现了字段级精准错误回填的组件，可作为改造其他表单的参考范例。

---

## 六、后端校验字段名与前端输入项的对应关系

### 6.1 字段名对照总表

后端 validator 返回的字段名是 **Go 结构体字段名首字母小写**（对应 `json` tag），而前端表单输入项有自己的 `name` / `id` / `className`。两者**不直接对应**：

| 后端字段（json） | 后端 Go 结构体字段 | 前端组件 | 前端 name 属性 | 前端 id 属性 | 前端 className | 能否直接对应 |
|-----------------|-------------------|---------|--------------|------------|--------------|------------|
| `name`          | `Name`            | AddEditUserDialog 用户名 | `username` | `username` | `name` | ❌ 需要映射 |
| `pass`          | `Pass`            | AddEditUserDialog 密码 | `password` | `password` | `password` | ❌ 需要映射 |
| `name`          | `Name`            | AddApplicationDialog 名称 | （无） | （无） | `name` | ❌ 无 name 属性 |
| `description`   | `Description`     | AddApplicationDialog 描述 | （无） | （无） | `description` | ❌ 无 name 属性 |
| `defaultPriority`| `DefaultPriority`| AddApplicationDialog 优先级 | （无） | （无） | `priority` | ❌ 无 name 属性 |
| `name`          | `Name`            | RegistrationDialog 用户名 | `username` | `register-username` | `name` | ❌ 需要映射 |
| `pass`          | `Pass`            | RegistrationDialog 密码 | `password` | `register-password` | `password` | ❌ 需要映射 |
| （业务错误，无字段） | — | 所有场景 | — | — | — | — | 业务错误如 "username already exists" 是纯文本，不带字段标识 |

### 6.2 具体对应关系分析

**用户创建场景 [AddEditUserDialog.tsx](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/user/AddEditUserDialog.tsx)**

```
后端字段: name  ←→  前端输入框 name="username", id="username", className="name"
后端字段: pass  ←→  前端输入框 name="password", id="password", className="password"
```

**问题**：
1. 后端 `name` ≠ 前端 `username` —— 命名不一致
2. 后端 `pass` ≠ 前端 `password` —— 命名不一致
3. 业务错误（如 "username already exists"）是纯字符串，没有字段标识

**应用创建场景 [AddApplicationDialog.tsx](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/application/AddApplicationDialog.tsx)**

```
后端字段: name             ←→  前端 className="name"         （无 name/id 属性）
后端字段: description      ←→  前端 className="description"  （无 name/id 属性）
后端字段: defaultPriority  ←→  前端 className="priority"     （无 name/id 属性）
```

**问题**：
1. 多数 TextField **没有设置 `name` 属性**，只有 `className` 用于 CSS 选择器
2. `defaultPriority` 和 `className="priority"` 命名也不一致

### 6.3 对精准回填的影响

由于以下三层不匹配，要实现"后端错误 → 精准回填到对应输入项"，**必须做字段名映射**：

1. **后端错误格式本身不结构化**：`errorDescription` 是拼接字符串，不是 `{field: message}`
2. **字段命名不一致**：后端 `pass` vs 前端 `password`，后端 `name` vs 前端 `username`
3. **前端输入项缺少 name 属性**：很多 TextField 没有 `name`，只有 `className`

> 当前项目选择了"全局 Snackbar 提示"的简化方案，绕开了字段映射问题，但牺牲了精准回填体验。

---

## 七、完整链路追踪（以创建用户为例）

### 7.1 路径A：全局拦截器路径（UserStore.create）

#### 步骤 1：用户在 Dialog 中填写表单

[AddEditUserDialog.tsx](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/user/AddEditUserDialog.tsx) 中通过 `useState` 管理 `name`、`pass`、`admin`。

#### 步骤 2：前端本地校验（按钮级禁用 + Tooltip）

`namePresent && passPresent` 为 true 时按钮才可用。否则按钮禁用，Tooltip 显示提示。

#### 步骤 3：用户点击提交，调用 Store 方法

[Users.tsx L91](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/user/Users.tsx#L91)：
```tsx
<AddEditDialog ... fOnSubmit={userStore.create} />
```

[UserStore.create](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/user/UserStore.ts#L22-L27)：
```typescript
await axios.post(`${config.get('url')}user`, {name, pass, admin});
// 使用默认 axios 实例 → 走全局拦截器
```

#### 步骤 4：后端 Gin Bind 校验

[api/user.go L189](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/api/user.go#L189)：
```go
if err := ctx.Bind(&user); err == nil { // 校验失败时 err != nil，写入 c.Errors
```

假设请求体 `{"admin":false}` —— name 和 pass 都缺失：

#### 步骤 5：错误中间件格式化

[error/handler.go L22-L34](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/error/handler.go#L22-L34)：
```
validator.ValidationErrors
  → "Field 'name' is required"
  → "Field 'pass' is required"
  → 用 "; " 拼接
```

#### 步骤 6：返回 model.Error JSON

```json
{
  "error": "Bad Request",
  "errorCode": 400,
  "errorDescription": "Field 'name' is required; Field 'pass' is required"
}
```

#### 步骤 7：axios 全局拦截器捕获

[apiAuth.ts L18-L20](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/apiAuth.ts#L18-L20)：
```typescript
snack(error.response.data.error + ': ' + error.response.data.errorDescription);
// → Snackbar 显示: "Bad Request: Field 'name' is required; Field 'pass' is required"
return Promise.reject(error);
```

#### 步骤 8：调用方 await 抛异常

[AddEditUserDialog.tsx L33-L36](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/user/AddEditUserDialog.tsx#L33-L36)：
```typescript
const submitAndClose = async () => {
    await fOnSubmit(name, pass, admin); // ← 这里抛出异常
    fClose(); // ← 这行不执行
};
```

#### 最终表现

✅ 顶部 Snackbar 全局提示（两行错误拼在一起）  
✅ **弹窗留在原处**（fClose 没执行）  
❌ 用户名输入框未高亮  
❌ 密码输入框未高亮  
❌ 错误文字未显示在输入框下方  

### 7.2 路径B：局部 catch 路径（CurrentUser.register）

以注册为例：

#### 步骤 1-3：同上，但调用的是 CurrentUser.register

[Login.tsx L105-L108](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/user/Login.tsx#L105-L108)：
```tsx
<RegistrationDialog
    fClose={() => setRegisterDialog(false)}
    fOnSubmit={currentUser.register}
/>
```

#### 步骤 4：register 方法使用 axios.create() 新实例

[CurrentUser.ts L25-L27](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/CurrentUser.ts#L25-L27)：
```typescript
axios.create()  // 新实例，不走全局拦截器
    .post(config.get('url') + 'user', {name, pass})
```

#### 步骤 5-6：后端处理同上，返回 400 错误

#### 步骤 7：局部 catch 捕获

[CurrentUser.ts L33-L43](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/CurrentUser.ts#L33-L43)：
```typescript
.catch((error: AxiosError<{error?: string; errorDescription?: string}>) => {
    const {data} = error.response;
    this.snack(`Register failed: ${data?.error ?? 'unknown'}: ${data?.errorDescription ?? ''}`);
    return false; // 返回 false 表示失败
})
```

#### 步骤 8：Dialog 根据返回值决定是否关闭

[Register.tsx L31-L35](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/user/Register.tsx#L31-L35)：
```typescript
fOnSubmit(name, pass).then((success) => {
    if (success) {
        fClose(); // success=false → 不关闭
    }
});
```

#### 最终表现

✅ 顶部 Snackbar 全局提示  
✅ **弹窗留在原处**  
❌ 输入框未高亮  
❌ 无字段级错误文字  

---

## 八、当前架构的局限性分析

### 8.1 后端侧的结构缺陷

| 问题 | 具体表现 | 影响 |
|------|---------|------|
| 错误非结构化 | `errorDescription` 是 `"; "` 拼接的字符串 | 前端无法解析出「哪个字段错了」 |
| 字段名与前端表单名不匹配 | 后端 `name` / `pass` vs 前端 `username` / `password` | 需要额外的字段名映射层 |
| 业务错误无字段关联 | 如 "username already exists" 是纯文本，不包含字段标识 | 无法自动对应到输入项 |
| 多错误只拼接不分字段 | 多个字段错误用分号拼接，丢失字段维度 | 无法分别回填 |

### 8.2 前端侧的处理缺失

| 问题 | 具体表现 | 影响 |
|------|---------|------|
| 两条路径不统一 | 全局拦截器 vs 局部 catch 并存 | 错误处理逻辑分散，维护成本高 |
| Store 层无错误状态 | BaseStore 及子类不保存 fieldErrors | 组件无法从 Store 读取错误进行回填 |
| 多数 TextField 缺少 name 属性 | 只有 className，没有标准的 name 属性 | 难以通过字段名自动匹配 |
| 表单组件未绑定 error/helperText | 除 ElevationForm 外都未设置 | 有错误也无法视觉高亮 |
| Dialog submit 缺少异常处理 | 异常静默冒泡，只有 Snackbar 提示 | 用户体验不一致 |

---

## 九、实现精准字段级回填的改进建议

### 9.1 后端改造：结构化错误响应

修改 [model/error.go](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/model/error.go) 和 [error/handler.go](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/error/handler.go)：

```go
type Error struct {
    Error            string            `json:"error"`
    ErrorCode        int               `json:"errorCode"`
    ErrorDescription string            `json:"errorDescription"`
    FieldErrors      map[string]string `json:"fieldErrors,omitempty"` // 新增
}

// 在 error handler 中，对 validator.ValidationErrors 做转换
func validationErrorsToMap(errs validator.ValidationErrors) map[string]string {
    fieldErrors := make(map[string]string)
    for _, err := range errs {
        runes := []rune(err.Field())
        runes[0] = unicode.ToLower(runes[0])
        fieldName := string(runes)
        fieldErrors[fieldName] = validationErrorToText(err)
    }
    return fieldErrors
}
```

**改造后响应示例**：
```json
{
  "error": "Bad Request",
  "errorCode": 400,
  "errorDescription": "Field 'name' is required; Field 'pass' is required",
  "fieldErrors": {
    "name": "Field 'name' is required",
    "pass": "Field 'pass' is required"
  }
}
```

对于业务错误，也可以关联字段：
```json
{
  "error": "Bad Request",
  "errorCode": 400,
  "errorDescription": "username already exists",
  "fieldErrors": {
    "name": "username already exists"
  }
}
```

### 9.2 前端改造：统一字段名映射

建立前端表单字段名 → 后端字段名的映射表（或统一命名）：

```typescript
// 表单字段映射配置（以用户表单为例）
const USER_FIELD_MAP: Record<string, string> = {
    username: 'name',     // 前端 name → 后端 name
    password: 'pass',     // 前端 password → 后端 pass
};
```

或者更简单：**统一前后端字段命名**，前端输入框的 `name` 属性直接与后端 `json` tag 对齐。

### 9.3 前端改造：Store 层增加错误容器

在 BaseStore 或各业务 Store 中增加字段级错误状态：

```typescript
export abstract class BaseStore<T extends HasID> implements IClearable {
    @observable accessor items: T[] = [];
    @observable accessor fieldErrors: Record<string, string> = {}; // 新增

    @action
    protected setFieldErrors = (errors: Record<string, string>) => {
        this.fieldErrors = errors;
    };

    @action
    public clearFieldErrors = () => {
        this.fieldErrors = {};
    };
}
```

### 9.4 前端改造：axios 拦截器增强 + 组件绑定

对于路径A（全局拦截器），需要一种机制将 fieldErrors 传递到**对应的** Store 中。有两种方案：

**方案一**：拦截器不做 snack，让 Promise reject 携带 fieldErrors 给调用方，由组件/Store 自行处理
**方案二**：增加 request 级别的元数据，标记"这个请求属于哪个 Store"，拦截器据此分发错误

更实际的做法是**方案一**：组件在 submit 时 try-catch，解析错误中的 fieldErrors 后设置到本地 state 或 Store 中。

### 9.5 前端改造：表单组件全面绑定 error 属性

参考 [ElevationForm](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/common/ElevationForm.tsx) 的模式，改造所有表单 Dialog：

```tsx
const [errors, setErrors] = useState<Record<string, string>>({});

const submitAndClose = async () => {
    try {
        await fOnSubmit(name, pass, admin);
        fClose();
    } catch (e) {
        // 从 axios error 中解析 fieldErrors
        const fieldErrs = extractFieldErrors(e);
        setErrors(fieldErrs);
        // snack 也可以保留，做双重提示
    }
};

<TextField
    name="username"
    label="Username *"
    value={name}
    onChange={(e) => {
        setName(e.target.value);
        if (errors.username) {
            setErrors(prev => ({ ...prev, username: '' }));
        }
    }}
    error={!!errors.username}      // ← 精准回填
    helperText={errors.username}   // ← 精准回填
    fullWidth
/>
```

---

## 十、代码文件索引

### 后端核心文件
| 文件 | 作用 |
|------|------|
| [model/error.go](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/model/error.go) | 统一错误响应结构定义 |
| [model/user.go](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/model/user.go) | 用户模型及 binding 校验标签 |
| [model/application.go](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/model/application.go) | 应用模型及校验标签 |
| [error/handler.go](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/error/handler.go) | Gin 错误中间件，格式化 validator 错误 |
| [api/errorHandling.go](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/api/errorHandling.go) | successOrAbort 工具函数 |
| [api/user.go](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/api/user.go) | 用户 API，含业务校验 |
| [api/application.go](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/api/application.go) | 应用 API，含业务校验 |

### 前端核心文件
| 文件 | 作用 |
|------|------|
| [ui/src/apiAuth.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/apiAuth.ts) | **axios 全局拦截器**（路径A），统一处理后端错误 |
| [ui/src/common/BaseStore.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/common/BaseStore.ts) | MobX Store 基类 |
| [ui/src/user/UserStore.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/user/UserStore.ts) | 用户 CRUD Store（路径A） |
| [ui/src/application/AppStore.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/application/AppStore.ts) | 应用 CRUD Store（路径A） |
| [ui/src/client/ClientStore.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/client/ClientStore.ts) | 客户端 CRUD Store（路径A） |
| [ui/src/CurrentUser.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/CurrentUser.ts) | 当前用户 Store，使用 axios.create()（路径B） |
| [ui/src/ElevateStore.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/ElevateStore.ts) | 权限提升 Store，使用 axios.create()（路径B） |
| [ui/src/snack/SnackManager.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/snack/SnackManager.ts) | 全局 Snackbar 通知封装 |
| [ui/src/user/AddEditUserDialog.tsx](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/user/AddEditUserDialog.tsx) | 用户新增/编辑表单（async/await 模式） |
| [ui/src/application/AddApplicationDialog.tsx](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/application/AddApplicationDialog.tsx) | 应用新增表单（async/await 模式） |
| [ui/src/application/UpdateApplicationDialog.tsx](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/application/UpdateApplicationDialog.tsx) | 应用更新表单（async/await 模式） |
| [ui/src/user/Register.tsx](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/user/Register.tsx) | 注册表单（Promise.then + success 标志模式） |
| [ui/src/common/ElevationForm.tsx](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/common/ElevationForm.tsx) | **唯一实现字段级回填**的组件（try-catch + 本地 state） |
| [ui/src/user/Login.tsx](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/user/Login.tsx) | 登录页面 |
| [ui/src/index.tsx](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/index.tsx) | 入口，初始化 Store 和 axios 拦截器 |
| [ui/src/stores.tsx](file:///d:/fz/0601-1/solo-dogfeeding/code/37-server/ui/src/stores.tsx) | Store Context 类型定义和 useStores hook |

---

## 十一、总结

### 三个关键事实的确认

1. **提交失败后弹窗留在原处** —— ✅ 确认
   - async/await 模式：await 抛异常 → fClose() 被跳过 → 弹窗不关闭
   - Promise.then 模式：success=false → 条件不满足 → 弹窗不关闭
   - try-catch 模式：catch 处理错误 → 弹窗当然不关闭

2. **后端校验字段名与前端输入项不直接对应** —— ❌ 不对应
   - 后端字段名：Go struct 字段名首字母小写（如 `name`、`pass`）
   - 前端输入框 name：`username`、`password` 等
   - 很多 TextField 甚至没有 `name` 属性，只有 `className`
   - 业务错误（如 "username already exists"）是纯文本，无字段标识

3. **两条错误处理路径并存且分流明确** —— ✅ 确认
   - **路径A（全局拦截器）**：默认 axios 实例 → initAxios 拦截器 → Snackbar → Promise.reject
     - 覆盖：AppStore、UserStore、ClientStore 等大多数 CRUD
   - **路径B（局部 catch）**：`axios.create()` 新实例 → 各自 catch 或继续抛出
     - 覆盖：CurrentUser.register、login、tryAuthenticate、ElevateStore.localElevate
     - 原因：认证请求需要特殊 headers 或独立错误处理逻辑

### 精准回填的缺失环节

- ❌ 后端错误不是 `{field: message}` 结构 → 前端无法"精准"识别字段
- ❌ 字段命名不统一 → 即使有字段也需要映射
- ❌ Store 层不保留错误状态 → 组件无法从 Store 读取错误
- ❌ 多数 TextField 未绑定 error/helperText → 有错误也显示不出来
- ❌ 两条路径不统一 → 改造需要覆盖两种模式

项目当前实现的是**全局提示（Snackbar）模式**，能够完成"错误反馈"但未做到"字段级精准回填"。要实现真正的精准回填，需要按照第九章的建议，从**后端错误结构结构化 → 字段命名统一 → Store增加错误状态 → 组件绑定 error 属性**四层同步改造。
