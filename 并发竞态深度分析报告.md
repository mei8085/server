# 并发竞态深度分析报告：持锁副作用 · 先回调后删库 · 先查再写 · 启动迁移

> 基于 Gotify Server v2 代码逐行梳理

---

## 一、插件启停（SetPluginEnabled）持锁期全动作分析

### 1.1 代码总览

核心函数位于 [plugin/manager.go:117-148](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/plugin/manager.go#L117-L148)：

```go
func (m *Manager) SetPluginEnabled(pluginID uint, enabled bool) error {
    // === 持锁前：不加锁的读阶段（存在 TOCTOU 窗口） ===
    instance, err := m.Instance(pluginID)                 // 读锁 → 查 instances map
    conf, err := m.db.GetPluginConfByID(pluginID)         // 无锁 → 查 DB
    if conf.Enabled == enabled {                           // 检查是否已经是目标状态
        return ErrAlreadyEnabledOrDisabled                 // 幂等退出
    }

    // === 持锁中：加写锁的阶段 ===
    m.mutex.Lock()                  // L131 ← 写锁开始
    defer m.mutex.Unlock()          // L132 ← 写锁在函数返回时释放

    // 动作 1：调用插件 Enable()/Disable()（外部副作用）
    if enabled {
        err = instance.Enable()     // L135
    } else {
        err = instance.Disable()    // L137
    }
    if err != nil {
        return err                  // ← 插件方法失败：直接返回，此时 enabled 字段尚未写入 DB
    }

    // 动作 2：二次读 DB（插件可能在 Enable 中改了自己的配置）
    if newConf, err := m.db.GetPluginConfByID(pluginID); err == nil {
        conf = newConf              // L143-145
    }

    // 动作 3：写内存对象字段
    conf.Enabled = enabled          // L146

    // 动作 4：写 DB
    return m.db.UpdatePluginConf(conf)  // L147 ← 返回此语句的 error
}
```

### 1.2 持锁期内动作时间轴（以 Enable 为例）

```
时间   动作                                    锁状态   读/写   副作用类型
──────┬──────────────────────────────────────┬────────┬───────┬───────────────
 T0   │ instance = m.Instance(pluginID)      │ 读锁   │ 读内存│ (查 instances)
 T1   │ conf = db.GetPluginConfByID(...)     │ 无锁   │ 读DB  │
 T2   │ if conf.Enabled == enabled → return  │ 无锁   │ 读    │ (TOCTOU 窗口)
 T3   │ m.mutex.Lock()  ────────────────────→│ 写锁↑  │       │
 T4   │ instance.Enable()                     │ 写锁   │ 执行  │ 外部副作用★
 T5   │ newConf = db.GetPluginConfByID(...)  │ 写锁   │ 读DB  │
 T6   │ conf.Enabled = enabled                │ 写锁   │ 写内存│
 T7   │ db.UpdatePluginConf(conf)             │ 写锁   │ 写DB  │
 T8   │ return (函数返回，defer 释放锁)        │ 写锁↓  │       │
```

**★ 外部副作用 T4 具体是什么？**

插件 `Enable()` 中可以做任意事情（由第三方插件代码决定），典型例子见 [plugin/example/clock/main.go:27-38](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/plugin/example/clock/main.go#L27-L38)：

```go
func (c *Plugin) Enable() error {
    c.enabled = true
    c.cronHandler = cron.New()                    // 创建定时器
    c.cronHandler.AddFunc("0 0 * * *", func() {  // 注册每小时 cron
        c.msgHandler.SendMessage(...)             // 注册后会通过 channel 发消息
    })
    c.cronHandler.Start()                         // ★ 启动 goroutine！
    return nil
}
```

即：**Enable() 可能启动 goroutine、定时器、网络连接、打开文件句柄等**，这些都是"外部副作用"，不可回滚。

### 1.3 T7 写 DB 失败时的三方状态定格

这是核心问题：**动作 T4（Enable）已经成功执行且不可回滚，但动作 T7（写 DB）失败会怎样？**

| 观察维度 | 失败时停在什么状态 | 是否与 enabled=true 一致 | 重启能否抹平 |
|---------|------------------|:---------------------:|:----------:|
| **① 进程运行态（内存中）** | - 插件 goroutine 已启动并在运行<br>- `c.cronHandler.Start()` 已执行<br>- 插件内 `enabled = true`<br>- **但** Manager 的 `conf.Enabled` 字段只改了内存副本，没改 `instances` 里的对象 | ❌ **不一致**：插件实际在跑，但 Manager 没持久化 | ❌ 重启可抹平 |
| **② DB `plugin_confs.enabled` 字段** | 仍然是 **旧值 false**（UpdatePluginConf 失败） | ❌ **不一致**：DB 说禁用，内存却在跑 | ❌ 重启可抹平 |
| **③ 外部连接/副作用** | - cron 定时器已启动且可能已触发任务<br>- Webhook 路由已注册<br>- 打开的文件/网络连接在关闭前一直有效 | ✅ **一致**：外部副作用就是 enabled=true 的表现 | ❌ **重启可抹平**（但运行期间造成的实际影响如已发消息不可撤销）|

**重启过程的修复路径**：重启后进入 `NewManager()` → `initializeForUser()` → 读 DB 中 `enabled=false` → **不会调用 Enable()** → 状态重新一致。

> ⚠️ **但是！** 运行期间从 T7 失败到进程重启这段时间，用户在前端看到的是 "插件已禁用"（GET /plugin 读 DB 返回 false），但实际插件在后台跑、可能还在发消息。这是一个 **幽灵插件** bug。

### 1.4 Disable 场景类似但镜像对称

`SetPluginEnabled(pluginID, false)` 时：
- T4 调用 `instance.Disable()` → 停止 cron、关闭 goroutine（**不可回滚**）
- T7 `UpdatePluginConf(enabled=false)` 可能失败

此时三方状态：
- ① 插件实际已停止（goroutine 没了）
- ② DB 中 enabled 仍为 true
- ③ 外部副作用已消失（定时器停了）

用户看到的是"插件启用中"，实际却停了。

### 1.5 TOCTOU 竞态：持锁前检查 vs 持锁后操作

注意 T0-T2 全部在持写锁之前完成。两个并发请求同时 Enable 同一个插件：

```
   请求 A (Enable=true)               请求 B (Enable=true)
   ──────────────────                 ──────────────────
T0: Instance() → 拿到 inst
T1: GetPluginConfByID → enabled=false
T2: conf.Enabled!=enabled → 通过
                                      T0': Instance() → 拿到同一个 inst
                                      T1': GetPluginConfByID → enabled=false
                                      T2': conf.Enabled!=enabled → 通过
T3: Lock() ← 拿到写锁
T4: inst.Enable() → OK
T5: 二次读 DB
T6: conf.Enabled=true
T7: UpdatePluginConf → OK
T8: Unlock() ← 释放
                                      T3': Lock() ← 拿到写锁
                                      T4': inst.Enable() → 被调用第二次！
```

结果：插件 `Enable()` 被调用 **两次**，而官方接口约定应该是幂等的。部分插件可能在第二次 Enable 时崩溃（重复启动 cron 等）。

> 正确做法：持锁后在锁内重新检查 `enabled` 状态，而不是在锁外检查。

---

## 二、删用户流程 "先回调再删库" 的深度剖析

### 2.1 代码链路全景

删除用户发生在 [api/user.go:330-354](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/api/user.go#L330-L354)：

```go
func (a *UserAPI) DeleteUserByID(ctx *gin.Context) {
    withID(ctx, "id", func(id uint) {
        user, err := a.DB.GetUserByID(id)
        // ... admin 数量校验 ...

        // 步骤 A：先调回调（内存操作，非事务）
        if err := a.UserChangeNotifier.fireUserDeleted(id); err != nil {
            ctx.AbortWithError(500, err)  // ← A 失败直接返回，DB 还没动
            return
        }

        // 步骤 B：再删数据库（多表，非事务）
        successOrAbort(ctx, 500, a.DB.DeleteUserByID(id))
    })
}
```

其中 `fireUserDeleted` 在 [api/user.go:42-49](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/api/user.go#L42-L49)：

```go
func (c *UserChangeNotifier) fireUserDeleted(uid uint) error {
    for _, cb := range c.userDeletedCallbacks {
        if err := cb(uid); err != nil {
            return err  // ← 有一个回调失败，后续回调不再执行
        }
    }
    return nil
}
```

回调注册于 [router/router.go:113-115](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/router/router.go#L113-L115)：

```go
userChangeNotifier.OnUserDeleted(streamHandler.NotifyDeletedUser)    // 回调 1
userChangeNotifier.OnUserDeleted(pluginManager.RemoveUser)           // 回调 2
```

### 2.2 回调 1 + 回调 2 的具体效果

**回调 1：streamHandler.NotifyDeletedUser**

通知所有 WebSocket 连接该用户已被删除 → 关闭客户端 socket → **不可回滚**。

**回调 2：pluginManager.RemoveUser**（[plugin/manager.go:184-208](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/plugin/manager.go#L184-L208)）：

```go
func (m *Manager) RemoveUser(userID uint) error {
    for _, p := range m.plugins {
        pluginConf, err := m.db.GetPluginConfByUserAndPath(userID, ...)

        if pluginConf.Enabled {
            inst, _ := m.Instance(pluginConf.ID)
            m.mutex.Lock()
            err = inst.Disable()         // ★ 停插件 goroutine（不可回滚）
            m.mutex.Unlock()
            if err != nil {
                return err                // ← 这里 return 的话，已经 Disable 的插件不会被 Enable 回来
            }
        }
        delete(m.instances, pluginConf.ID)  // ★ 从内存 map 删除实例（不可回滚）
    }
    return nil
}
```

### 2.3 四种失败场景的状态定格表

| 场景 | 停在哪一步 | 用户 DB 记录 | Stream WS 连接 | 插件 goroutine | Plugin instances map | DB 中用户的 apps/clients/plugin_confs | 重启能恢复？ |
|-----|----------|:-----------:|:-------------:|:-------------:|:--------------------:|:------------------------------------:|:----------:|
| **① RemoveUser 中 `inst.Disable()` 失败**（第 2 个插件 Disable 抛错） | fireUserDeleted 内部 return | ✅ 还在 | 回调 1 已关 | ❌ 已 Disable 的插件停了，后面的没停 | ❌ 已 Disable 的删了，后面的没删 | ✅ 全在 | ⚠️ 部分恢复：插件 DB enabled=true 但实例没了，重启会重新 enable 新的实例 |
| **② fireUserDeleted 全部成功，DeleteUserByID 第一步 `GetApplicationsByUser` 失败** | 步骤 B 开头 | ✅ 还在 | ❌ 全关了 | ❌ 全停了 | ❌ 全删了 | ✅ 全在 | ⚠️ **WS 永久断开（用户以为自己被删了实际没删）** |
| **③ DeleteUserByID 执行一半（apps 删了，clients 没删）** | 步骤 B 中间 | ✅ 还在 | ❌ 全关了 | ❌ 全停了 | ❌ 全删了 | ⚠️ apps 已删但 clients 还在 | ✅ DB 不一致；重启不会自动恢复 clients |
| **④ DeleteUserByID 最后一步删 users 失败** | 步骤 B 末尾 | ✅ 还在 | ❌ 全关了 | ❌ 全停了 | ❌ 全删了 | ❌ apps/clients/plugin_confs 全删了，只剩 users 表一条孤记录 | ✅ 重启后 users 有一条无依赖的孤记录，可手动清理 |

### 2.4 场景 ② 的"幽灵登出"特别严重

最诡异的场景是回调都执行完了但 DB 删除刚开始就失败：
- 用户 Bob 点击删除自己的账号 → 返回 500
- Bob 所有 WS 连接被关闭 → 所有设备掉线
- Bob 所有插件被停了（RSS 定时、监控告警都没了）
- 但 Bob 的账号 **实际还在**，下次可以正常登录
- Bob 登录后：所有插件显示 "已启用" 但实际没工作，需要手动点一次禁用再启用

---

## 三、"先查再写"竞态全清单：撞唯一索引、抛错分类、循环重试

### 3.1 七个竞态场景逐一对比

| # | 场景 | 先查什么 | 再写什么 | 唯一索引兜底 | 错误处理 | 循环重试覆盖 | 代码位置 |
|:-:|-----|---------|---------|:----------:|---------|:----------:|---------|
| **1** | **创建应用 CreateApplication** | SELECT 该用户最大 sort_key | INSERT 新应用 | ✅ `(user_id, sort_key)` 联合唯一 | `handleApplicationError`: 捕获 `gorm.ErrDuplicatedKey` → **HTTP 400** "sort key is not unique" | ❌ 不重试，让用户操作 | [database/application.go:39-55](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/database/application.go#L39-L55)<br>[api/application.go:485-491](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/api/application.go#L485-L491) |
| **2** | **创建用户 CreateUser** | SELECT users WHERE name=? | INSERT 新用户 | ✅ `users.name` 唯一 | ⚠️ 只在 **查阶段** catch：`existingUser!=nil` 时返回 400<br>**写阶段** 撞唯一索引 → 被 `successOrAbort` 转成 **HTTP 500** | ❌ 不重试 | [api/user.go:188-239](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/api/user.go#L188-L239) |
| **3** | **Application Token 生成** | SELECT apps WHERE token=? | （后续 CreateApplication 时写入） | ✅ `applications.token` 唯一 | ✅ **`GenerateNotExistingToken` 死循环重试**（每次随机生 token，直到数据库中不存在） | ✅ **全覆盖：for 循环 + tokenExists 查库** | [auth/token.go:28-35](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/auth/token.go#L28-L35)<br>[api/application.go:100](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/api/application.go#L100) |
| **4** | **Client Token 生成** | SELECT clients WHERE token=? | （后续 CreateClient 时写入） | ✅ `clients.token` 唯一 | ✅ 同上 for 循环重试 | ✅ 全覆盖 | [api/client.go:155](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/api/client.go#L155) |
| **5** | **Plugin Token 生成** | SELECT plugin_confs WHERE token=? | （后续 CreatePluginConf 时写入） | ✅ `plugin_confs.token` 唯一 | ✅ 同上 for 循环重试 | ✅ 全覆盖 | [plugin/manager.go:403](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/plugin/manager.go#L403) |
| **6** | **插件启停 SetPluginEnabled** | SELECT plugin_confs WHERE id=? 读 enabled 字段 | UPDATE plugin_confs SET enabled=? | ❌ 无唯一索引冲突 | ① 幂等检查：`conf.Enabled==enabled` → 抛 `ErrAlreadyEnabledOrDisabled` → API 层 **HTTP 400**<br>② 插件方法失败 → API 层 **HTTP 500**<br>③ UpdatePluginConf 失败 → API 层 **HTTP 500** | ❌ 不重试 | [plugin/manager.go:117-148](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/plugin/manager.go#L117-L148)<br>[api/plugin.go:125-146](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/api/plugin.go#L125-L146) |
| **7** | **启动期默认用户创建** | `db.Find().Count(&userCount)` 计数 | `db.Create(&model.User{Name: defaultUser, ...})` | ✅ `users.name` 唯一 | ⚠️ **完全不捕获错误**，Count 为 0 就写入；若并发启动多个 Gotify 实例，第 2 个启动进程会 panic（`createDefaultUserIfNotExist` 通常是真，但多实例部署下会炸） | ❌ 不重试 | [database/database.go:88-92](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/database/database.go#L88-L92) |

### 3.2 Token 生成 for 循环的实际安全性

`GenerateNotExistingToken` 实现（[auth/token.go:28-35](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/auth/token.go#L28-L35)）：

```go
func GenerateNotExistingToken(generateToken func() string, tokenExists func(token string) bool) string {
    for {
        token := generateToken()
        if !tokenExists(token) {
            return token
        }
    }
}
```

⚠️ **仍然是 TOCTOU 竞态**（在锁外/事务外检查）：
```
   协程 A                                    协程 B
   生成 "Aabc123"
   tokenExists("Aabc123") → false
   return "Aabc123" ← 选中此 token
                                            生成 "Aabc123"（2^132.5 空间内极罕见撞）
                                            tokenExists("Aabc123") → false（A 还没写入 DB）
                                            return "Aabc123" ← 也选中同一个！
   CreateApplication 写入 → OK
                                            CreateApplication 写入 → 💥 唯一索引冲突 500
```

但 22 字符 × 65 字符集 ≈ 7.65×10³⁹ 空间，碰撞概率可忽略。**实际风险是：** 发生碰撞时 token 生成的 for 循环没重跑（因为已经 return 了），而是一路传到 SQL 执行层，炸成 HTTP 500，而不是 400。

### 3.3 错误抛 400 vs 500 的不完整映射表

| 原始错误 | 来自哪个竞态 | 当前状态码 | 应不应该是 400？ |
|---------|------------|:--------:|:--------------:|
| `gorm.ErrDuplicatedKey` on applications(user_id,sort_key) | 场景 1 | **400** ✅ | 是 |
| `gorm.ErrDuplicatedKey` on applications.token | 场景 3 极端碰撞 | **500** ❌ | 应该是 400 或者自动重试生成新 token |
| `gorm.ErrDuplicatedKey` on clients.token | 场景 4 极端碰撞 | **500** ❌ | 同上 |
| `gorm.ErrDuplicatedKey` on plugin_confs.token | 场景 5 极端碰撞 | **500** ❌ | 同上 |
| `gorm.ErrDuplicatedKey` on users.name（并发创建同名） | 场景 2 写阶段 | **500** ❌ | 应该是 400 "username already exists" |
| `gorm.ErrDuplicatedKey` on users.name（默认用户多实例启动） | 场景 7 | **进程 panic** ❌ | 应该静默忽略 |
| `plugin.ErrAlreadyEnabledOrDisabled` | 场景 6 幂等检查 | **400** ✅ | 是（语义上不是错误） |
| 插件 `Enable()` 返回业务错误 | 场景 6 插件方法 | **500** ⚠️ | 看情况，插件业务错误可能应 400 |

### 3.4 场景 7（启动期默认用户）的真实隐患

代码 [database/database.go:88-92](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/database/database.go#L88-L92)：

```go
userCount := int64(0)
db.Find(new(model.User)).Count(&userCount)
if createDefaultUserIfNotExist && userCount == 0 {
    db.Create(&model.User{Name: defaultUser, Pass: password.CreatePassword(defaultPass, strength), Admin: true})
}
```

问题：
- **Count 与 Create 之间无事务、无锁** — K8s 中同时拉起两个 Gotify 副本时，两个副本都看到 `userCount=0`，两个都去 Create
- 第一个成功 → 有默认用户了
- 第二个失败 → **`db.Create` 直接 panic**（TranslateError 也救不了 `New()` 函数，因为 error 没被 return）—— 实际看代码，`db.Create` 返回的是 `*gorm.DB`，`.Error` 没有被检查。实际如果唯一索引冲突，`db.Error` 会有错误但不会 panic，这个默认用户只是不会被创建到第 2 个实例的内存里？ 不，DB 是共享的，所以第 2 个实例只会写库失败，**但 `New()` 不会返回 error**，进程启动完成后，第 2 个实例没有默认用户问题（DB 共享，读出来都有）。**实际上没那么严重，但依然是错误未处理的代码味道。**

---

## 四、启动期补 sort_key 迁移：读写范围与客户端排序静默重排

### 4.1 迁移触发条件与总体流程

代码 [database/database.go:94-96](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/database/database.go#L94-L96)：

```go
if err := db.Transaction(fillMissingSortKeys, 
    &sql.TxOptions{Isolation: sql.LevelSerializable}); err != nil {
    return nil, err
}
```

迁移函数 `fillMissingSortKeys`（[database/database.go:120-151](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/database/database.go#L120-L151)）：

```go
func fillMissingSortKeys(db *gorm.DB) error {
    // 步骤 1：COUNT 统计是否需要迁移（全表扫描）
    missingSort := int64(0)
    db.Model(new(model.Application)).
        Where("sort_key IS NULL OR sort_key = ''").
        Count(&missingSort)  // ← 全表扫描，但只读索引
    if missingSort == 0 {
        return nil  // ✅ 快速退出，无写操作
    }

    // 步骤 2：读全表 —— 注意！读的是所有应用，不是只有缺 sort_key 的！
    var apps []*model.Application
    db.Order("user_id, sort_key, id ASC").Find(&apps)
    //      ↑↑↑ 排序规则：先按 user_id 分桶，桶内按 (sort_key, id) 排

    fmt.Println("Migrating", len(apps), "application sort keys")

    // 步骤 3：遍历所有应用，重新计算 sort_key
    sortKey := ""
    currentUser := uint(math.MaxUint)
    for _, app := range apps {
        if currentUser != app.UserID {
            sortKey = ""          // 换用户，重置基准
            currentUser = app.UserID
        }
        sortKey, _ = fracdex.KeyBetween(sortKey, "")  // 生成当前用户桶内的下一个 key
        app.SortKey = sortKey  // ← 修改内存对象
    }

    // 步骤 4：全表写回所有应用（UPDATE applications SET sort_key=? ...）
    return db.Save(apps).Error  // ← GORM Save 会更新所有字段
}
```

### 4.2 关键问题 1：只缺一行时，读写范围是全表还是局部？

**答案：是全表。**

```
用户有 500 个应用，其中只有第 499 个的 sort_key 是空 → missingSort=1

执行路径：
  1. SELECT COUNT(*) FROM applications WHERE sort_key IS NULL OR sort_key='' → 返回 1
  2. 因为 missingSort != 0 → 进入迁移
  3. SELECT * FROM applications ORDER BY user_id, sort_key, id ASC → 读全部 500 行
  4. for 循环：给全部 500 行重新算 sort_key（即使 499 行本来就有值！）
  5. db.Save(apps) → UPDATE 全部 500 行写入数据库！
```

**即使只有一行 sort_key 缺失，也会：**
- 全表 SELECT
- **重写表中所有行的 sort_key 字段**（实际上值可能没变，因为用户原来的排序规则就是 user_id,sort_key,id，新生成的应该和旧的一致，但这个一致是隐含假设，代码没有验证）

### 4.3 关键问题 2：客户端原排序是否被静默重排？

**答案：大概率被重排，且用户看不到任何通知。**

#### 重排触发条件

迁移前的排序（老版本、sort_key 引入之前）通常是按 `id` 或 `created_at` 排序。
迁移代码中的 **读排序** 是 `ORDER BY user_id, sort_key, id ASC`。

问题场景（用户 A 有 3 个应用，都缺失 sort_key）：

```
迁移前（显示顺序按 id，即创建顺序）：
  id=10  name="监控告警"   created_at="2024-01-01"
  id=20  name="CI/CD"      created_at="2024-02-01"
  id=30  name="备份系统"   created_at="2024-03-01"

fillMissingSortKeys 读出来时 ORDER BY user_id, sort_key, id ASC:
  因为 sort_key 都是空或 null，排序实际变成 user_id, id ASC → 同上顺序
  for 循环分配：
    id=10 → sort_key = KeyBetween("", "")  = "a0"
    id=20 → sort_key = KeyBetween("a0","") = "a1"
    id=30 → sort_key = KeyBetween("a1","") = "a2"

迁移后正常读排序（GetApplicationsByUser）是 ORDER BY sort_key, id:
  读出来是 a0(id10) → a1(id20) → a2(id30)，顺序没变 ✅

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚠️ 但如果存在以下情况，顺序就会变：

迁移前（假设某些应用后来通过 API 手动获得了 sort_key）：
  id=10  name="监控告警"   sort_key="z99"   ← 被手动调到最后
  id=20  name="CI/CD"      sort_key=""      ← 还缺
  id=30  name="备份系统"   sort_key=""      ← 还缺

fillMissingSortKeys 读 ORDER BY user_id, sort_key, id ASC:
  sort_key 排第一：空字符串 < "z99"
  实际读出顺序：
    id=20 (空) → id=30 (空) → id=10 ("z99")

  for 循环分配：
    id=20 → sort_key = KeyBetween("", "")  = "a0"   ← 跳到第一
    id=30 → sort_key = KeyBetween("a0","") = "a1"   ← 第二
    id=10 → sort_key = KeyBetween("a1","") = "a2"   ← z99 被覆盖成 a2，变第三！

迁移后正常读排序：
  读出来是 a0(id20) → a1(id30) → a2(id10)
  用户原本把"监控告警"拖到最后，现在被顶到前面了！
```

#### 静默重排的发生路径

```
  T0 (2024年某日): 用户 Bob 在老版本上手动排序了自己的应用，
                   将"监控告警"拖到最后，sort_key 变为 z99
                   ↓
  T1 (升级到带 sort_key 的版本前): 某 Bug 导致 id=20 的应用 sort_key 被清空了
                   ↓
  T2 (第一次启动新版本): fillMissingSortKeys 触发
                   ↓
  T3: 所有应用 sort_key 被重新计算，Bob 的手动排序丢失！
                   ↓
  T4: Bob 登录 Gotify → 眼前一亮：应用全乱序了，
      但没有任何通知、日志、弹窗告诉他发生了什么。
```

### 4.4 隔离级别与迁移中的阻塞

迁移使用 `Serializable` 级别，意味着：
- MySQL/PG 中迁移事务期间，**所有其他事务对 applications 表的写入都会被阻塞或失败**
- 如果用户有 10000 个应用，迁移可能持续数秒到数十秒
- 这段时间内：**创建应用的 API 会卡住或报错**

SQLite 场景下因为 `MaxOpenConns=1`，迁移事务独占唯一连接，其他所有请求排队。

### 4.5 改进建议对照表

| 问题 | 现状 | 建议 |
|-----|------|------|
| 只缺 1 行就重写全表 | `Find(&apps)` 读全部后重算 | 只 SELECT 那些 `sort_key IS NULL` 的行，针对缺失行按用户单独插入，已存在行不动 |
| 静默覆盖已有的 sort_key | 无条件重写 | 对已存在 sort_key 的行跳过；或者保存旧 sort_key 值，迁移后排序没变时不写 |
| Serializable 阻塞太久 | 所有行在 1 个大事务里 | 分用户分批提交事务（每个用户单独事务）；或者用 Repeatable Read 级 |
| 无迁移日志 | 只打印行数 Migrating N | 详细记录：哪些用户被迁移了、迁移前后顺序对比、迁移耗时 |

---

## 五、总结速查表

### 5.1 不一致状态总览（"写库失败会怎样"矩阵）

| 操作 | 写库失败时的三方状态 | 重启能抹平？ |
|-----|-------------------|:----------:|
| **插件 Enable** | 插件在跑（cron/goroutine）✅ <br> DB enabled=false ❌ <br> 前端显示"已禁用" ✅ | ✅ 能抹平，但运行期是幽灵插件 |
| **插件 Disable** | 插件停了 ❌ <br> DB enabled=true ✅ <br> 前端显示"已启用" ❌ | ✅ 能抹平，但运行期是假死插件 |
| **先回调后删用户** | WS 关了/插件停了 ❌ <br> DB 中用户还在 ✅ <br> 用户以为自己被删了实际没删 | ⚠️ 半抹平：WS 不会自动重连 |
| **创建应用 sort_key 冲突** | DB 回滚（事务）✅ <br> 返回 400 给用户 ✅ | 不需要重启 |
| **创建用户并发撞 name** | DB 回滚 ✅ <br> 返回 500 而非 400 ❌ | 不需要重启，错误码不对 |

### 5.2 核心代码定位速查

| 关注点 | 文件 | 行号 |
|-------|------|-----|
| SetPluginEnabled 持锁 + 外部副作用 + 写库 | [plugin/manager.go](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/plugin/manager.go) | 117-148 |
| Enable 中启动 cron 定时器的典型插件 | [plugin/example/clock/main.go](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/plugin/example/clock/main.go) | 27-46 |
| 插件启停的 Enable 失败测试 | [plugin/manager_test.go](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/plugin/manager_test.go) | 251-277 |
| 删用户先回调再删库 | [api/user.go](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/api/user.go) | 330-354 |
| RemoveUser 的 Disable + 内存 map 清理 | [plugin/manager.go](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/plugin/manager.go) | 184-208 |
| Token 生成 for 循环重试 | [auth/token.go](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/auth/token.go) | 28-35 |
| 唯一索引冲突 400 处理（仅 Application） | [api/application.go](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/api/application.go) | 485-491 |
| sort_key 迁移函数（全表重写） | [database/database.go](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/database/database.go) | 120-151 |
| 应用模型联合唯一索引定义 | [model/application.go](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/model/application.go) | 22-25,67 |
| 应用列表读取排序规则（迁移后用户看到的顺序） | [database/application.go](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/database/application.go) | 63-71 |
