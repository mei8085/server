# 唯一索引冲突错误传播链路深度分析报告

> 从数据库引擎 → GORM → Database 层 → API 层 → Gin 中间件 → HTTP 响应的全链路追踪

---

## 一、错误处理基础设施（三层架构）

### 1.1 全局错误流的四层处理架构

```
┌───────────────────────────────────────────────────────────────────┐
│ L1：数据库引擎层（MySQL / PostgreSQL / SQLite）                    │
│   · 抛出数据库原始错误（如 MySQL 1062 Duplicate entry）            │
└──────────────────────────┬────────────────────────────────────────┘
                           ▼
┌───────────────────────────────────────────────────────────────────┐
│ L2：GORM 框架层                                                   │
│   · TranslateError: true                                         │
│   · 将数据库方言特定错误翻译为通用错误类型                         │
│   · gorm.ErrDuplicatedKey ← 唯一索引冲突的通用表示                │
└──────────────────────────┬────────────────────────────────────────┘
                           ▼
┌───────────────────────────────────────────────────────────────────┐
│ L3：API Handler 层（api/*.go）                                    │
│   · 两种接住错误的函数：                                           │
│     ① handleApplicationError（应用专用，区分 400/500）            │
│     ② successOrAbort（通用，用调用方传的 code，永远是 500）        │
│   · 通过 ctx.AbortWithError(code, err) 推入 ctx.Errors            │
└──────────────────────────┬────────────────────────────────────────┘
                           ▼
┌───────────────────────────────────────────────────────────────────┐
│ L4：Gin 中间件层（error/handler.go）                              │
│   · Handler() 中间件遍历 ctx.Errors                               │
│   · writeError() 读取 ctx.Writer.Status() 作为状态码              │
│   · 输出 JSON：{error, errorCode, errorDescription}               │
└───────────────────────────────────────────────────────────────────┘
```

### 1.2 关键配置：GORM TranslateError

**代码位置**：[database/database.go:36-41](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/database/database.go#L36-L41)

```go
gormConfig := &gorm.Config{
    Logger:                                   dbLogger,
    DisableForeignKeyConstraintWhenMigrating: true,
    TranslateError:                           true,  // ← 关键开关
    NowFunc:                                  now,
}
```

**作用**：
- **MySQL**：Error 1062 (23000): Duplicate entry → 翻译为 `gorm.ErrDuplicatedKey`
- **PostgreSQL**：ERROR: duplicate key value violates unique constraint → 翻译为 `gorm.ErrDuplicatedKey`
- **SQLite**：UNIQUE constraint failed → 翻译为 `gorm.ErrDuplicatedKey`

如果不开启 `TranslateError`，上层 `errors.Is(err, gorm.ErrDuplicatedKey)` 永远为 false。

### 1.3 关键函数 1：successOrAbort（通用处理）

**代码位置**：[api/errorHandling.go:5-9](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/api/errorHandling.go#L5-L9)

```go
func successOrAbort(ctx *gin.Context, code int, err error) (success bool) {
    if err != nil {
        ctx.AbortWithError(code, err)    // ← 用调用方传的 code，永远"照单全收"
    }
    return err == nil
}
```

**特点**：
- 不分析 error 内容，不做类型判断
- 状态码 = 调用方传入的参数（所有创建入口传的都是 **500**）
- 对 `gorm.ErrDuplicatedKey` **完全无感**，无论撞的是哪一列的唯一索引

### 1.4 关键函数 2：handleApplicationError（专用处理）

**代码位置**：[api/application.go:485-491](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/api/application.go#L485-L491)

```go
func handleApplicationError(ctx *gin.Context, err error) {
    if errors.Is(err, gorm.ErrDuplicatedKey) {
        ctx.AbortWithError(400, errors.New("sort key is not unique"))  // ← 硬编码消息
    } else {
        ctx.AbortWithError(500, err)
    }
}
```

**特点**：
- 识别 `gorm.ErrDuplicatedKey` 并降为 400
- ⚠️ **错误消息硬编码**：永远说 "sort key is not unique"，即使实际撞的是 `applications.token` 列
- 仅被 `CreateApplication` 和 `UpdateApplication` 调用

### 1.5 关键中间件：gerror.Handler（最终落盘 HTTP）

**代码位置**：[error/handler.go:14-41](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/error/handler.go#L14-L41) + L58-63

```go
func Handler() gin.HandlerFunc {
    return func(c *gin.Context) {
        c.Next()   // 先执行后续 Handler（它们调 AbortWithError 把错误塞进来）
        if len(c.Errors) > 0 {
            for _, e := range c.Errors {
                switch e.Type {
                case gin.ErrorTypeBind:
                    // ... validator 错误处理 ...
                default:
                    writeError(c, e.Err.Error())  // ← 普通错误走这里
                }
            }
        }
    }
}

func writeError(ctx *gin.Context, errString string) {
    status := http.StatusBadRequest           // 默认 400
    if ctx.Writer.Status() != http.StatusOK { // 如果 Handler 已设置了状态码
        status = ctx.Writer.Status()          // 就用 Handler 设置的那个
    }
    ctx.JSON(status, &model.Error{
        Error:            http.StatusText(status),
        ErrorCode:        status,
        ErrorDescription: errString,          // ← 直接输出 error.Error() 的文本
    })
}
```

**最终输出格式**：
```json
{
    "error": "Bad Request",        // ← 状态码对应的 HTTP 标准文本
    "errorCode": 400,               // ← 状态码本身
    "errorDescription": "..."       // ← 实际错误消息（可能友好也可能是原生堆栈）
}
```

---

## 二、五条创建入口的完整错误链路逐案分析

### 2.1 入口一：创建应用 CreateApplication

#### 撞 (user_id, sort_key) 联合唯一索引

```
  数据库层（MySQL）
  ├─ INSERT INTO applications (...)
  ├─ 冲突列：(user_id=5, sort_key='a2') 违反 uix_applications_user_id_sort_key
  └─ 抛出 Error 1062: Duplicate entry '5-a2' for key 'uix_applications_user_id_sort_key'
        │
        ▼
  GORM 层
  ├─ TranslateError: true 生效
  └─ 返回包装了 gorm.ErrDuplicatedKey 的 error 对象
        │
        ▼
  Database 层 [database/application.go:39-55]
  ├─ CreateApplication 处于 db.Transaction Serializable 事务
  ├─ 事务内检测到 error → GORM 自动 ROLLBACK
  └─ return err（原样向上传递，不做任何转换）
        │
        ▼
  API 层 [api/application.go:105-108]
  ├─ err := a.DB.CreateApplication(&app)
  ├─ err != nil → 调用 handleApplicationError(ctx, err) ★
  ├─ handleApplicationError:
  │   ├─ errors.Is(err, gorm.ErrDuplicatedKey) → true ✅
  │   └─ ctx.AbortWithError(400, errors.New("sort key is not unique"))
  └─ ★ 正确识别并转 400，消息匹配冲突列
        │
        ▼
  中间件层 [error/handler.go]
  ├─ ctx.Writer.Status() = 400（已设置）
  ├─ errString = "sort key is not unique"
  └─ ctx.JSON(400, {error:"Bad Request", errorCode:400,
                     errorDescription:"sort key is not unique"})
```

**✅ 本链路无问题**：状态码 400，消息与实际冲突列对得上。

#### 撞 applications.token 唯一索引（极端罕见）

前面 `GenerateNotExistingToken` 已经 for 循环检查过了，但如果 token 碰撞在"检查后、写入前"的窗口发生，链路如下：

```
  ... 同前 ...
  GORM 层返回 gorm.ErrDuplicatedKey
        │
        ▼
  Database 层：return err
        │
        ▼
  API 层：handleApplicationError → errors.Is == true
  └─ ctx.AbortWithError(400, errors.New("sort key is not unique"))  ← ⚠️ 消息对不上！
```

**最终响应用户看到**：
```json
{
    "error": "Bad Request",
    "errorCode": 400,
    "errorDescription": "sort key is not unique"  // ← 实际是 token 重复！用户会懵
}
```

**问题**：用户改了应用名重试也没用（因为 sort_key 其实没问题，是 token 自动生成器的锅）。

---

### 2.2 入口二：创建客户端 CreateClient

#### 撞 clients.token 唯一索引

```
  数据库层
  ├─ INSERT INTO clients (...)
  ├─ 冲突列：token='CWH0wZ5r0Mbac.r...' 违反 uix_clients_token
  └─ 抛出 UNIQUE constraint failed / Duplicate entry
        │
        ▼
  GORM 层
  └─ 返回 gorm.ErrDuplicatedKey
        │
        ▼
  Database 层 [database/client.go:37-43]
  ├─ CreateClient：无事务包裹，单条 INSERT
  └─ return d.DB.Create(client).Error  （原样传递）
        │
        ▼
  API 层 [api/client.go:162-164]
  ├─ if success := successOrAbort(ctx, 500, a.DB.CreateClient(&client)); !success
  │       ↑↑↑ 这里 code 固定写死 500！
  │
  └─ successOrAbort:
      ├─ err != nil → ctx.AbortWithError(500, err)
      └─ err 是 gorm.ErrDuplicatedKey，但此处完全不识别 ❌
        │
        ▼
  中间件层 [error/handler.go]
  ├─ ctx.Writer.Status() = 500
  ├─ errString = err.Error()  ← 直接输出 GORM 包装的底层原生错误文本
  │   （MySQL 会输出 "Error 1062 (23000): Duplicate entry..."）
  │   （PG 会输出 "ERROR: duplicate key value violates..."）
  │   （SQLite 会输出 "UNIQUE constraint failed: clients.token"）
  └─ ctx.JSON(500, {error:"Internal Server Error", errorCode:500,
                     errorDescription:"Error 1062 (23000): Duplicate entry..."})
```

**❌ 三大问题**：

| 问题 | 具体表现 | 影响 |
|-----|---------|------|
| 状态码错 | 应该 400，实际 500 | 前端/调用方按"服务器故障"处理，重试可能没用 |
| 消息不友好 | 直接暴露数据库方言错误（含表名列名） | 1. 用户看不懂 2. 有信息泄露风险（攻击者可以探测表结构） |
| 无重试机制 | token for 循环在 CreateClient 之前就已 return | 撞了就是撞了，不会自动重生成 token 再试 |

---

### 2.3 入口三：创建插件配置 createPluginConf

插件创建有 **两条入口路径**（都走同一个 `createPluginConf` 函数）：

#### 路径 A：启动期初始化（NewManager → initializeForUser）

**调用链**：
```
router.go [NewManager()]
  → manager.go:90 GetUsers()  → 读所有用户
  → manager.go:95 initializeForUser(*user)  ← 每个用户跑一次
    → manager.go:288-291 for each plugin:
        → initializeSingleUserPlugin  ← 见 manager.go:315
          → GetPluginConfByUserAndPath → 不存在
          → createPluginConf(instance, info, userID)  ← 开始创建
```

**撞 token 唯一索引的完整链路**：

```
  数据库层 → GORM 层：返回 gorm.ErrDuplicatedKey（撞 plugin_confs.token 或 applications.token）
        │
        ▼
  Database 层：
  可能在以下两处出错：
  ① m.db.CreateApplication(app)   ← 撞 applications.token
  ② m.db.CreatePluginConf(p)      ← 撞 plugin_confs.token
  任一处失败都 return nil, err
        │
        ▼
  initializeSingleUserPlugin [manager.go:327-330]
  ├─ pluginConf, err = m.createPluginConf(...)
  ├─ if err != nil → return err   ← 不包装，原样传
  │
  ▼
  initializeForUser [manager.go:288-291]
  ├─ if err := m.initializeSingleUserPlugin(...); err != nil
  │       return err              ← 第 N 个插件失败直接 return，后续插件不初始化
  │
  ▼
  NewManager [manager.go:95-98]
  ├─ if err := manager.initializeForUser(*user); err != nil
  │       return nil, err         ← 启动直接失败！
  │
  ▼
  调用者 [router.go:54]
  ├─ pluginManager, err := plugin.NewManager(...)
  ├─ if err != nil {
  │       log.Fatal(err)  ← ★★ 进程直接 panic 退出！★★
  │   }
  │
  ▼
  进程运行态：FATAL，退出码非 0
  用户感知：服务完全启动不了，容器 CrashLoopBackOff
  日志：有（在 NewManager 之前 log.Fatal），但非常模糊
```

**严重程度**：🚨 **启动期致命级**。一个用户的一个插件 token 碰撞导致整个 Gotify 实例启动失败，连带所有用户都无法使用。

#### 路径 B：用户创建后回调触发（OnUserAdded → InitializeForUserID）

**调用链**：
```
CreateUser API [api/user.go:230]
  → fireUserAdded(internal.ID)
    → OnUserAdded 回调链注册于 router.go:115
      → pluginManager.InitializeForUserID(userID)
        → m.initializeForUser(*user)
          → initializeSingleUserPlugin
            → createPluginConf
```

**撞 token 的完整链路**：

```
  数据库层 → GORM 层：gorm.ErrDuplicatedKey
        │
        ▼
  createPluginConf → return err
  initializeSingleUserPlugin → return err
  initializeForUser → return err
  InitializeForUserID → return err
        │
        ▼
  API 层 [api/user.go:230-233]
  ├─ if err := a.UserChangeNotifier.fireUserAdded(internal.ID); err != nil {
  │       ctx.AbortWithError(500, err)  ← ★ 状态码 500
  │       return
  │   }
  │   注意：此时 CreateUser 已经成功（DB 中用户已插入）！
  │
  ▼
  中间件层 → ctx.JSON(500, ...原生数据库错误...)
```

**❌ 关键问题：用户创建成功但插件初始化失败，产生半残用户**

| 维度 | 状态 |
|-----|------|
| users 表中的用户记录 | ✅ 已写入 |
| 用户的应用/客户端 | ✅ 可以正常创建 |
| 用户的 plugin_confs 记录 | ❌ 第 N 个插件之后的都没写 |
| 用户的插件 instances map | ❌ 第 N 个插件之后的都没加载 |
| 重启能否恢复 | ✅ 下次重启时走路径 A 初始化，若 token 不再碰撞就能补齐 |

**用户体验**：CreateUser API 返回 500，用户以为没注册成功，实际上用户名密码都能用，只是插件列表是空的或不全。

---

### 2.4 入口四：创建用户 CreateUser（API）

#### 撞 users.name 唯一索引

此处有 **两层防御**：

```
用户 API 提交创建请求
  │
  ├─ 防御层 1：应用层 SELECT 预检查
  │   [api/user.go:196-199]
  │   existingUser, err := a.DB.GetUserByName(internal.Name)
  │   if success := successOrAbort(ctx, 500, err); !success { return }
  │
  │   若 existingUser != nil → 进入 else 分支
  │       [api/user.go:235-237]
  │       ctx.AbortWithError(400, errors.New("username already exists"))
  │       → ✅ 400 + 友好消息，正确
  │
  └─ 防御层 2：数据库唯一索引（被防御层 1 的 TOCTOU 窗口绕过才有机会触发）
      并发场景：两个同名用户同时创建
      都通过了防御层 1（existingUser == nil）
      同时进入 CreateUser(internal)
```

**撞索引的链路（仅在并发时可能）**：

```
  数据库层 → GORM 层：gorm.ErrDuplicatedKey（撞 users.name）
        │
        ▼
  Database 层 [database/user.go:77-79]
  └─ return d.DB.Create(user).Error  （原样传递）
        │
        ▼
  API 层 [api/user.go:226-229]
  ├─ if existingUser == nil {
  │       if success := successOrAbort(ctx, 500, a.DB.CreateUser(internal)); !success
  │           ↑↑↑ 固定 500！
  │           return
  │   }
  │
  ▼
  successOrAbort [api/errorHandling.go]
  └─ ctx.AbortWithError(500, err)  ← 完全不识别 ErrDuplicatedKey ❌
        │
        ▼
  中间件层：ctx.JSON(500, {
      error: "Internal Server Error",
      errorCode: 500,
      errorDescription: "Error 1062: Duplicate entry 'bob' for key 'uix_users_name'"
  })
```

**❌ 问题对照表**：

| 期望 | 实际 |
|-----|------|
| HTTP 400 Bad Request | HTTP 500 Internal Server Error |
| "username already exists"（友好） | "Error 1062 (23000): Duplicate entry 'bob' for key 'uix_users_name'"（原生 SQL 错误） |
| 用户知道"这名字被用了换一个" | 用户以为服务器炸了去重试，越撞越错 |
| 不暴露数据库内部信息 | 暴露了表名 `uix_users_name`、列名、索引名 |

---

### 2.5 入口五：启动期默认用户创建

**代码位置**：[database/database.go:88-92](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/database/database.go#L88-L92)

```go
userCount := int64(0)
db.Find(new(model.User)).Count(&userCount)

if createDefaultUserIfNotExist && userCount == 0 {
    // ↓↓↓ 返回值直接丢弃！
    db.Create(&model.User{
        Name: defaultUser,
        Pass: password.CreatePassword(defaultPass, strength),
        Admin: true,
    })
}
```

#### db.Create 返回值被丢弃的实际后果分析

**`db.Create` 返回的是 `*gorm.DB`**，不是 `error`。真正的错误存在于返回对象的 `.Error` 字段里。

当前代码：
```go
db.Create(...)       // 返回 *gorm.DB
                     // 整个返回值被 GC 回收
                     // ← 内含的 .Error 字段从未被读过！
```

**三种撞唯一索引的场景分析**：

| 场景 | 发生条件 | DB.Error 有值没人读的具体后果 | 进程能否启动 | 有无日志 | 用户可感知？ |
|-----|---------|---------------------------|:----------:|:-------:|:----------:|
| **① 单实例常规启动** | 表中已有默认用户（Count 读到 userCount ≥ 1） | if 不进，db.Create 根本不执行 | ✅ 正常启动 | GORM 日志：无（没有写操作） | 完全无感知 |
| **② 单实例首次启动** | 表中无用户，Count=0，且没有并发创建者 | if 进入，db.Create 成功 → .Error == nil | ✅ 正常启动 | GORM 日志：INSERT 成功（Trace 级别） | 无感知，正常生成默认用户 admin/admin |
| **③ 单实例首次启动但 DB 里早已有人手动插了同名 admin** | 诡异场景：userCount=0 但实际 users 表有 admin（不可能，Count 是真实读的，跳过） | — | — | — | — |
| **④ K8s 多实例并发启动** | 两个 Gotify Pod 同时起来，都读到 userCount=0，都进 if | 实例 A 先执行 db.Create → 成功，.Error=nil | ✅ A 正常启动 | A：INSERT 成功日志 | 无感知 |
| | | 实例 B 后执行 db.Create → 撞唯一索引，**gorm.ErrDuplicatedKey 写入了 `.Error` 字段，但没人读** | **✅ B 也能正常启动！**（不会 panic，也不会 return error） | **⚠️ 只有 GORM 内部 WARN/ERROR 级日志**（默认 Logger Config 的 LogLevel 是 Warn，唯一索引冲突不一定被打出来） | 无感知，因为两个实例共享同一个 DB，第一个写成功了，第二个写不进去实际也不影响运行 |
| **⑤ 磁盘满/权限错/连接断等非唯一索引错误** | 写库时磁盘刚好满、用户权限 revoked 等 | `.Error` 里是真正的系统级错误，但还是没人读 | **✅ 进程照样启动！** | **⚠️ 仅 GORM 层日志**（如果没开 Warn 以上可能根本看不到） | **严重感知：启动后登录 admin 账号报错找不到用户！** |

**最隐蔽的 bug：场景 ⑤**

```
真实发生时序：
  T0: 运维清理磁盘失败，磁盘空间剩 2KB
  T1: Gotify 重启
  T2: database.New() 开始
  T3: AutoMigrate 成功（没写数据，只是改 schema）
  T4: db.Find().Count(&userCount) → 0 行，成功（只读）
  T5: userCount==0，进 if
  T6: db.Create(&model.User{Name:"admin", ...})
         ├─ 实际 SQL: INSERT INTO users (...)
         ├─ 数据库执行：磁盘满 → 返回 "No space left on device"
         ├─ GORM 将 err 写入返回值 .Error 字段
         └─ 返回 *gorm.DB 被丢弃，.Error 没人读
  T7: fillMissingSortKeys → 读空表 → 没写 → 成功
  T8: fillMissingCreatedAt → 读空表 → 没写 → 成功
  T9: return &GormDatabase{DB: db}, nil  ← 返回成功！没有 error！
  T10: 进程继续启动，监听 80 端口
  T11: 用户访问 / 尝试登录 admin/admin
         ├─ GetUserByName("admin")
         ├─ SELECT * FROM users WHERE name='admin'
         └─ → 0 rows → 用户不存在！
  T12: 登录失败，界面提示 "invalid credentials"
```

**诡异症状**：
- Gotify 启动成功，健康检查 /health 返回 200
- 默认用户 admin/admin 登录不上
- 所有需要注册用户的功能都打不开（因为创建用户也会写库失败，但那时候 CreateUser 是被 successOrAbort 包着的，至少会有错误返回）
- 运维去看启动日志：**完全没有错误**（因为 LogLevel 是 Warn，而 `db.Create` 的错误如果被 GORM 当作 Error 而非 Warn 打出来的话，需要看 stderr）

#### 补充：`db.Error` 全局字段？

GORM 的 `*gorm.DB` 每次执行方法后会返回一个新的 Session 或 chain 对象。**`db.Create` 返回的是一个临时链对象**，不是原始的 `db`。所以：

```go
result := db.Create(...)    // result.Error 有错误
if db.Error != nil { ... }  // ❌ 这读不到！db.Error 是 nil
```

当前代码连 `db.Error` 也没读。所以**无论什么错误都完全丢了**。

---

## 三、五条入口对照表（总览）

| 入口 | 撞哪个唯一索引 | 被哪段接住 | 状态码 | errorDescription | 匹配吗？ | 自动重试？ |
|-----|--------------|----------|:-----:|-----------------|:-------:|:--------:|
| **创建应用 (1a)** | `(user_id, sort_key)` | `handleApplicationError` | **400** ✅ | "sort key is not unique" | ✅ 完美匹配 | ❌ 不重试 |
| **创建应用 (1b)** | `applications.token` | `handleApplicationError` | **400** ⚠️ | "sort key is not unique" | ❌ 张冠李戴 | ❌ 不重试 |
| **创建客户端 (2)** | `clients.token` | `successOrAbort(ctx, 500, ...)` | **500** ❌ | "Error 1062 Duplicate entry..."（原生 SQL 错） | ❌ 对不上 | ❌ 不重试 |
| **插件创建 (3A)** | `plugin_confs.token`<br>或 `applications.token` | `log.Fatal(err)` in `router.go:54` | **进程崩溃** 💥 | stderr + 容器日志（堆栈） | N/A | ❌ 整个实例重启 |
| **插件创建 (3B)** | 同上 | `successOrAbort(ctx, 500, ...)` 间接 | **500** ❌ | 原生 SQL 错 + 用户已创建但无插件 | ❌ 状态码错 + 数据不一致 | ❌ 不重试 |
| **创建用户 (4)** | `users.name` | `successOrAbort(ctx, 500, ...)` | **500** ❌ | "Duplicate entry 'bob' for key 'uix_users_name'"（原生 SQL 错） | ❌ 应是 400 + "username already exists" | ❌ 不重试 |
| **默认用户 (5)** | `users.name` 或 任何写库错 | **无人接住**！ | **200 OK**（进程照样启动） | 仅 GORM 日志（可能被 LogLevel 过滤） | ❌ 完全没有响应面 | ❌ 不重试 |

---

## 四、核心代码定位速查

| 关注点 | 文件 | 行号 |
|-------|------|-----|
| GORM 开启 TranslateError | [database/database.go](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/database/database.go) | 36-41 |
| successOrAbort 通用错误处理（永远照单全收 code） | [api/errorHandling.go](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/api/errorHandling.go) | 5-9 |
| handleApplicationError（唯一识别 ErrDuplicatedKey 的处理函数） | [api/application.go](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/api/application.go) | 485-491 |
| gerror.Handler 中间件（最终 JSON 化） | [error/handler.go](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/error/handler.go) | 14-64 |
| 应用模型：token + (user_id,sort_key) 两个唯一索引 | [model/application.go](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/model/application.go) | 22-25 |
| 客户端模型：token 唯一索引 | [model/client.go](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/model/client.go) | 22 |
| 用户模型：name 唯一索引 | [model/user.go](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/model/user.go) | 8 |
| 插件模型：token 唯一索引 | [model/pluginconf.go](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/model/pluginconf.go) | 10 |
| Token for 循环重试生成 | [auth/token.go](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/auth/token.go) | 28-35 |
| CreateApplication：Serializable 事务 + sort_key 生成 | [database/application.go](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/database/application.go) | 39-55 |
| CreateClient：无事务单条 INSERT | [database/client.go](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/database/client.go) | 37-43 |
| CreateUser（Database 层） | [database/user.go](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/database/user.go) | 77-79 |
| CreateUser API 预检查 + successOrAbort | [api/user.go](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/api/user.go) | 188-239 |
| createPluginConf：先插 Application 再插 PluginConf（非事务） | [plugin/manager.go](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/plugin/manager.go) | 399-424 |
| 启动期默认用户创建：.Error 丢弃 | [database/database.go](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/database/database.go) | 88-92 |
| NewManager 失败直接 log.Fatal | [router/router.go](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/router/router.go) | 54-56 |

---

## 五、改进建议（按严重度排序）

### 🔴 P0：默认用户创建丢弃错误 — 改 5 行

```go
// 旧代码（坏）：
db.Create(&model.User{...})

// 新代码（好）：
if result := db.Create(&model.User{...}); result.Error != nil {
    if !errors.Is(result.Error, gorm.ErrDuplicatedKey) {
        return nil, result.Error  // 非唯一索引错误才退出
    }
    // 唯一索引冲突说明并发场景下别人已帮我创建了，静默成功即可
}
```

### 🟠 P1：插件启动期撞索引导致进程崩溃 — 降级处理

`NewManager` 中某个用户的某个插件初始化失败不应导致整体失败：
- 初始化失败时 log 一条 Error 但继续初始化其他用户/插件
- 将失败的插件列入"待重试列表"，后续通过 API 手动重试

### 🟠 P1：CreateUser 撞 name 返回 500 — 在 DB 层或 API 层捕获

方案 A（推荐，侵入性最小）：在 API 层 CreateUser 里仿照 handleApplicationError 捕获：
```go
if errors.Is(err, gorm.ErrDuplicatedKey) {
    ctx.AbortWithError(400, errors.New("username already exists"))
}
```

方案 B（更通用）：写一个通用的 `handleDuplicateKey(ctx, err, friendlyMsg)` 工具函数。

### 🟡 P2：CreateClient 撞 token 返回 500 + 原生错 — 同上思路处理

### 🟡 P2：handleApplicationError 消息硬编码 — 改进

不要硬编码 "sort key is not unique"，更不要对所有列都说这个。建议：
- 简单方案：写两个不同的 handle 函数，或传参区分消息
- 优雅方案：Database 层用 `fmt.Errorf("create application failed on %s: %w", "sort_key", err)` 包装，API 层识别后用不同消息

### 🟢 P3：Token 生成循环与 INSERT 之间的窗口 — 最小化

虽然碰撞概率极低，但可以把 token 生成和 INSERT 绑在一起（例如 Database 层 `CreateXxx` 内部如果遇到 `ErrDuplicatedKey` 是 token 列，就自动重生成 token 并重试 3 次）。

### 🟢 P3：所有 `successOrAbort(ctx, 500, ...)` 自查

全局 37 处调用。逐一检查：如果调用的是 INSERT/UPDATE 且涉及唯一索引，不能无脑 500。
