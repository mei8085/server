# 客户端获取管理权限全过程分析报告（校正版）

## 概述

本文档深入分析了 Gotify 服务端的权限管理体系，涵盖会话与令牌管理、权限提升流程、以及异常情况下的权限回收机制。

---

## 一、会话与令牌管理：服务端如何将令牌与特定客户端关联

### 1.1 令牌类型与生成

系统包含多种类型的令牌，通过前缀区分：

| 令牌类型 | 前缀 | 生成位置 |
|---------|------|---------|
| 客户端令牌 | `C` | `auth/token.go:43` |
| 应用令牌 | `A` | `auth/token.go:38` |
| 插件令牌 | `P` | `auth/token.go:48` |

**生成机制**：
- 令牌由随机字符串 + 前缀组成，随机部分长度为 22 字符
- 使用密码学安全的随机源 `crypto/rand`
- 字符集包含大小写字母、数字及 `._-`

**代码参考**：
- `auth/token.go:57-68` - `generateRandomToken` 函数
- `auth/token.go:61-68` - `generateRandomString` 函数

### 1.2 客户端与会话的关系

**客户端（Client）模型** (`model/client.go:10-38`)：

```go
type Client struct {
    ID            uint        // 主键
    Token         string      // 唯一令牌，用于认证
    UserID        uint        // 所属用户
    Name          string      // 客户端名称（如浏览器/设备名）
    LastUsed      *time.Time  // 最后使用时间
    ElevatedUntil *time.Time  // 权限提升有效期
}
```

**关键点**：
- 每个 Web 会话对应一个 Client 记录
- 令牌 `Token` 是客户端的唯一标识
- `UserID` 建立与用户的关联
- `ElevatedUntil` 记录权限提升的过期时间

### 1.3 会话创建流程（登录）

**入口**：`api/session.go:56-98` - `Login` 函数

**流程步骤**：

1. **Basic Auth 验证**
   - 从请求头提取用户名和密码
   - 调用 `a.DB.GetUserByName(name)` 查询用户
   - 使用 `password.ComparePassword` 验证密码

2. **创建客户端记录**
   ```go
   elevatedUntil := time.Now().Add(model.DefaultElevationDuration)  // 默认 1 小时
   client := model.Client{
       Name:          clientParams.Name,
       Token:         auth.GenerateNotExistingToken(generateClientToken, a.clientExists),
       UserID:        user.ID,
       ElevatedUntil: &elevatedUntil,  // 登录时自动提升权限
   }
   a.DB.CreateClient(&client)
   ```

3. **设置 Cookie**
   - Cookie 名称：`gotify-client-token`
   - 存储值：客户端令牌
   - 有效期：7 天 (`CookieMaxAge = 7 * 24 * 60 * 60`)
   - 安全特性：`HttpOnly=true`, `SameSite=Strict`, `Secure`（根据配置）

**代码参考**：
- `auth/cookie.go:12-21` - `SetCookie` 函数
- `model/elevate.go:16` - `DefaultElevationDuration = time.Hour`

### 1.4 请求认证流程

**中间件**：`auth/authentication.go:52-59`

支持的认证方式：

| 方式 | 位置 | 代码位置 |
|-----|------|---------|
| Basic Auth | Authorization 头 | `auth/authentication.go:124` |
| 客户端令牌 | X-Gotify-Key 头 | `auth/authentication.go:231` |
| 客户端令牌 | Authorization: Bearer | `auth/authentication.go:234-247` |
| 客户端令牌 | URL Query 参数 `token` | `auth/authentication.go:226-228` |
| 客户端令牌 | Cookie | `auth/authentication.go:218-224` |

**认证处理流程** (`auth/authentication.go:143-176`)：

```
handleClient()
    ↓
readTokenFromRequest() → 从多种来源提取令牌
    ↓
GetClientByToken(token) → 数据库查询客户端
    ↓
RegisterClient(ctx, client) → 注册到 Gin Context
    ↓
更新 LastUsed（每 5 分钟一次）
    ↓
执行权限检查（如 checkClientElevated）
```

**上下文注册** (`auth/util.go:22-24`)：

```go
func RegisterClient(ctx *gin.Context, client *model.Client) {
    ctx.Set(authKey, &authentication{client: client})
}
```

通过 `GetUserID()` 获取当前用户 ID 时，支持从 user/client/app 三种认证类型中提取。

---

## 二、"谁负责放行"的决策链：按中间件分支独立说明（已校正）

> **重要校正**：不存在统一的"三层顺序"适用于所有场景。三个中间件的判定链各不相同，某些场景根本不走管理员判定。

### 2.1 决策链总览：三个中间件，三条独立路径

```
请求到达
    │
    ├─── RequireClient ──────── 只检查"你是谁"
    │       │
    │       └── 不走管理员判定
    │       └── 不走权限提升判定
    │
    ├─── RequireElevatedClient ─ 检查"你是谁" + "你近期重新认证了吗"
    │       │
    │       └── ⚠️ 根本不走管理员判定
    │       └── 走权限提升判定
    │
    └─── RequireAdmin ───────── 检查"你是谁" + "你是管理员吗" + "你近期重新认证了吗"
            │
            └── 走管理员判定
            └── 走权限提升判定（客户端令牌路径）
```

### 2.2 共同基础：认证入口（两个并行通道）

所有中间件都共享同一套认证入口机制。请求先尝试一条通道，失败则 `Skip` 尝试下一条。

**两个并行入口** (`auth/authentication.go:122-176`)：

| 入口函数 | 凭证类型 | 验证方式 | 通过后注册到 Context |
|---------|---------|---------|---------------------|
| `handleUser(checks...)` | Basic Auth (用户名+密码) | `GetUserByName` + `password.ComparePassword` | `RegisterUser(ctx, user)` |
| `handleClient(checks...)` | 客户端令牌 (C 前缀) | `GetClientByToken(token)` | `RegisterClient(ctx, client)` |

**判定逻辑** (`auth/authentication.go:84-106`)：
```go
func (a *Auth) evaluate(ctx *gin.Context, funcs ...func(ctx *gin.Context) (authState, error)) bool {
    for _, fn := range funcs {  // 顺序：先 handleUser，再 handleClient
        state, err := fn(ctx)
        switch state {
        case authStateSkip:
            continue  // 当前入口不适用，试下一个
        case authStateOk:
            ctx.Next()
            return true
        case authStateForbidden:
            a.abort403(ctx)  // 403
            return true
        case authStateNotElevated:
            ctx.AbortWithError(403, ...)  // 403 带提示
            return true
        }
    }
    return false  // 所有入口都 Skip → 401
}
```

**关键机制**：`handleUser` 和 `handleClient` 可以接受 **checks 参数**，这些 checks 是**可选的附加判定**。不同中间件传入不同的 checks 组合。

---

### 2.3 中间件一：RequireClient（最宽松）

**适用路由** (`router/router.go:183-218`)：
- `GET /application`、`POST /application`
- `GET /client`、`POST /client`
- `GET /message`、`GET /stream`
- `GET /current/user`

**中间件定义** (`auth/authentication.go:52-54`)：
```go
func (a *Auth) RequireClient(ctx *gin.Context) {
    a.evaluateOr401(ctx, 
        a.handleUser(),       // 不带任何 checks！
        a.handleClient()      // 不带任何 checks！
    )
}
```

**判定链**：

```
请求到达
    ↓
┌──────────────────────────────────┐
│ 通道 A：handleUser()             │
│ （Basic Auth 路径，无附加检查）   │
│                                  │
│ 1. 提取 Authorization 头         │
│ 2. GetUserByName + 密码比对      │
│ 3. 密码正确 → authStateOk → 放行 │
│ 4. 无 Basic Auth → Skip → 试通道 B│
└──────────────────────────────────┘
    ↓ Skip
┌──────────────────────────────────┐
│ 通道 B：handleClient()           │
│ （客户端令牌路径，无附加检查）    │
│                                  │
│ 1. 从 5 种来源提取 token          │
│ 2. GetClientByToken(token)       │
│ 3. 令牌有效 → authStateOk → 放行 │
│ 4. 无令牌/无效 → Skip → 结束      │
└──────────────────────────────────┘
    ↓ 两个通道都 Skip
401 Unauthorized
```

**关键事实**：
- **不走管理员判定**：普通用户和管理员都能访问
- **不走权限提升判定**：`ElevatedUntil` 根本不看
- Basic Auth 和客户端令牌**等价**，都直接放行

---

### 2.4 中间件二：RequireElevatedClient（中等严格）

**⚠️ 重要发现**：此中间件**不检查管理员身份**！普通用户和管理员都需要提升，都能通过（只要提升了）。

**适用路由** (`router/router.go:220-227`)：
- `POST /client/:id/elevate`（提升权限本身）
- `DELETE /application/:id`
- `DELETE /client/:id`
- `POST /current/user/password`

**中间件定义** (`auth/authentication.go:56-59`)：
```go
func (a *Auth) RequireElevatedClient(ctx *gin.Context) {
    a.evaluateOr401(ctx, 
        a.handleUser(),                              // 不带 checks（Basic Auth 自动满足提升）
        a.handleClient(a.checkClientElevated)        // 只带一个 check：检查提升
    )
}
```

**判定链**：

```
请求到达
    ↓
┌─────────────────────────────────────────┐
│ 通道 A：handleUser()                     │
│ （Basic Auth 路径，无附加检查）           │
│                                         │
│ 1. 提取 Basic Auth，验证密码             │
│ 2. 密码正确 → authStateOk → 放行        │
│    （Basic Auth 每次请求都带密码，        │
│     相当于每次都重新认证，不需要提升）    │
│ 3. 无 Basic Auth → Skip → 试通道 B      │
└─────────────────────────────────────────┘
    ↓ Skip
┌─────────────────────────────────────────┐
│ 通道 B：handleClient(checkClientElevated)│
│ （客户端令牌路径，需检查提升状态）        │
│                                         │
│ 1. 提取 token，GetClientByToken         │
│ 2. 令牌有效 → 进入 checks                │
│    └── checkClientElevated(client)      │
│        ├── ElevatedUntil > now → Ok → 放行
│        ├── ElevatedUntil 已过 → authStateNotElevated → 403
│        └── ElevatedUntil == nil → authStateNotElevated → 403
│ 3. 无令牌/无效 → Skip → 结束             │
└─────────────────────────────────────────┘
    ↓ 两个通道都 Skip
401 Unauthorized
```

**关键事实**：
- **不走管理员判定**：`checkClientAdmin` 根本没传入
- 普通用户提升后也能删除自己的客户端/应用
- 管理员身份**不影响**此中间件的判定

**为什么 Basic Auth 通道不带 check？**
- 因为 Basic Auth 每次请求都携带密码，相当于每次都重新认证
- 设计理念："你刚输入了密码 = 你近期已认证"，所以自动满足"提升"要求

---

### 2.5 中间件三：RequireAdmin（最严格）

**适用路由** (`router/router.go:229-236`)：
- `GET /user`（列出所有用户）
- `DELETE /user/:id`
- `POST /user/:id`

**中间件定义** (`auth/authentication.go:46-48`)：
```go
func (a *Auth) RequireAdmin(ctx *gin.Context) {
    a.evaluateOr401(ctx, 
        a.handleUser(a.checkUserAdmin),                    // Basic Auth 路径：带 checkUserAdmin
        a.handleClient(a.checkClientAdmin, a.checkClientElevated)  // 客户端令牌：带两个 checks
    )
}
```

**注意**：对于 `handleClient`，checks 按**数组顺序**执行：先 `checkClientAdmin`，再 `checkClientElevated`。

**判定链**：

```
请求到达
    ↓
┌──────────────────────────────────────────────┐
│ 通道 A：handleUser(checkUserAdmin)            │
│ （Basic Auth 路径）                           │
│                                              │
│ 1. 提取 Basic Auth，验证密码                  │
│ 2. 密码正确 → 进入 checks                     │
│    └── checkUserAdmin(user)                   │
│        ├── user.Admin == true → Ok → 放行     │
│        └── user.Admin == false → Forbidden → 403
│ 3. 无 Basic Auth → Skip → 试通道 B            │
└──────────────────────────────────────────────┘
    ↓ Skip
┌──────────────────────────────────────────────┐
│ 通道 B：handleClient(                         │
│     checkClientAdmin, checkClientElevated)    │
│ （客户端令牌路径，两个 check 顺序执行）         │
│                                              │
│ 1. 提取 token，GetClientByToken              │
│ 2. 令牌有效 → 进入 checks（按顺序！）          │
│    │                                         │
│    ├── check 1: checkClientAdmin(client)      │
│    │   ├── 通过 UserID 查询 user              │
│    │   ├── user.Admin == true → 继续 check 2  │
│    │   └── user.Admin == false → Forbidden → 403
│    │                                         │
│    └── check 2: checkClientElevated(client)   │
│        ├── ElevatedUntil > now → Ok → 放行    │
│        └── 已过期/nil → NotElevated → 403     │
│                                              │
│ 3. 无令牌/无效 → Skip → 结束                  │
└──────────────────────────────────────────────┘
    ↓ 两个通道都 Skip
401 Unauthorized
```

**关键事实**：
- **两个通道的判定链不同**：
  - Basic Auth：只需 `Admin == true`（不需要提升检查）
  - 客户端令牌：需要 `Admin == true` **且** `ElevatedUntil > now`
- **checks 顺序重要**：客户端路径先检查是否管理员，管理员判定不通过直接 403，根本不检查提升
- 这是**唯一**走管理员判定的中间件

---

### 2.6 三个中间件的判定链对比表

| 维度 | RequireClient | RequireElevatedClient | RequireAdmin |
|-----|---------------|----------------------|--------------|
| **认证入口** | handleUser() → handleClient() | handleUser() → handleClient() | handleUser(checkUserAdmin) → handleClient(checkClientAdmin, checkClientElevated) |
| **Basic Auth 通道 checks** | 无 | 无 | `checkUserAdmin` |
| **客户端令牌通道 checks** | 无 | `[checkClientElevated]` | `[checkClientAdmin, checkClientElevated]` |
| **走管理员判定？** | ❌ 否 | ❌ **否（关键！）** | ✅ 是 |
| **走权限提升判定？** | ❌ 否 | ✅ 是（仅客户端令牌） | ✅ 是（仅客户端令牌） |
| **适用用户** | 所有认证用户 | 所有认证用户（需提升） | 仅管理员（客户端令牌还需提升） |
| **401 触发条件** | 无有效凭证 | 无有效凭证 | 无有效凭证 |
| **403 触发条件** | 永不（只要认证就过） | 会话未提升 | 非管理员 **或** 会话未提升 |

---

### 2.7 典型决策路径示例（按中间件分类）

#### 示例 A：普通用户用客户端令牌访问 `GET /application`（RequireClient）
```
handleUser() → Skip（无 Basic Auth）
    ↓
handleClient() → 令牌有效 → 无 checks → authStateOk
    ↓
✅ 放行（普通用户也能看自己的应用）
```

#### 示例 B：普通用户用客户端令牌删除自己的应用（RequireElevatedClient，已提升）
```
handleUser() → Skip（无 Basic Auth）
    ↓
handleClient(checkClientElevated) → 令牌有效
    ↓
checkClientElevated → ElevatedUntil > now → Ok
    ↓
✅ 放行（⚠️ 普通用户也能删自己的应用！不走管理员判定）
```

#### 示例 C：普通用户用客户端令牌访问 `GET /user`（RequireAdmin）
```
handleUser(checkUserAdmin) → Skip（无 Basic Auth）
    ↓
handleClient(checkClientAdmin, checkClientElevated) → 令牌有效
    ↓
checkClientAdmin → user.Admin = false → authStateForbidden
    ↓
❌ 403 Forbidden（这才需要管理员）
```

#### 示例 D：管理员用已过期提升的客户端令牌访问 `GET /user`（RequireAdmin）
```
handleUser(checkUserAdmin) → Skip（无 Basic Auth）
    ↓
handleClient(checkClientAdmin, checkClientElevated) → 令牌有效
    ↓
checkClientAdmin → user.Admin = true → Ok（继续下一个 check）
    ↓
checkClientElevated → ElevatedUntil 已过期 → authStateNotElevated
    ↓
❌ 403: "session not elevated, use basic auth or call /client:elevate"
```

#### 示例 E：管理员用 Basic Auth 访问 `GET /user`（RequireAdmin）
```
handleUser(checkUserAdmin) → 密码正确
    ↓
checkUserAdmin → user.Admin = true → authStateOk
    ↓
✅ 放行（Basic Auth 不需要提升检查）
```

---

### 2.8 认证状态枚举与响应映射

| 状态 | 值 | 含义 | 触发的 HTTP 响应 |
|-----|-----|------|-----------------|
| `authStateSkip` | 0 | 当前认证方式不适用，尝试下一种 | 继续循环 |
| `authStateForbidden` | 1 | 管理员判定失败 | 403 Forbidden: "you are not allowed to access this api" |
| `authStateNotElevated` | 2 | 权限提升判定失败 | 403 Forbidden: "session not elevated, use basic auth or call /client:elevate" |
| `authStateOk` | 3 | 全部检查通过 | 放行 → `ctx.Next()` |

**关键区别**：`Forbidden` 和 `NotElevated` 都返回 403，但**错误信息不同**，前端可以据此判断是"非管理员"还是"需要提升"。

---

## 三、权限提升请求的校验与执行流程

### 3.1 权限提升 API 调用链

**API 端点**：`POST /client/{id}/elevate` (`api/client.go:249-312`)

**请求体** (`model/elevate.go:8-14`)：
```json
{
    "durationSeconds": 900  // 提升持续时间（秒）
}
```

**处理流程** (`api/client.go:287-312`)：

```
ElevateClient(ctx)
    ↓
绑定 ElevateRequest 参数
    ↓
GetClientByID(id) → 检查客户端是否存在
    ↓
验证 client.UserID == auth.GetUserID(ctx) → 确保是自己的客户端
    ↓
计算 elevatedUntil = time.Now().Add(durationSeconds)
    ↓
UpdateClientElevatedUntil(client.ID, &elevatedUntil) → 持久化到数据库
    ↓
返回 204 No Content
```

**关键代码** (`api/client.go:304-308`)：
```go
elevatedUntil := time.Now().Add(time.Duration(params.DurationSeconds) * time.Second)
if err := a.DB.UpdateClientElevatedUntil(client.ID, &elevatedUntil); err != nil {
    ctx.AbortWithError(500, err)
    return
}
```

**数据库操作** (`database/client.go:66-69`)：
```go
func (d *GormDatabase) UpdateClientElevatedUntil(id uint, t *time.Time) error {
    return d.DB.Model(&model.Client{}).Where("id = ?", id).Update("elevated_until", t).Error
}
```

### 3.2 前端提升流程（两种方式）

#### 方式一：本地密码认证

**前端代码** (`ui/src/ElevateStore.ts:34-45`)：

```typescript
public localElevate = async (password: string, durationSeconds: number): Promise<void> => {
    await axios.create().request({
        url: `${config.get('url')}client/${this.currentUser.user.clientId}/elevate`,
        method: 'POST',
        data: {durationSeconds},
        headers: {
            Authorization: 'Basic ' + btoa(this.currentUser.user.name + ':' + password),
        },
    });
    await this.currentUser.tryAuthenticate();  // 刷新用户状态
};
```

**关键点**：
- 使用 Basic Auth 携带密码请求提升
- 成功后调用 `tryAuthenticate()` 刷新 `elevatedUntil`

#### 方式二：OIDC 认证

**流程** (`ui/src/ElevateStore.ts:47-88`)：

```
oidcElevate(durationSeconds)
    ↓
打开 popup 窗口 → /auth/oidc/elevate?id=clientId&durationSeconds=...
    ↓
服务端生成 state，存储到 pendingSessions（10 分钟有效期）
    ↓
重定向到 OIDC 提供商登录
    ↓
回调到 /auth/oidc/callback
    ↓
handleElevationCallback() → 验证后更新 ElevatedUntil
    ↓
前端轮询 popup 关闭，然后刷新用户状态
```

**服务端 OIDC 提升处理** (`api/oidc.go:235-266`)：

```go
func (a *OIDCAPI) handleElevationCallback(w http.ResponseWriter, elevate *pendingElevation, user *model.User) {
    client, _ := a.DB.GetClientByID(elevate.ClientID)
    // 验证 client.UserID == user.ID
    elevatedUntil := time.Now().Add(time.Duration(elevate.DurationSeconds) * time.Second)
    a.DB.UpdateClientElevatedUntil(client.ID, &elevatedUntil)
}
```

### 3.3 运行时权限注入

每次请求通过中间件时，权限检查是**实时**进行的：

1. **请求到达** → 中间件 `RequireElevatedClient` 执行
2. **提取令牌** → 从 Cookie/Header/Query 获取 token
3. **查询客户端** → `GetClientByToken(token)` 获取完整 Client 对象
4. **权限检查** → `checkClientElevated(client)` 比较 `ElevatedUntil` 与当前时间
5. **上下文注册** → `RegisterClient(ctx, client)` 将完整客户端信息存入请求上下文
6. **后续使用** → 业务处理通过 `auth.GetClient(ctx)` 获取客户端信息

**关键点**：
- 没有单独的"权限缓存"机制
- 每次请求都从数据库获取最新的 `ElevatedUntil`
- 因此提升权限后，后续请求立即生效

---

## 四、异常情况：权限回收与会话删除流程（已校正）

### 4.1 权限自然过期

**机制**：`ElevatedUntil` 时间戳比较

**检查时机**：
- 每次请求进入 `RequireElevatedClient` 中间件时
- `checkClientElevated()` 函数实时比较

**过期表现**：
- 返回 403 Forbidden
- 错误信息：`"session not elevated, use basic auth or call /client:elevate"`

**代码** (`auth/authentication.go:259-260`)：
```go
if client.ElevatedUntil == nil || !timeNow().Before(*client.ElevatedUntil) {
    return authStateNotElevated, nil
}
```

**前端感知** (`ui/src/ElevateStore.ts:19-32`)：
```typescript
public refreshElevated = (): number => {
    const elevatedUntil = this.currentUser.user.elevatedUntil;
    if (!elevatedUntil) {
        this.elevated = false;
        return 0;
    }
    const ms = new Date(elevatedUntil).getTime() - 30_000 - Date.now();
    if (ms <= 0) {
        this.elevated = false;
        return 0;
    }
    this.elevated = true;
    return ms;  // 返回剩余毫秒数，可用于设置定时器
};
```

### 4.2 主动取消提升

**前端选项** (`ui/src/client/ElevateClientDialog.tsx:21-27`)：

```typescript
const durationOptions = [
    {label: 'Cancel elevation', seconds: -1},  // 取消提升
    {label: '1 hour', seconds: 60 * 60},
    // ...
];
```

**服务端处理**：
- `durationSeconds = -1` 时，`elevatedUntil = now + (-1)秒`
- 即设置为过去的时间，下次检查立即失效

### 4.3 用户登出（会话删除）

**API**：`POST /auth/logout` (`api/session.go:123-138`)

**处理流程**：

```
Logout(ctx)
    ↓
SetCookie(ctx.Writer, "", -1, a.SecureCookie)  // 清除 Cookie（MaxAge=-1）
    ↓
client := auth.GetClient(ctx)  // 获取当前客户端
    ↓
NotifyDeleted(client.UserID, client.Token)  // 通知流式连接断开
    ↓
DeleteClientByID(client.ID)  // 从数据库删除客户端记录
```

**关键代码** (`api/session.go:124-135`)：
```go
auth.SetCookie(ctx.Writer, "", -1, a.SecureCookie)  // 清除 Cookie
client := auth.GetClient(ctx)
a.NotifyDeleted(client.UserID, client.Token)  // 通知 stream handler
a.DB.DeleteClientByID(client.ID)
```

**数据库删除** (`database/client.go:51-54`)：
```go
func (d *GormDatabase) DeleteClientByID(id uint) error {
    return d.DB.Where("id = ?", id).Delete(&model.Client{}).Error
}
```

### 4.4 用户被删除时的真实清理流程（已校正：不是自动级联）

**重要发现**：系统**没有**使用数据库外键级联删除，而是通过应用层代码**显式逐步清理**。

**证据 1**：GORM 配置禁用了外键约束 (`database/database.go:38`)：
```go
gormConfig := &gorm.Config{
    DisableForeignKeyConstraintWhenMigrating: true,  // 关键！
    // ...
}
```

**证据 2**：真实的清理代码 (`database/user.go:54-69`)：

```go
func (d *GormDatabase) DeleteUserByID(id uint) error {
    // 第一步：清理所有 Applications
    apps, _ := d.GetApplicationsByUser(id)
    for _, app := range apps {
        d.DeleteApplicationByID(app.ID)  // 每个 Application 内部还要清理 Messages
    }
    
    // 第二步：清理所有 Clients（会话/令牌）
    clients, _ := d.GetClientsByUser(id)
    for _, client := range clients {
        d.DeleteClientByID(client.ID)
    }
    
    // 第三步：清理所有 Plugin 配置
    pluginConfs, _ := d.GetPluginConfByUser(id)
    for _, conf := range pluginConfs {
        d.DeletePluginConfByID(conf.ID)
    }
    
    // 第四步：最后才删除 User 本身
    return d.DB.Where("id = ?", id).Delete(&model.User{}).Error
}
```

### 4.5 清理流程的真实执行顺序（详细展开）

**API 层触发** (`api/user.go:329-353`)：

```
DeleteUserByID(ctx)  [API Handler]
    ↓
检查是否是最后一个管理员（如果是 admin 用户）
    ↓
fireUserDeleted(id)  → 应用层回调（先断开连接）
    ↓
DeleteUserByID(id)   → 数据库层逐步清理（见下方详细流程）
```

**应用层回调** (`router/router.go:103-105`)：
```go
userChangeNotifier.OnUserDeleted(streamHandler.NotifyDeletedUser)   // 断开 WebSocket
userChangeNotifier.OnUserDeleted(pluginManager.RemoveUser)          // 卸载插件
```

**数据库层逐步清理的完整顺序** (`database/user.go:54-69` + `database/application.go:58-61` + `database/message.go:83-86`)：

```
DeleteUserByID(userID)
    │
    ├─→ 【第一层：遍历 Applications】
    │       GetApplicationsByUser(userID) → [app1, app2, ...]
    │       │
    │       └─→ 对每个 app 调用 DeleteApplicationByID(app.ID)
    │               │
    │               ├─→ 【第二层：删除该 App 的所有 Messages】
    │               │       DeleteMessagesByApplication(app.ID)
    │               │       → DELETE FROM messages WHERE application_id = ?
    │               │
    │               └─→ 删除 Application 记录本身
    │                       DELETE FROM applications WHERE id = ?
    │
    ├─→ 【第三层：遍历 Clients（会话/令牌）】
    │       GetClientsByUser(userID) → [client1, client2, ...]
    │       │
    │       └─→ 对每个 client 调用 DeleteClientByID(client.ID)
    │               DELETE FROM clients WHERE id = ?
    │               （客户端令牌失效 = 会话被强制登出）
    │
    ├─→ 【第四层：遍历 Plugin 配置】
    │       GetPluginConfByUser(userID) → [conf1, conf2, ...]
    │       │
    │       └─→ 对每个 conf 调用 DeletePluginConfByID(conf.ID)
    │               DELETE FROM plugin_confs WHERE id = ?
    │
    └─→ 【第五层：最后删除 User】
            DELETE FROM users WHERE id = ?
```

### 4.6 清理流程的责任边界

| 阶段 | 执行者 | 清理内容 | 代码位置 |
|-----|-------|---------|---------|
| 阶段 0 | `api/user.go` | 触发回调：断开 WebSocket、卸载插件 | `fireUserDeleted()` |
| 阶段 1 | `database/user.go` | 遍历 Applications | 第 56-59 行 |
| 阶段 2 | `database/application.go` | 每个 App 先删 Messages，再删 App | `DeleteApplicationByID()` |
| 阶段 3 | `database/user.go` | 遍历删除所有 Clients | 第 60-63 行 |
| 阶段 4 | `database/user.go` | 遍历删除所有 PluginConfs | 第 64-67 行 |
| 阶段 5 | `database/user.go` | 最后删除 User 记录 | 第 68 行 |

**关键区别**（与之前错误描述对比）：

| 项目 | 之前错误描述 | 真实情况 |
|-----|------------|---------|
| 触发方式 | GORM 自动级联 | 应用层代码显式循环删除 |
| 外键约束 | 数据库迁移时创建 | `DisableForeignKeyConstraintWhenMigrating: true` 已禁用 |
| 清理顺序 | 数据库自动决定 | 代码严格控制：Apps→Messages→Clients→Plugins→User |
| Messages 清理 | 随 User 级联 | 随 Application 逐个清理（两层循环） |

### 4.7 令牌主动失效

删除客户端记录即等同于令牌失效：

1. **数据库层面**：Client 记录被删除
2. **认证层面**：下次请求 `GetClientByToken(token)` 返回 nil
3. **结果**：中间件返回 `authStateSkip`，最终 401 Unauthorized

### 4.8 流式连接的断开通知

当客户端/用户被删除时，WebSocket 连接需要断开：

**通知机制** (`router/router.go:83, 89`)：
```go
clientHandler := api.ClientAPI{
    NotifyDeleted: streamHandler.NotifyDeletedClient,
}
sessionHandler := api.SessionAPI{
    NotifyDeleted: streamHandler.NotifyDeletedClient,
}
```

**Stream Handler** 维护活跃连接，收到删除通知后断开对应 token 的连接。

---

## 五、关键数据结构总结

### 5.1 User 表 (`model/user.go:6-14`)

| 字段 | 类型 | 说明 |
|-----|------|------|
| ID | uint | 主键 |
| Name | string | 用户名（唯一） |
| Pass | []byte | 密码哈希 |
| Admin | bool | 是否管理员 |

### 5.2 Client 表 (`model/client.go:10-38`)

| 字段 | 类型 | 说明 |
|-----|------|------|
| ID | uint | 主键 |
| Token | string | 客户端令牌（唯一，C 前缀） |
| UserID | uint | 外键，关联 User |
| Name | string | 客户端名称 |
| LastUsed | *time.Time | 最后使用时间 |
| ElevatedUntil | *time.Time | 权限提升截止时间 |

### 5.3 认证状态枚举 (`auth/authentication.go:15-20`)

| 状态 | 值 | 说明 |
|-----|-----|------|
| authStateSkip | 0 | 跳过当前认证方式，尝试下一种 |
| authStateForbidden | 1 | 认证通过但权限不足（403） |
| authStateNotElevated | 2 | 会话未提升（403） |
| authStateOk | 3 | 认证和权限检查通过 |

---

## 六、完整流程时序图

### 6.1 登录与会话创建

```
用户浏览器                    服务端                      数据库
   |                            |                           |
   | POST /auth/local/login     |                           |
   | (Basic Auth + 客户端名)     |                           |
   |--------------------------->|                           |
   |                            | GetUserByName(name)       |
   |                            |-------------------------->|
   |                            |<--------------------------|
   |                            | 验证密码                   |
   |                            |                           |
   |                            | GenerateClientToken()     |
   |                            | CreateClient()            |
   |                            |-------------------------->|
   |                            |<--------------------------|
   |                            | Set-Cookie: token         |
   | Set-Cookie + CurrentUser   |                           |
   |<---------------------------|                           |
   |                            |                           |
```

### 6.2 权限提升流程

```
用户浏览器                    服务端                      数据库
   |                            |                           |
   | 执行敏感操作（如删除客户端）  |                           |
   |--------------------------->|                           |
   |                            | RequireElevatedClient     |
   |                            | 检查 ElevatedUntil        |
   |                            |                           |
   |         ← 403 未提升        |                           |
   |                            |                           |
   | 弹出 ElevationForm         |                           |
   | 输入密码 / OIDC 登录        |                           |
   |                            |                           |
   | POST /client/:id/elevate   |                           |
   | (Basic Auth + duration)    |                           |
   |--------------------------->|                           |
   |                            | 验证密码 / OIDC 回调       |                           |
   |                            |                           |
   |                            | UpdateClientElevatedUntil |
   |                            |-------------------------->|
   |                            |<--------------------------|
   |         ← 204              |                           |
   |                            |                           |
   | GET /current/user          |                           |
   |--------------------------->|                           |
   |         ← {elevatedUntil}  |                           |
   |                            |                           |
   | 重试敏感操作                |                           |
   |--------------------------->|                           |
   |                            | checkClientElevated OK    |
   |         ← 操作成功          |                           |
```

### 6.3 登出与会话销毁

```
用户浏览器                    服务端                      数据库
   |                            |                           |
   | POST /auth/logout          |                           |
   | (Cookie: token)            |                           |
   |--------------------------->|                           |
   |                            | GetClientByToken(token)   |
   |                            |-------------------------->|
   |                            |<--------------------------|
   |                            |                           |
   |                            | NotifyDeleted(userID, token)
   |                            | → 断开 WebSocket          |
   |                            |                           |
   |                            | DeleteClientByID(id)      |
   |                            |-------------------------->|
   |                            |<--------------------------|
   |                            |                           |
   | Set-Cookie: MaxAge=-1      |                           |
   |<---------------------------|                           |
   |                            |                           |
```

### 6.4 用户删除时的逐步清理（真实流程）

```
管理员请求 DELETE /user/:id
        |
        ▼
┌─────────────────────────────────┐
│ api/user.go: DeleteUserByID     │
│ 1. 检查是否是最后一个管理员       │
│ 2. fireUserDeleted(id)          │
│    ├── NotifyDeletedUser()      │ ← 断开所有 WebSocket
│    └── pluginManager.RemoveUser │ ← 卸载用户插件
│ 3. 调用 database.DeleteUserByID │
└──────────────┬──────────────────┘
               │
               ▼
┌────────────────────────────────────────────────────┐
│ database/user.go: DeleteUserByID(id)              │
│                                                    │
│ 【第一步】清理 Applications                        │
│   apps = GetApplicationsByUser(id)                 │
│   for app in apps:                                 │
│       DeleteApplicationByID(app.ID) ─┐             │
│                                     ▼             │
│          ┌─────────────────────────────────────┐  │
│          │ DeleteApplicationByID(app.ID)       │  │
│          │ 1. DeleteMessagesByApplication(id)  │  │
│          │    → 删该 App 所有消息              │  │
│          │ 2. DELETE FROM applications         │  │
│          └─────────────────────────────────────┘  │
│                                                    │
│ 【第二步】清理 Clients（会话/令牌）                │
│   clients = GetClientsByUser(id)                  │
│   for client in clients:                          │
│       DeleteClientByID(client.ID)                 │
│       → 令牌失效，会话强制登出                      │
│                                                    │
│ 【第三步】清理 PluginConfs                         │
│   plugins = GetPluginConfByUser(id)               │
│   for conf in plugins:                            │
│       DeletePluginConfByID(conf.ID)               │
│                                                    │
│ 【第四步】最后删除 User 记录                        │
│   DELETE FROM users WHERE id = ?                  │
└────────────────────────────────────────────────────┘
```

---

## 七、安全特性总结

### 7.1 令牌安全
- 22 字符随机令牌，约 2^132.5 的密钥空间
- 使用 `crypto/rand` 而非伪随机
- 数据库唯一索引防止重复

### 7.2 Cookie 安全
- `HttpOnly` - 防止 XSS 窃取
- `SameSite=Strict` - 防止 CSRF
- `Secure` - HTTPS 环境下只通过加密连接传输
- 7 天有效期，但会话可随时通过登出销毁

### 7.3 权限提升安全
- 需重新验证身份（密码或 OIDC）
- 提升有时间限制（默认可选 1 小时 ~ 1 年）
- 敏感操作要求提升状态，减少长期权限暴露

### 7.4 边界检查
- 只能提升自己的客户端（`client.UserID == auth.GetUserID(ctx)`）
- 管理员操作需要用户本身是 Admin
- 不能删除最后一个管理员

### 7.5 清理安全（已校正）
- 应用层显式控制清理顺序，而非依赖数据库级联
- 先断开连接，再清理数据，避免访问已删除资源
- Messages 随 Application 逐层清理，确保数据一致性

---

## 八、校正要点对照表

| 编号 | 原描述 | 校正后 | 证据代码 |
|-----|-------|--------|---------|
| 1 | 决策链：认证→权限→管理员 | 决策链：认证入口→管理员判定→权限判定 | `RequireAdmin` 中 checks 顺序是 `checkClientAdmin, checkClientElevated` |
| 2 | 用户删除时 GORM 自动级联 | 应用层显式逐步清理，禁用了外键 | `database/user.go:54-69` + `DisableForeignKeyConstraintWhenMigrating: true` |
| 3 | Clients/Apps 随 User 自动删除 | 代码循环遍历：Apps(含 Messages)→Clients→Plugins→User | `database/user.go:56-68` 五步清理 |

---

## 九、相关文件索引

| 文件 | 职责 |
|-----|------|
| `auth/authentication.go` | 认证中间件、决策链核心、权限检查逻辑 |
| `auth/token.go` | 令牌生成 |
| `auth/cookie.go` | Cookie 管理 |
| `auth/util.go` | 上下文认证信息存取 |
| `api/session.go` | 登录/登出 API |
| `api/client.go` | 客户端管理 + 权限提升 API |
| `api/oidc.go` | OIDC 认证与提升 |
| `api/user.go` | 用户管理 API |
| `database/user.go` | ⭐ 用户删除时的逐步清理逻辑 |
| `database/application.go` | 应用删除时的消息清理 |
| `database/client.go` | Client 数据库操作 |
| `database/database.go` | GORM 配置（禁用外键约束） |
| `model/user.go` | User 模型定义 |
| `model/client.go` | Client 模型定义 |
| `model/elevate.go` | 提升请求模型 |
| `router/router.go` | 路由与中间件配置 |
| `ui/src/ElevateStore.ts` | 前端权限提升状态管理 |
| `ui/src/client/ElevateClientDialog.tsx` | 前端提升对话框 |
| `ui/src/common/ElevationForm.tsx` | 前端密码/OIDC 提升表单 |
