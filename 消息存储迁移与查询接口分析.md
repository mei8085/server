# 消息存储迁移路径与查询接口分析

## 一、数据库版本检查与迁移执行

### 1.1 迁移入口与初始化流程

数据库初始化通过 `database.New()` 函数完成，核心迁移逻辑位于 `database/database.go:27-97`：

```go
func New(dialect, connection, defaultUser, defaultPass string, strength int, createDefaultUserIfNotExist bool) (*GormDatabase, error) {
    // 1. SQLite 目录创建
    createDirectoryIfSqlite(dialect, connection)
    
    // 2. 数据库连接与配置
    db, err := gorm.Open(...)
    
    // 3. 连接池配置
    sqldb.SetMaxOpenConns(10)
    // SQLite 特殊处理：限制为单连接
    
    // 4. 自动迁移 Schema
    db.AutoMigrate(new(model.User), new(model.Application), new(model.Message), new(model.Client), new(model.PluginConf))
    
    // 5. 默认用户创建（空数据库时）
    if createDefaultUserIfNotExist && userCount == 0 {
        db.Create(&model.User{...})
    }
    
    // 6. 数据迁移：填充缺失字段
    db.Transaction(fillMissingSortKeys, &sql.TxOptions{Isolation: sql.LevelSerializable})
}
```

### 1.2 新旧 Schema 兼容策略

系统采用**渐进式迁移 + 向后兼容**的策略，核心体现在两个层面：

#### 1.2.1 GORM AutoMigrate 机制
- **位置**：`database/database.go:83`
- **特性**：GORM 的 `AutoMigrate` 仅创建缺失的表和列，不修改现有列或删除数据
- **兼容表**：`User`、`Application`、`Message`、`Client`、`PluginConf`

#### 1.2.2 显式数据迁移：Application SortKey
从迁移测试 `database/migration_test.go` 可见系统支持从旧版本升级：

**旧版本状态**（测试模拟）：
- 仅有 `User` 表
- 无 `Application` 表
- 无 `SortKey` 字段

**迁移后状态**：
- `Application` 表被创建
- 旧用户数据保留
- `SortKey` 字段自动填充

**迁移函数 `fillMissingSortKeys`**（`database/database.go:100-131`）：

```go
func fillMissingSortKeys(db *gorm.DB) error {
    // 检查是否有缺失 sort_key 的应用
    db.Model(new(model.Application)).Where("sort_key IS NULL OR sort_key = ''").Count(&missingSort)
    
    if missingSort == 0 {
        return nil  // 无需迁移
    }
    
    // 按用户分组，生成有序的排序键
    for _, app := range apps {
        sortKey, _ = fracdex.KeyBetween(sortKey, "")  // 分数索引
        app.SortKey = sortKey
    }
    return db.Save(apps).Error
}
```

**兼容策略要点**：
1. **幂等性**：先检查是否需要迁移，已完成则直接跳过
2. **事务保护**：使用 `Serializable` 隔离级别，确保并发安全
3. **数据保留**：仅新增/更新，不删除任何已有数据
4. **分组隔离**：按 `user_id` 分组生成排序键，不同用户互不影响

## 二、消息查询接口分析

### 2.1 查询接口总览

| 接口函数 | 数据库层位置 | API 层位置 | 用途 |
|---------|-------------|-----------|------|
| `GetMessageByID` | `database/message.go:9-19` | - | 单条消息查询 |
| `GetMessagesByUser` | `database/message.go:27-35` | - | 用户全部消息（无分页） |
| `GetMessagesByUserSince` | `database/message.go:39-51` | `api/message.go:88-98` | 用户分页消息 |
| `GetMessagesByApplication` | `database/message.go:54-61` | - | 应用全部消息（无分页） |
| `GetMessagesByApplicationSince` | `database/message.go:65-76` | `api/message.go:179-198` | 应用分页消息 |

### 2.2 典型查询场景分析

#### 2.2.1 按应用检索

**数据库实现**（`database/message.go:65-76`）：

```go
func (d *GormDatabase) GetMessagesByApplicationSince(appID uint, limit int, since uint) ([]*model.Message, error) {
    db := d.DB.Where("application_id = ?", appID).Order("messages.id desc").Limit(limit)
    if since != 0 {
        db = db.Where("messages.id < ?", since)
    }
    err := db.Find(&messages).Error
    return messages, err
}
```

**查询条件**：
- `application_id = ?`：应用维度过滤
- `messages.id < ?`：时间窗口（通过 since 参数）
- `Order("messages.id desc")`：按 ID 倒序（等价于按创建时间倒序）

**API 接口**（`api/message.go:179`）：`GET /application/{id}/message`

#### 2.2.2 按用户检索

**数据库实现**（`database/message.go:39-51`）：

```go
func (d *GormDatabase) GetMessagesByUserSince(userID uint, limit int, since uint) ([]*model.Message, error) {
    db := d.DB.Joins("JOIN applications ON applications.user_id = ?", userID).
        Where("messages.application_id = applications.id").
        Order("messages.id desc").Limit(limit)
    if since != 0 {
        db = db.Where("messages.id < ?", since)
    }
    err := db.Find(&messages).Error
    return messages, err
}
```

**关键设计**：
- 使用 `JOIN` 关联 `applications` 表获取用户的所有应用
- 通过 `applications.user_id` 过滤，而非直接在 `messages` 表存储 `user_id`
- 减少冗余：`messages` 仅存 `application_id`，用户归属通过关联表解析

**API 接口**（`api/message.go:88`）：`GET /message`

#### 2.2.3 按时间窗口检索

系统采用**基于消息 ID 的游标分页**策略，而非传统的 offset/limit 或日期范围：

| 机制 | 实现方式 | 位置 |
|-----|---------|------|
| 起始标记 | `since = 0` 表示从最新消息开始 | `database/message.go:44` |
| 游标前进 | `since = 最后一条消息的 ID` | `api/message.go:106` |
| 过滤条件 | `messages.id < since` | `database/message.go:44` |

**优势**：
- 避免大 offset 的性能问题
- 消息插入/删除不会导致游标偏移
- ID 递增天然保证时间顺序（自增主键）

### 2.3 分页与排序的数据库层实现

#### 2.3.1 分页参数模型

```go
type pagingParams struct {
    Limit int  `form:"limit" binding:"min=1,max=200"`  // 默认 100
    Since uint `form:"since" binding:"min=0"`          // 默认 0
}
```

#### 2.3.2 预读 +1 策略

API 层巧妙使用 `limit + 1` 策略检测是否还有下一页（`api/message.go:92, 188`）：

```go
// 查询 limit + 1 条
messages, err := a.DB.GetMessagesByUserSince(userID, params.Limit+1, params.Since)

// 构建响应时：
if len(messages) > paging.Limit {
    useMessages = messages[:len(messages)-1]           // 截取 limit 条
    since = useMessages[len(useMessages)-1].ID         // 下一页游标
    // 构建 next 链接
}
```

**分页响应结构**（`model/paging.go`）：

```go
type PagedMessages struct {
    Paging   Paging                // 分页元信息
    Messages []*MessageExternal    // 消息列表
}

type Paging struct {
    Next  string  // 下一页 URL
    Size  int     // 当前页条数
    Since uint    // 当前页游标
    Limit int     // 每页限制
}
```

#### 2.3.3 排序策略

**统一排序规则**：`Order("messages.id desc")`

- 所有查询接口均使用**消息 ID 倒序**
- 利用自增主键的单调性：ID 越大 = 创建时间越晚
- 无需额外索引或日期字段
- 与 `since` 游标机制天然配合

## 三、历史消息清理逻辑与查询接口关联

### 3.1 清理接口总览

| 清理函数 | 位置 | 作用范围 |
|---------|------|---------|
| `DeleteMessageByID` | `database/message.go:79-81` | 单条消息 |
| `DeleteMessagesByApplication` | `database/message.go:84-86` | 应用维度 |
| `DeleteMessagesByUser` | `database/message.go:89-94` | 用户维度 |

### 3.2 清理实现分析

#### 3.2.1 单条删除

```go
func (d *GormDatabase) DeleteMessageByID(id uint) error {
    return d.DB.Where("id = ?", id).Delete(&model.Message{}).Error
}
```

#### 3.2.2 应用维度清理

```go
func (d *GormDatabase) DeleteMessagesByApplication(applicationID uint) error {
    return d.DB.Where("application_id = ?", applicationID).Delete(&model.Message{}).Error
}
```

**与查询的对应关系**：
- 查询条件：`application_id = ?`
- 删除条件：`application_id = ?`
- 清理后，`GetMessagesByApplication` 将返回空结果

#### 3.2.3 用户维度清理

```go
func (d *GormDatabase) DeleteMessagesByUser(userID uint) error {
    app, _ := d.GetApplicationsByUser(userID)
    for _, app := range app {
        d.DeleteMessagesByApplication(app.ID)
    }
    return nil
}
```

**设计特点**：
- 复用 `DeleteMessagesByApplication` 实现
- 先获取用户所有应用，再逐个清理
- 与查询接口 `GetMessagesByUser` 的 JOIN 策略保持一致

### 3.3 清理与查询的关联

#### 3.3.1 清理对分页查询的影响

由于采用**基于 ID 的游标分页**而非日期范围，消息删除不会导致分页混乱：

| 场景 | 传统 offset 分页 | ID 游标分页 |
|-----|----------------|------------|
| 删除已读过的消息 | 下一页可能重复/缺失 | 不受影响（since 指向已读位置之后） |
| 删除未读过的消息 | 页码偏移 | 仅影响删除的消息，游标继续有效 |
| 并发插入 | 页码可能变化 | 不影响，since < ID 过滤不受新增影响 |

#### 3.3.2 API 层权限校验

删除接口与查询接口共享相同的权限校验逻辑（`api/message.go`）：

**删除单条消息**（`api/message.go:307-326`）：
1. 查询消息 → 获取 `application_id`
2. 查询应用 → 校验 `app.UserID == 当前用户ID`
3. 执行删除

**删除应用所有消息**（`api/message.go:259-270`）：
1. 查询应用 → 校验 `app.UserID == 当前用户ID`
2. 执行 `DeleteMessagesByApplication`

**删除用户所有消息**（`api/message.go:220-222`）：
- 直接执行 `DeleteMessagesByUser(userID)`
- 已通过认证获取 `userID`，无需额外校验

### 3.4 清理策略总结

| 维度 | 清理函数 | 对应查询 | 调用方式 |
|-----|---------|---------|---------|
| 单条 | `DeleteMessageByID` | `GetMessageByID` | `DELETE /message/{id}` |
| 应用 | `DeleteMessagesByApplication` | `GetMessagesByApplicationSince` | `DELETE /application/{id}/message` |
| 用户 | `DeleteMessagesByUser` | `GetMessagesByUserSince` | `DELETE /message` |

## 四、系统架构总结

### 4.1 数据流关系

```
消息创建 (CreateMessage)
    ↓
Message 表 (id, application_id, ...)
    ↓
    ├─→ 按应用查询: WHERE application_id = ?
    ├─→ 按用户查询: JOIN applications ON user_id = ?
    └─→ 分页游标: id < since + Order by id desc
    
消息删除 (DeleteMessage*)
    ↓
    ├─→ 单条: WHERE id = ?
    ├─→ 应用: WHERE application_id = ?
    └─→ 用户: 遍历应用并删除
```

### 4.2 设计亮点

1. **Schema 兼容**：`AutoMigrate` + 显式数据迁移函数，支持从旧版本平滑升级
2. **数据冗余最小化**：`messages` 仅存 `application_id`，用户归属通过 JOIN 解析
3. **游标分页高效**：基于 ID 的 since 机制避免 offset 性能问题
4. **清理复用设计**：`DeleteMessagesByUser` 复用应用级删除，保持逻辑一致性
5. **权限统一校验**：删除前先查询确认归属，与查询接口使用相同的数据库函数
