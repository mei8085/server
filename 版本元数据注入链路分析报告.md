# 构建版本/提交散列元数据注入与展示链路分析报告

## 概述

构建版本（Version）、提交散列（Commit）、构建日期（BuildDate）三类元数据从编译期注入到最终展示给用户，**全程共 7 跳**，存在两条并行的输出路径：
- **路径 A**：服务端模板注入 → 前端 UI 渲染（供浏览器用户查看）
- **路径 B**：REST API JSON 输出（供外部程序/脚本调用）

---

## 链路总览图

```
┌───────────────────────────────────────────────────────────────────────────────┐
│  第1跳  CI 环境变量 (GitHub Actions)                                           │
│           ↓ LD_FLAGS                                                           │
│  第2跳  Makefile → go build -ldflags "-X main.Version=... -X main.Commit=..." │
│           ↓ 编译期符号替换                                                      │
│  第3跳  Go 包级变量 (app.go L16-L25)                                           │
│           ↓ 结构体封装                                                          │
│  第4跳  model.VersionInfo 对象 (app.go L28)                                    │
│           ↓                                                                   │
│     ┌─────┴───────────────────────────────────────┐                           │
│     ↓ 路径A：服务端模板注入                         ↓ 路径B：REST API          │
│  第5跳  ui.Register(*vInfo)                   第5跳  /version + /gotifyinfo    │
│     ↓ JSON序列化 + 字符串替换                     ↓ JSON 序列化               │
│  第6跳  index.html → window.config             第6跳  HTTP Response Body       │
│     ↓ JS 全局变量读取                             ↓                           │
│  第7跳  config.ts → Layout.tsx → Header.tsx   第7跳  调用方解析 JSON           │
│           ↓ DOM 渲染                                                           │
│        用户可见：@{version} 超链接                                               │
└───────────────────────────────────────────────────────────────────────────────┘
```

---

## 每一跳详细分析

### 第 1 跳：CI 环境变量构建 LD_FLAGS

- **载体**：GitHub Actions `$GITHUB_ENV` 环境变量文件
- **源文件**：[build.yml](file:///d:/fz/0601-1/solo-dogfeeding/code/11-server/.github/workflows/build.yml#L29-L39)
- **关键代码**：

```yaml
- if: startsWith(github.ref, 'refs/tags/v')
  run: echo "VERSION=${GITHUB_REF/refs\/tags\/v/}" >> $GITHUB_ENV
- if: startsWith(github.ref, 'refs/tags/v')
  run: |
    export LD_FLAGS="-w -s -X main.Version=$VERSION -X main.BuildDate=$(date "+%F-%T") -X main.Commit=$(git rev-parse --verify HEAD) -X main.Mode=prod"
    echo "LD_FLAGS=$LD_FLAGS" >> $GITHUB_ENV
```

- **数据提取逻辑**：
  - `Version`：从 Git tag `vX.Y.Z` 提取前缀去除后的 `X.Y.Z`
  - `Commit`：`git rev-parse --verify HEAD`（完整 40 位 SHA-1）
  - `BuildDate`：`date "+%F-%T"`（格式 `YYYY-MM-DD-HH:MM:SS`）
  - `Mode`：固定字符串 `prod`

> master 分支构建时另有一套逻辑，见 [build.yml L110-L122](file:///d:/fz/0601-1/solo-dogfeeding/code/11-server/Makefile#L110-L122) 的 `build-docker-multiarch-master`，Version 为 `master-<short commit>`。

---

### 第 2 跳：Makefile → Go 编译器 ldflags 注入

- **载体**：`go build` 的 `-ldflags` 参数 + Docker `--build-arg LD_FLAGS`
- **源文件**：[Makefile](file:///d:/fz/0601-1/solo-dogfeeding/code/11-server/Makefile#L8-L9)
- **关键代码**：

```makefile
DOCKER_RUN=docker run --rm -e LD_FLAGS="$$LD_FLAGS" ...
DOCKER_GO_BUILD=go build -mod=readonly -a -installsuffix cgo -ldflags "$$LD_FLAGS"
```

- **注入原理**：Go 链接器支持通过 `-ldflags "-X <package>.<var>=<value>"` 在编译期重写包级字符串变量的值。此处重写的是 `main` 包的 4 个变量。

---

### 第 3 跳：Go 包级变量初始化（默认值被 ldflags 覆盖）

- **载体**：Go 二进制 `.data` 段中的字符串符号
- **源文件**：[app.go](file:///d:/fz/0601-1/solo-dogfeeding/code/11-server/app.go#L16-L25)
- **关键代码**：

```go
var (
    Version   = "unknown"
    Commit    = "unknown"
    BuildDate = "unknown"
    Mode      = mode.Dev
)
```

- **说明**：若编译时未传 ldflags（本地开发），则使用默认值 `"unknown"` 与 `mode.Dev`，保证程序可正常运行。

---

### 第 4 跳：包级变量 → `model.VersionInfo` 结构体封装

- **载体**：堆上分配的 `*model.VersionInfo` 对象
- **源文件**：[app.go L28](file:///d:/fz/0601-1/solo-dogfeeding/code/11-server/app.go#L28) + [version.go](file:///d:/fz/0601-1/solo-dogfeeding/code/11-server/model/version.go#L6-L22)
- **关键代码**：

```go
vInfo := &model.VersionInfo{Version: Version, Commit: Commit, BuildDate: BuildDate}
```

```go
type VersionInfo struct {
    Version   string `json:"version"`
    Commit    string `json:"commit"`
    BuildDate string `json:"buildDate"`
}
```

- **作用**：将三个独立变量封装为带 JSON tag 的结构体，便于后续通过两种路径输出。

---

### 第 5A 跳（路径 A）：`vInfo` 传入 `ui.Register()`

- **载体**：函数参数值拷贝（`model.VersionInfo` 为值类型）
- **源文件**：[router.go L117](file:///d:/fz/0601-1/solo-dogfeeding/code/11-server/router/router.go#L117)
- **关键代码**：

```go
ui.Register(g, *vInfo, conf.Registration, conf.OIDC.Enabled)
```

---

### 第 5B 跳（路径 B）：`vInfo` 被路由闭包捕获 → API 端点

- **载体**：Gin 路由闭包环境
- **源文件**：[router.go L161-L189](file:///d:/fz/0601-1/solo-dogfeeding/code/11-server/router/router.go#L161-L189)
- **关键代码**：

```go
// GET /version → 返回完整 VersionInfo
g.GET("version", func(ctx *gin.Context) {
    ctx.JSON(200, vInfo)
})

// GET /gotifyinfo → 仅返回 Version（加 register/oidc）
g.GET("gotifyinfo", func(ctx *gin.Context) {
    ctx.JSON(200, &model.GotifyInfo{Version: vInfo.Version, Oidc: conf.OIDC.Enabled, Register: conf.Registration})
})
```

- **两个端点区别**：

| 端点 | 字段 | 认证要求 | 用途 |
|---|---|---|---|
| `GET /version` | version, commit, buildDate | 无 | 排障、详细版本信息 |
| `GET /gotifyinfo` | version, register, oidc | 无 | 登录前 UI 判断功能开关 |

---

### 第 6A 跳（路径 A）：`ui.Register` 内 JSON 序列化 + HTML 占位符替换

- **载体**：内存中 `index.html` 字符串（通过 `strings.Replace` 替换 `%CONFIG%`）
- **源文件**：[serve.go L18-L37](file:///d:/fz/0601-1/solo-dogfeeding/code/11-server/ui/serve.go#L18-L37) + [index.html L38](file:///d:/fz/0601-1/solo-dogfeeding/code/11-server/ui/index.html#L38)
- **关键代码**：

```go
type uiConfig struct {
    Register bool              `json:"register"`
    Version  model.VersionInfo `json:"version"`
    OIDC     bool              `json:"oidc"`
}

func Register(r *gin.Engine, version model.VersionInfo, register, oidcEnabled bool) {
    uiConfigBytes, _ := json.Marshal(uiConfig{Version: version, ...})
    replaceConfig := func(content string) string {
        return strings.Replace(content, "%CONFIG%", string(uiConfigBytes), 1)
    }
    ui.GET("/", serveFile("index.html", "text/html", replaceConfig))
}
```

```html
<script>window.config = %CONFIG%;</script>
```

- **效果**：浏览器收到的 HTML 中会出现类似：
  ```html
  <script>window.config = {"register":true,"version":{"version":"2.5.0","commit":"ae95...","buildDate":"2024-01-01-12:00:00"},"oidc":false};</script>
  ```

---

### 第 6B 跳（路径 B）：HTTP JSON 响应到客户端

- **载体**：HTTP Response Body（`application/json`）
- **示例响应**：
  ```http
  HTTP/1.1 200 OK
  Content-Type: application/json

  {"version":"2.5.0","commit":"ae9512b6...","buildDate":"2024-01-01-12:00:00"}
  ```

---

### 第 7A 跳（路径 A）：前端 `config.ts` 读取 → React 组件渲染

- **载体 1**：ES Module 级别的 `config` 单例对象
- **源文件**：[config.ts](file:///d:/fz/0601-1/solo-dogfeeding/code/11-server/ui/src/config.ts#L16-L22)

```typescript
const config: IConfig = {
    url: 'unset', register: false,
    version: {commit: 'unknown', buildDate: 'unknown', version: 'unknown'},
    oidc: false,
    ...window.config, // ← 从第6A跳的 window.config 展开覆盖
};
```

- **载体 2**：React Props（`version: string`）
- **源文件**：[Layout.tsx L77, L116-L127](file:///d:/fz/0601-1/solo-dogfeeding/code/11-server/ui/src/layout/Layout.tsx#L77)

```tsx
const {version} = config.get('version');  // 仅取 version 字符串
// ...
<Header version={version} ... />
```

- **载体 3**：React DOM（最终用户可见）
- **源文件**：[Header.tsx L114-L124](file:///d:/fz/0601-1/solo-dogfeeding/code/11-server/ui/src/layout/Header.tsx#L114-L124)

```tsx
<a href={
    version.startsWith('master-')
        ? `https://github.com/gotify/server/commit/${version.replace('master-', '')}`
        : `https://github.com/gotify/server/releases/tag/v${version}`
}>
    <Typography variant="button" color="inherit">
        @{version}
    </Typography>
</a>
```

- **用户可见效果**：页面顶部 Gotify 标题旁显示形如 `@2.5.0` 或 `@master-ae9512b` 的可点击超链接，跳转至对应 GitHub Release 或 Commit 页面。

---

### 第 7B 跳（路径 B）：调用方解析 JSON

- **载体**：调用方内存中的 JSON 对象
- **典型消费者**：第三方客户端、自动化脚本、健康检查工具
- **示例调用**：
  ```bash
  curl https://gotify.example.com/version
  ```

---

## 两条路径对比总结

| 维度 | 路径 A（UI 展示） | 路径 B（API 调用） |
|---|---|---|
| **用户** | 浏览器端人类用户 | 脚本/外部程序 |
| **出口** | `window.config` 全局变量 | `/version` `/gotifyinfo` HTTP 端点 |
| **进入前端方式** | SSR 式字符串替换注入 HTML | 前端额外 fetch（本项目未使用此方式） |
| **展示内容** | 仅 `version` 字符串 | 全部 3 字段（`/version`）或仅 `version`（`/gotifyinfo`） |
| **认证** | 无需 | 无需 |
| **跳转数** | 7 跳 | 7 跳（前 4 跳共享） |

---

## 关键设计亮点

1. **双重输出保证可用性**：`/gotifyinfo` 在登录前即可获取 Version + 功能开关，避免首次渲染时额外 API 往返。
2. **默认值兜底**：`app.go` 中包级变量的 `"unknown"` 默认值确保本地 `go run` 无需任何 ldflags 即可运行。
3. **ldflags 与环境变量解耦**：通过 `LD_FLAGS` 作为中间变量，Makefile、Docker、GitHub Actions 可独立修改而不侵入 Go 源码。
