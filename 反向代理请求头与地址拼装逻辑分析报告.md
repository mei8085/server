# 反向代理请求头与地址拼装逻辑分析报告

## 一、概述

在 Gotify Server 项目中，当服务部署在反向代理（如 Nginx、Apache、Kubernetes Ingress 等）后面时，需要通过反向代理透传的请求头来获取客户端真实的访问地址（协议 + 主机名），从而正确拼装对外访问的 URL。本报告详细分析这一机制的实现原理、各场景下 scheme/host/path 的来源，以及容易混淆的配置点。

## 二、核心依赖库：gotify/location

项目使用 `github.com/gotify/location` 库来实现反向代理环境下的地址检测。该库作为 Gin 中间件，自动从请求中提取 scheme（协议）和 host（主机名）信息。

### 2.1 库的初始化

在 [router.go](file:///d:/fz/0601-1/solo-dogfeeding/code/38-server/router/router.go#L43) 中，通过 `location.Default()` 注册中间件：

```go
g.Use(gin.LoggerWithFormatter(logFormatter), gin.Recovery(), gerror.Handler(), location.Default())
```

**注册顺序关键点**：`location.Default()` 在第 43 行注册，HTTPS 重定向中间件在第 46-67 行注册（位置靠后），因此 `location` 中间件**先于** HTTPS 重定向执行，`location.Get(ctx)` 在重定向逻辑中也可以拿到值（但实际并未使用，详见后文）。

## 三、反向代理请求头的作用

### 3.1 涉及的核心请求头

| 请求头 | 作用 | 作用范围 |
|--------|------|----------|
| `X-Forwarded-Proto` | 透传客户端原始访问协议（http / https） | `gotify/location` 库使用 |
| `X-Forwarded-Host` | 透传客户端原始访问的主机名 | `gotify/location` 库使用 |
| `X-Forwarded-For` | 透传客户端真实 IP 地址 | Gin 框架 `ClientIP()` 使用 |

### 3.2 Scheme（协议）解析逻辑

在 `gotify/location` 库的 [config.go](https://github.com/gotify/location/blob/03bc4ad20437/config.go#L46) 中，`resolveScheme` 方法按以下优先级解析协议：

```go
func (l *location) resolveScheme(r *http.Request) string {
    switch {
    case r.Header.Get(l.headers.scheme) == "https":  // 1. X-Forwarded-Proto
        return "https"
    case r.URL.Scheme == "https":                    // 2. URL Scheme
        return "https"
    case r.TLS != nil:                               // 3. TLS 连接状态
        return "https"
    case strings.HasPrefix(r.Proto, "HTTPS"):        // 4. HTTP 协议版本
        return "https"
    default:
        return l.scheme                              // 5. 回退到默认配置
    }
}
```

**优先级说明（从高到低）：**
1. **`X-Forwarded-Proto` 请求头**：反向代理透传的原始协议，优先级最高
2. **`r.URL.Scheme`**：请求 URL 中的协议部分（Go `net/http` 中此字段通常为空）
3. **`r.TLS`**：当前连接是否为 TLS 连接（直接 HTTPS 时非 nil）
4. **`r.Proto`**：HTTP 协议字符串（如 "HTTP/1.1"，实际不会出现 "HTTPS" 前缀）
5. **默认配置值**：当以上都无法确定时，使用默认值

> **注意**：在 Go 的 `net/http` 标准库中，`r.URL.Scheme` 和 `r.URL.Host` 在服务端通常是空字符串，所以第 2、4 项基本是兜底，真正起作用的是第 1、3、5 项。

### 3.3 Host（主机名）解析逻辑

同样在 [config.go](https://github.com/gotify/location/blob/03bc4ad20437/config.go#L59) 中，`resolveHost` 方法按以下优先级解析主机：

```go
func (l *location) resolveHost(r *http.Request) (host string) {
    switch {
    case r.Header.Get(l.headers.host) != "":   // 1. X-Forwarded-Host
        return r.Header.Get(l.headers.host)
    case r.Header.Get("X-Host") != "":         // 2. X-Host
        return r.Host
    case r.Host != "":                         // 3. Host 请求头
        return r.Host
    case r.URL.Host != "":                     // 4. URL Host
        return r.URL.Host
    default:
        return l.host                          // 5. 回退到默认配置
    }
}
```

**优先级说明（从高到低）：**
1. **`X-Forwarded-Host` 请求头**：反向代理透传的原始主机名，优先级最高
2. **`X-Host` 请求头**：备用的主机头（较少使用）
3. **`r.Host`**：HTTP 请求的 Host 头（反向代理通常会改写或保留）
4. **`r.URL.Host`**：请求 URL 中的主机部分（服务端通常为空）
5. **默认配置值**：当以上都无法确定时，使用默认值

## 四、配置缺省时的回退逻辑

### 4.1 默认配置值

当使用 `location.Default()` 时，默认配置由 `DefaultConfig()` 返回，见 [location.go](https://github.com/gotify/location/blob/03bc4ad20437/location.go#L27)：

```go
func DefaultConfig() Config {
    return Config{
        Host:   "localhost:8080",
        Scheme: "http",
    }
}
```

**缺省回退结果：**
- Scheme 回退为 `http`
- Host 回退为 `localhost:8080`
- Base（Path）默认为空字符串

### 4.2 实际应用中的回退场景

在 Gotify Server 中，由于直接使用 `location.Default()`，没有传入自定义配置，因此：
- **没有反向代理头时**：Scheme 从请求本身推断，最终回退到 `http`；Host 从 `Host` 头获取，最终回退到 `localhost:8080`
- **有反向代理头时**：优先使用反向代理透传的 `X-Forwarded-Proto` 和 `X-Forwarded-Host`

---

## 五、可信代理配置（TrustedProxies）到底影响什么？——容易混淆的点

### 5.1 关键结论

**`SetTrustedProxies` 只影响 Gin 框架的 `Context.ClientIP()` 方法（即客户端 IP 解析），完全不影响 `gotify/location` 库对 `X-Forwarded-Proto` 和 `X-Forwarded-Host` 的读取！**

### 5.2 代码证据

在 [router.go](file:///d:/fz/0601-1/solo-dogfeeding/code/38-server/router/router.go#L32-L34) 中同时配置了：

```go
g.RemoteIPHeaders = []string{"X-Forwarded-For"}
g.SetTrustedProxies(conf.Server.TrustedProxies)
g.ForwardedByClientIP = true
```

但 `gotify/location` 库的 `resolveScheme` 和 `resolveHost` 方法（见前文 3.2、3.3 节）是**直接**调用 `r.Header.Get(...)` 读取请求头的，**没有经过 Gin 的可信代理校验逻辑**。

### 5.3 影响范围对比

| 功能 | 是否受 TrustedProxies 保护 | 说明 |
|------|----------------------------|------|
| `ctx.ClientIP()` 客户端 IP | ✅ **是** | Gin 内部先校验 RemoteAddr 是否在可信代理列表中，再决定是否信任 `X-Forwarded-For` |
| `location.Get(ctx).Scheme` 协议 | ❌ **否** | `gotify/location` 直接读 header，无条件信任 `X-Forwarded-Proto` |
| `location.Get(ctx).Host` 主机名 | ❌ **否** | `gotify/location` 直接读 header，无条件信任 `X-Forwarded-Host` |
| HTTPS 重定向中的 host | ❌ **否** | 直接用 `ctx.Request.Host`，不经过 location |

### 5.4 安全隐患说明

这意味着：
- 即使配置了 `TrustedProxies`，攻击者仍然可以通过伪造 `X-Forwarded-Proto: https` 和 `X-Forwarded-Host: evil.com` 来影响分页链接、Swagger 文档和插件展示 URL 的生成。
- 但 `X-Forwarded-For`（IP 地址）是受保护的，不会被伪造。
- **建议**：除了配置 `TrustedProxies`，还应确保反向代理层在转发时**覆盖**而非追加 `X-Forwarded-Proto` 和 `X-Forwarded-Host`，防止客户端伪造。

---

## 六、各场景下 scheme / host / path 的来源拆解

### 6.1 消息分页 URL（buildWithPaging）

代码位置：[api/message.go](file:///d:/fz/0601-1/solo-dogfeeding/code/38-server/api/message.go#L100-L119)

```go
func buildWithPaging(ctx *gin.Context, paging *pagingParams, messages []*model.Message) *model.PagedMessages {
    // ...
    url := location.Get(ctx)          // ① 从 location 中间件获取基础 URL
    url.Path = ctx.Request.URL.Path   // ② 覆盖 Path 为当前请求路径
    query := url.Query()
    query.Add("limit", strconv.Itoa(paging.Limit))
    query.Add("since", strconv.FormatUint(uint64(since), 10))
    url.RawQuery = query.Encode()
    next = url.String()
    // ...
}
```

| 组成部分 | 来源 | 反向代理头是否起作用 |
|----------|------|----------------------|
| **Scheme** | `location.Get(ctx).Scheme` → `X-Forwarded-Proto` 或回退值 | ✅ 是 |
| **Host** | `location.Get(ctx).Host` → `X-Forwarded-Host` 或 `r.Host` 或回退值 | ✅ 是 |
| **Path** | `ctx.Request.URL.Path`（反向代理若做了路径剥离则是剥离后的路径） | ❌ 否，由 Gin 路由匹配后的路径决定 |
| **Query** | 重新拼装 `limit` + `since` | — |

### 6.2 插件展示 URL（GetDisplay）

代码位置：[api/plugin.go](file:///d:/fz/0601-1/solo-dogfeeding/code/38-server/api/plugin.go#L258) + [echo.go](file:///d:/fz/0601-1/solo-dogfeeding/code/38-server/plugin/example/echo/echo.go#L98-L109)

调用方：
```go
ctx.JSON(200, instance.GetDisplay(location.Get(ctx)))
```

插件端实现（echo 示例）：
```go
func (c *EchoPlugin) GetDisplay(location *url.URL) string {
    loc := &url.URL{
        Path: c.basePath,              // ① Path 来自插件注册时保存的 basePath
    }
    if location != nil {
        loc.Scheme = location.Scheme   // ② Scheme 从 location 参数取
        loc.Host = location.Host       // ③ Host 从 location 参数取
    }
    loc = loc.ResolveReference(&url.URL{
        Path: "echo",                  // ④ 追加具体 endpoint
    })
    return "Echo plugin running at: " + loc.String()
}
```

其中 `c.basePath` 的来源在 [plugin/manager.go](file:///d:/fz/0601-1/solo-dogfeeding/code/38-server/plugin/manager.go#L348-L351)：

```go
g := m.mux.Group(pluginConf.Token+"/", requirePluginEnabled(id, m.db))
instance.RegisterWebhook(strings.Replace(g.BasePath(), ":id", strconv.Itoa(int(id)), 1), g)
```

| 组成部分 | 来源 | 反向代理头是否起作用 |
|----------|------|----------------------|
| **Scheme** | `location.Get(ctx).Scheme` → `X-Forwarded-Proto` 或回退值 | ✅ 是 |
| **Host** | `location.Get(ctx).Host` → `X-Forwarded-Host` 或 `r.Host` 或回退值 | ✅ 是 |
| **Path** | 插件注册时的路由组 BasePath（如 `/plugin/1/custom/Pabcd/`）+ endpoint | ❌ 否，由 Gin RouterGroup 的内部路径决定 |

### 6.3 Swagger 文档 Base URL

代码位置：[docs/swagger.go](file:///d:/fz/0601-1/solo-dogfeeding/code/38-server/docs/swagger.go#L15-L21)

```go
func Serve(ctx *gin.Context) {
    base := location.Get(ctx).Host              // ① 取 location 的 Host 部分
    if basePathFromQuery := ctx.Query("base"); basePathFromQuery != "" {
        base = basePathFromQuery                 // ② 支持通过 query 参数覆盖（Swagger UI 自动传递）
    }
    ctx.Writer.WriteString(getSwaggerJSON(base)) // ③ 替换 spec.json 中的 "localhost"
}
```

| 组成部分 | 来源 | 反向代理头是否起作用 |
|----------|------|----------------------|
| **Host** | `location.Get(ctx).Host`，或 query 参数 `?base=...` 覆盖 | ✅ 是，可被 query 进一步覆盖 |

---

## 七、HTTPS 重定向是不是走同一套逻辑？——结论：不是！

### 7.1 HTTPS 重定向代码

代码位置：[router.go](file:///d:/fz/0601-1/solo-dogfeeding/code/38-server/router/router.go#L46-L67)

```go
if conf.Server.SSL.Enabled && conf.Server.SSL.RedirectToHTTPS {
    g.Use(func(ctx *gin.Context) {
        if ctx.Request.TLS != nil {              // ① 仅检查直连 TLS 状态
            ctx.Next()
            return
        }
        if ctx.Request.Method != http.MethodGet && ctx.Request.Method != http.MethodHead {
            ctx.Data(http.StatusBadRequest, "text/plain; charset=utf-8", []byte("Use HTTPS"))
            ctx.Abort()
            return
        }
        host := ctx.Request.Host                  // ② 直接取 Request.Host
        if idx := strings.LastIndex(host, ":"); idx != -1 {
            host = host[:idx]
        }
        if conf.Server.SSL.Port != 443 {
            host = fmt.Sprintf("%s:%d", host, conf.Server.SSL.Port)
        }
        ctx.Redirect(http.StatusFound, fmt.Sprintf("https://%s%s", host, ctx.Request.RequestURI))
        ctx.Abort()
    })
}
```

### 7.2 与 location 逻辑的对比

| 判断维度 | HTTPS 重定向逻辑 | `gotify/location` 逻辑 |
|----------|-----------------|------------------------|
| **协议判断依据** | `ctx.Request.TLS != nil`（仅直连 TLS） | 优先级：`X-Forwarded-Proto` → `URL.Scheme` → `TLS` → `Proto` → 默认值 |
| **是否识别 `X-Forwarded-Proto`** | ❌ **完全不识别** | ✅ 识别，且优先级最高 |
| **Host 来源** | `ctx.Request.Host`（HTTP Host 头） | 优先级：`X-Forwarded-Host` → `X-Host` → `r.Host` → 默认值 |
| **是否使用 location 中间件结果** | ❌ **完全不使用**，自己独立实现 | ✅ 就是 location 本身 |

### 7.3 潜在问题

当 Gotify 部署在**反向代理做 TLS 终结**的场景下时：

```
客户端 → (HTTPS) → 反向代理 → (HTTP) → Gotify
```

此时：
- 反向代理通常会设置 `X-Forwarded-Proto: https`
- 但 `ctx.Request.TLS` 是 `nil`（因为 Gotify 收到的是 HTTP）
- **结果**：HTTPS 重定向中间件会**错误地**把已经是 HTTPS 的请求再次重定向到 HTTPS，可能导致重定向循环，或者将反向代理设置的协议丢失。

**正确做法**：在反向代理做 TLS 终结的部署场景下，应该**关闭** `server.ssl.redirecttohttps`（设为 `false`），因为 TLS 升级由反向代理负责。Gotify 的这个重定向逻辑仅适用于 Gotify 自身直接暴露 HTTPS 端口的场景。

---

## 八、OIDC 回调的特殊处理

代码位置：[api/oidc.go](file:///d:/fz/0601-1/solo-dogfeeding/code/38-server/api/oidc.go#L43-L51) + [api/oidc.go](file:///d:/fz/0601-1/solo-dogfeeding/code/38-server/api/oidc.go#L226-L230)

OIDC Provider 初始化时，RedirectURL **直接从静态配置读取**：

```go
provider, err := rp.NewRelyingPartyOIDC(
    context.Background(),
    conf.OIDC.Issuer,
    conf.OIDC.ClientID,
    conf.OIDC.ClientSecret,
    conf.OIDC.RedirectURL,   // 静态配置，不依赖请求头
    scopes,
    opts...,
)
```

回调成功后使用**相对路径**重定向回 UI：

```go
// A reverse proxy may have already stripped a url prefix from the URL
// without us knowing, we have to make a relative redirect.
w.Header().Set("Location", "../../")
w.WriteHeader(http.StatusTemporaryRedirect)
```

**设计考量**：避免依赖反向代理头和绝对路径，用相对路径 `../../` 让浏览器自行解析。

---

## 九、总结

### 9.1 核心逻辑总结

| 关注点 | 结论 |
|--------|------|
| 反向代理头作用 | `X-Forwarded-Proto` 和 `X-Forwarded-Host` 仅通过 `gotify/location` 库影响分页链接、Swagger、插件展示三处的 URL 拼装 |
| TrustedProxies 实际影响 | **仅保护客户端 IP 解析**（`ClientIP()`），对 scheme/host 读取无保护 |
| Path 来源 | 与反向代理头无关，直接来自 `ctx.Request.URL.Path` 或 Gin RouterGroup 内部路径 |
| HTTPS 重定向 | **不走 location 逻辑**，仅判断 `ctx.Request.TLS` 和 `ctx.Request.Host`，反向代理 TLS 终结场景下应关闭 |

### 9.2 各场景 URL 组件来源速查表

| 场景 | Scheme 来源 | Host 来源 | Path 来源 |
|------|------------|-----------|-----------|
| 消息分页 next URL | `X-Forwarded-Proto` / location 默认值 | `X-Forwarded-Host` / `r.Host` / location 默认值 | `ctx.Request.URL.Path` |
| 插件 Display URL | `X-Forwarded-Proto` / location 默认值 | `X-Forwarded-Host` / `r.Host` / location 默认值 | RouterGroup BasePath + endpoint |
| Swagger spec host | `X-Forwarded-Host` / `r.Host` / location 默认值（可被 query `?base=` 覆盖） | 同左 | — |
| HTTPS 重定向 URL | 硬编码 `https://` | `ctx.Request.Host` | `ctx.Request.RequestURI` |
| OIDC RedirectURL | 静态配置 `oidc.redirecturl` | 同左 | 同左 |

### 9.3 配置建议

对于部署在反向代理后的生产环境：

1. **配置 `server.trustedproxies`**：明确指定反向代理的 IP 地址，保护客户端 IP 解析。
2. **确保反向代理正确设置并覆盖请求头**：
   - `proxy_set_header X-Forwarded-Proto $scheme;`（覆盖，防止伪造）
   - `proxy_set_header X-Forwarded-Host $host;`（覆盖，防止伪造）
   - `proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;`
3. **反向代理 TLS 终结场景关闭 `server.ssl.redirecttohttps: false`**：避免重定向逻辑与代理层冲突。
4. **OIDC 场景务必正确配置 `oidc.redirecturl`**：该值不依赖请求头动态生成，必须是外部可访问的完整回调 URL。
5. **注意 `gotify/location` 无信任校验**：如果担心 `X-Forwarded-Proto` / `X-Forwarded-Host` 被伪造，应在反向代理层确保这些头被强制覆盖，不保留客户端传入的值。
