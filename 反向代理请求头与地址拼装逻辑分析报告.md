# 反向代理请求头与地址拼装逻辑分析报告

## 一、概述

在 Gotify Server 项目中，当服务部署在反向代理（如 Nginx、Apache、Kubernetes Ingress 等）后面时，需要通过反向代理透传的请求头来获取客户端真实的访问地址（协议 + 主机名），从而正确拼装对外访问的 URL。本报告详细分析这一机制的实现原理、作用以及配置缺省时的回退逻辑。

## 二、核心依赖库：gotify/location

项目使用 `github.com/gotify/location` 库来实现反向代理环境下的地址检测。该库作为 Gin 中间件，自动从请求中提取 scheme（协议）和 host（主机名）信息。

### 2.1 库的初始化

在 [router.go](file:///d:/fz/0601-1/solo-dogfeeding/code/38-server/router/router.go#L43) 中，通过 `location.Default()` 注册中间件：

```go
g.Use(gin.LoggerWithFormatter(logFormatter), gin.Recovery(), gerror.Handler(), location.Default())
```

## 三、反向代理请求头的作用

### 3.1 涉及的核心请求头

| 请求头 | 作用 |
|--------|------|
| `X-Forwarded-Proto` | 透传客户端原始访问协议（http / https） |
| `X-Forwarded-Host` | 透传客户端原始访问的主机名 |
| `X-Forwarded-For` | 透传客户端真实 IP 地址（gin 框架层处理） |

### 3.2 Scheme（协议）解析逻辑

在 `gotify/location` 库的 [config.go](https://github.com/gotify/location/blob/03bc4ad20437/config.go#L46) 中，`resolveScheme` 方法按以下优先级解析协议：

```go
func (l *location) resolveScheme(r *http.Request) string {
    switch {
    case r.Header.Get(l.headers.scheme) == "https":  // 1. X-Forwarded-Proto
        return "https"
    case r.URL.Scheme == "https":                    // 2. URL Scheme
        return "https"
    case r.TLS != nil:                               // 3. TLS 连接状态
        return "https"
    case strings.HasPrefix(r.Proto, "HTTPS"):        // 4. HTTP 协议版本
        return "https"
    default:
        return l.scheme                              // 5. 回退到默认配置
    }
}
```

**优先级说明（从高到低）：**
1. **`X-Forwarded-Proto` 请求头**：反向代理透传的原始协议，优先级最高
2. **`r.URL.Scheme`**：请求 URL 中的协议部分
3. **`r.TLS`**：当前连接是否为 TLS 连接
4. **`r.Proto`**：HTTP 协议字符串（如 "HTTP/1.1"）
5. **默认配置值**：当以上都无法确定时，使用默认值

### 3.3 Host（主机名）解析逻辑

同样在 [config.go](https://github.com/gotify/location/blob/03bc4ad20437/config.go#L59) 中，`resolveHost` 方法按以下优先级解析主机：

```go
func (l *location) resolveHost(r *http.Request) (host string) {
    switch {
    case r.Header.Get(l.headers.host) != "":   // 1. X-Forwarded-Host
        return r.Header.Get(l.headers.host)
    case r.Header.Get("X-Host") != "":         // 2. X-Host
        return r.Header.Get("X-Host")
    case r.Host != "":                         // 3. Host 请求头
        return r.Host
    case r.URL.Host != "":                     // 4. URL Host
        return r.URL.Host
    default:
        return l.host                          // 5. 回退到默认配置
    }
}
```

**优先级说明（从高到低）：**
1. **`X-Forwarded-Host` 请求头**：反向代理透传的原始主机名，优先级最高
2. **`X-Host` 请求头**：备用的主机头
3. **`r.Host`**：HTTP 请求的 Host 头
4. **`r.URL.Host`**：请求 URL 中的主机部分
5. **默认配置值**：当以上都无法确定时，使用默认值

## 四、配置缺省时的回退逻辑

### 4.1 默认配置值

当使用 `location.Default()` 时，默认配置由 `DefaultConfig()` 返回，见 [location.go](https://github.com/gotify/location/blob/03bc4ad20437/location.go#L27)：

```go
func DefaultConfig() Config {
    return Config{
        Host:   "localhost:8080",
        Scheme: "http",
    }
}
```

**缺省回退结果：**
- Scheme 回退为 `http`
- Host 回退为 `localhost:8080`

### 4.2 实际应用中的回退场景

在 Gotify Server 中，由于直接使用 `location.Default()`，没有传入自定义配置，因此：
- **没有反向代理头时**：Scheme 从请求本身推断，最终回退到 `http`；Host 从 `Host` 头获取，最终回退到 `localhost:8080`
- **有反向代理头时**：优先使用反向代理透传的 `X-Forwarded-Proto` 和 `X-Forwarded-Host`

### 4.3 TrustedProxies 安全配置

为了防止伪造的 `X-Forwarded-*` 头，Gin 框架提供了 `TrustedProxies` 配置。在 [router.go](file:///d:/fz/0601-1/solo-dogfeeding/code/38-server/router/router.go#L32-L34) 中：

```go
g.RemoteIPHeaders = []string{"X-Forwarded-For"}
g.SetTrustedProxies(conf.Server.TrustedProxies)
g.ForwardedByClientIP = true
```

**安全说明：**
- `TrustedProxies` 配置在 [config.go](file:///d:/fz/0601-1/solo-dogfeeding/code/38-server/config/config.go#L44) 中定义，类型为 `[]string`，默认值为空
- 只有来自可信代理 IP 的请求，其 `X-Forwarded-*` 头才会被信任
- 当 `TrustedProxies` 为空时，Gin 的行为取决于版本，可能不信任任何代理头

## 五、地址拼装在项目中的应用场景

### 5.1 消息分页 URL 构造

在 [api/message.go](file:///d:/fz/0601-1/solo-dogfeeding/code/38-server/api/message.go#L107-L113) 中，使用 location 构造分页的 next URL：

```go
func buildWithPaging(ctx *gin.Context, paging *pagingParams, messages []*model.Message) *model.PagedMessages {
    // ...
    url := location.Get(ctx)
    url.Path = ctx.Request.URL.Path
    query := url.Query()
    query.Add("limit", strconv.Itoa(paging.Limit))
    query.Add("since", strconv.FormatUint(uint64(since), 10))
    url.RawQuery = query.Encode()
    next = url.String()
    // ...
}
```

**作用：** 返回给客户端的分页链接必须是客户端可访问的外部地址，因此需要使用反向代理透传的真实地址。

### 5.2 Swagger 文档 Base URL

在 [docs/swagger.go](file:///d:/fz/0601-1/solo-dogfeeding/code/38-server/docs/swagger.go#L15-L21) 中，使用 location 提供的 host 替换 swagger spec 中的 localhost：

```go
func Serve(ctx *gin.Context) {
    base := location.Get(ctx).Host
    if basePathFromQuery := ctx.Query("base"); basePathFromQuery != "" {
        base = basePathFromQuery
    }
    ctx.Writer.WriteString(getSwaggerJSON(base))
}
```

**作用：** Swagger UI 中的 API 调用地址需要与实际访问地址一致。

### 5.3 插件 Display 信息

在 [api/plugin.go](file:///d:/fz/0601-1/solo-dogfeeding/code/38-server/api/plugin.go#L258) 中，将 location 传递给插件的 Display 方法：

```go
ctx.JSON(200, instance.GetDisplay(location.Get(ctx)))
```

在示例插件 [echo.go](file:///d:/fz/0601-1/solo-dogfeeding/code/38-server/plugin/example/echo/echo.go#L98-L109) 中：

```go
func (c *EchoPlugin) GetDisplay(location *url.URL) string {
    loc := &url.URL{
        Path: c.basePath,
    }
    if location != nil {
        loc.Scheme = location.Scheme
        loc.Host = location.Host
    }
    loc = loc.ResolveReference(&url.URL{
        Path: "echo",
    })
    return "Echo plugin running at: " + loc.String()
}
```

**作用：** 插件需要向用户展示可访问的 Webhook URL，必须使用外部地址。

### 5.4 OIDC 回调地址

在 [api/oidc.go](file:///d:/fz/0601-1/solo-dogfeeding/code/38-server/api/oidc.go#L48) 中，OIDC 的 RedirectURL 直接从配置读取，而非动态从请求头获取：

```go
conf.OIDC.RedirectURL,
```

在 [CallbackHandler](file:///d:/fz/0601-1/solo-dogfeeding/code/38-server/api/oidc.go#L226-L230) 中，使用相对路径进行重定向：

```go
// A reverse proxy may have already stripped a url prefix from the URL
// without us knowing, we have to make a relative redirect.
// We cannot use http.Redirect as this normalizes the Path with r.URL.
w.Header().Set("Location", "../../")
w.WriteHeader(http.StatusTemporaryRedirect)
```

**设计考量：** 使用相对路径重定向，避免了依赖反向代理头可能导致的路径问题。

## 六、总结

### 6.1 核心逻辑总结

1. **反向代理头的作用**：`X-Forwarded-Proto` 和 `X-Forwarded-Host` 用于在反向代理环境下还原客户端的真实访问地址，确保服务生成的外部 URL（如分页链接、文档地址、插件 URL）是正确可访问的。

2. **优先级机制**：反向代理头优先级最高，其次是请求本身的信息，最后是默认配置值。

3. **缺省回退**：
   - 无反向代理头且无法从请求推断时，Scheme 回退为 `http`
   - Host 回退为 `localhost:8080`
   - 回退值由 `gotify/location` 库的默认配置决定

4. **安全机制**：通过 `TrustedProxies` 配置限制可信代理，防止请求头伪造攻击。

### 6.2 配置建议

对于部署在反向代理后的生产环境，建议：

1. **配置 `server.trustedproxies`**：明确指定反向代理的 IP 地址，确保只有可信代理的 `X-Forwarded-*` 头被接受
2. **确保反向代理正确设置请求头**：
   - `X-Forwarded-Proto`：设置为客户端实际访问的协议
   - `X-Forwarded-Host`：设置为客户端实际访问的主机名
   - `X-Forwarded-For`：设置为客户端真实 IP
3. **OIDC 场景**：务必正确配置 `oidc.redirecturl`，因为它不依赖请求头动态生成
