# 消息存储迁移分析 - 索引事实校准报告

## 一、校准背景与范围

本报告对之前的《消息存储迁移机制与查询接口兼容性分析》进行**逐条事实校准**，重点核对：

1. 所有"已建索引/依赖索引"的说法是否与模型定义一致
2. `messages.application_id` 的实际索引状态
3. `applications.user_id` 的实际索引状态
4. 联合索引 `uix_application_user_id_sort_key` 的实际状态
5. 补充可直接执行的数据库排查命令

---

## 二、模型定义中的索引声明（事实基准）

### 2.1 各模型字段索引声明对照表

以下直接摘自 `model/*.go` 中的 `gorm` tag：

#### Message 模型 (`model/message.go:8-16`)

```go
type Message struct {
    ID            uint   `gorm:"autoIncrement;primaryKey;index"`  // 主键 + 额外索引
    ApplicationID uint                                          // ⚠️ 无任何索引声明！
    Message       string `gorm:"type:text"`
    Title         string `gorm:"type:text"`
    Priority      int
    Extras        []byte
    Date          time.Time
}
```

#### Application 模型 (`model/application.go:10-62`)

```go
type Application struct {
    ID        uint   `gorm:"primaryKey;autoIncrement"`
    Token     string `gorm:"type:varchar(180);uniqueIndex:uix_applications_token"`
    UserID    uint   `gorm:"index;uniqueIndex:uix_application_user_id_sort_key,priority:1"`
    Name      string `gorm:"type:text"`
    SortKey   string `gorm:"type:bytes;uniqueIndex:uix_application_user_id_sort_key,priority:2,length:255"`
    // ... 其他字段
}
```

#### Client 模型 (`model/client.go:10-38`)

```go
type Client struct {
    ID     uint   `gorm:"primaryKey;autoIncrement"`
    Token  string `gorm:"type:varchar(180);uniqueIndex:uix_clients_token"`
    UserID uint   `gorm:"index"`
    // ... 其他字段
}
```

#### User 模型 (`model/user.go:6-14`)

```go
type User struct {
    ID    uint   `gorm:"primaryKey;autoIncrement"`
    Name  string `gorm:"type:varchar(180);uniqueIndex:uix_users_name"`
    // ... 其他字段
}
```

#### PluginConf 模型 (`model/pluginconf.go:4-13`)

```go
type PluginConf struct {
    ID            uint   `gorm:"primaryKey;autoIncrement"`
    UserID        uint                                    // ⚠️ 无索引
    Token         string `gorm:"type:varchar(180);uniqueIndex:uix_plugin_confs_token"`
    ApplicationID uint                                    // ⚠️ 无索引
    // ... 其他字段
}
```

### 2.2 索引状态汇总表

| 表 | 字段 | 模型中的索引声明 | GORM AutoMigrate 实际创建 |
|---|------|-----------------|--------------------------|
| `messages` | `id` | `primaryKey;index` | 主键索引 + 额外单列索引 |
| `messages` | `application_id` | **❌ 无** | **❌ 无索引** |
| `applications` | `id` | `primaryKey` | 主键索引 |
| `applications` | `token` | `uniqueIndex:uix_applications_token` | 单列唯一索引 |
| `applications` | `user_id` | `index; uniqueIndex:uix_application_user_id_sort_key,priority:1` | 单列普通索引 **+** 联合唯一索引第1列 |
| `applications` | `sort_key` | `uniqueIndex:uix_application_user_id_sort_key,priority:2` | 联合唯一索引第2列 |
| `clients` | `id` | `primaryKey` | 主键索引 |
| `clients` | `token` | `uniqueIndex:uix_clients_token` | 单列唯一索引 |
| `clients` | `user_id` | `index` | 单列普通索引 |
| `users` | `id` | `primaryKey` | 主键索引 |
| `users` | `name` | `uniqueIndex:uix_users_name` | 单列唯一索引 |
| `plugin_confs` | `id` | `primaryKey` | 主键索引 |
| `plugin_confs` | `token` | `uniqueIndex:uix_plugin_confs_token` | 单列唯一索引 |
| `plugin_confs` | `user_id` | **❌ 无** | **❌ 无索引** |
| `plugin_confs` | `application_id` | **❌ 无** | **❌ 无索引** |

---

## 三、原报告错误结论逐条校准

### 3.1 关于 messages.application_id 索引

#### 原报告中的错误说法

| 原报告位置 | 原结论 | 校准结果 |
|-----------|--------|---------|
| 2.85 行 | "新增 `messages` 表索引" | ✅ 正确（但不具体） |
| 2.86 行 | "新增 `application_id` 索引" | ❌ **错误**：根本没有此索引 |
| 2.88 行 | "`application_id` 列类型变更" | ⚠️ 部分正确（但不存在索引） |
| 2.89 行 | "外键约束缺失导致孤立消息" | ✅ 正确（但原因是无外键，不是索引） |
| 3.439 行 | "`application_id` 索引变更" | ❌ **错误**：不存在此索引 |

#### 事实真相

`model/message.go:10` 中：
```go
ApplicationID uint  // ⚠️ 没有任何 gorm tag，更没有 index
```

**后果**：
- `GetMessagesByApplicationSince(appID, ...)` 中 `WHERE application_id = ?` **无法使用索引**
- 数据库只能走**全表扫描**或**主键索引扫描**
- 消息量大时查询性能会显著下降

#### 修正后的结论

原报告"新增 `application_id` 索引"的说法是**错误的**。实际上：

| 场景 | 实际数据库行为 |
|-----|--------------|
| `WHERE application_id = ?` | 全表扫描（无索引可用） |
| 升级时是否创建索引 | ❌ 不会创建 |
| 依赖此索引的查询性能 | 可能随数据量增长而下降 |

---

### 3.2 关于 applications.user_id 索引

#### 原报告中的说法

| 原报告位置 | 原结论 | 校准结果 |
|-----------|--------|---------|
| 3.319 行 | "关键依赖：`applications.user_id` 索引" | ✅ **正确**（但不完整） |
| 3.326 行 | "新增 `applications.user_id` 索引" | ✅ **正确** |

#### 事实真相

`model/application.go:23` 中：
```go
UserID uint `gorm:"index;uniqueIndex:uix_application_user_id_sort_key,priority:1"`
```

**这个字段实际上有两个索引**：
1. **单列普通索引**：`index` → GORM 会创建一个单例索引
2. **联合唯一索引第1列**：`uniqueIndex:uix_application_user_id_sort_key,priority:1`

#### GORM 索引创建规则

在 GORM 中，同一字段上的多个索引声明会**分别创建**：

```go
// 这行会创建两个索引：
UserID uint `gorm:"index;uniqueIndex:uix_xxx,priority:1"`
// 1. 单列索引：idx_applications_user_id（或类似命名）
// 2. 联合索引的一部分：uix_application_user_id_sort_key(user_id, sort_key)
```

#### 修正后的完整结论

`applications.user_id` 有**两层索引覆盖**：

| 索引类型 | 索引名 | 列 | 适用查询 |
|---------|--------|---|---------|
| 单列普通索引 | `idx_applications_user_id` | `user_id` | `WHERE user_id = ?` |
| 联合唯一索引 | `uix_application_user_id_sort_key` | `(user_id, sort_key)` | `WHERE user_id = ?` + 按 sort_key 排序 |

**查询优化说明**：
- `GetApplicationsByUser(userID)` 使用单列索引即可
- 按用户获取应用并排序时，联合索引可覆盖排序

---

### 3.3 关于联合索引 uix_application_user_id_sort_key

#### 原报告中的说法

| 原报告位置 | 原结论 | 校准结果 |
|-----------|--------|---------|
| 2.192 行 | "新增组合唯一索引 `(user_id, sort_key)`" | ✅ **正确** |
| 2.255 行 | "检查索引完整性时应包含此索引" | ✅ **正确** |

#### 事实真相

`model/application.go:23,61`：
```go
UserID  uint   `gorm:"index;uniqueIndex:uix_application_user_id_sort_key,priority:1"`
SortKey string `gorm:"type:bytes;uniqueIndex:uix_application_user_id_sort_key,priority:2,length:255"`
```

**联合索引定义**：
- 索引名：`uix_application_user_id_sort_key`
- 列顺序：`(user_id, sort_key)`
- 类型：`UNIQUE`
- `sort_key` 列的长度限制：`length:255`

#### 最左前缀原则

此联合索引可用于以下查询模式：

```sql
-- ✅ 可使用索引（最左列）
WHERE user_id = ?

-- ✅ 可使用索引（最左两列）
WHERE user_id = ? AND sort_key = ?

-- ✅ 可使用索引并覆盖排序
WHERE user_id = ? ORDER BY sort_key
```

---

### 3.4 原报告中其他索引相关说法的校准

#### 原报告 2.255 行：索引完整性检查

**原说法**：
> "含 uix_applications_token, uix_application_user_id_sort_key"

**校准**：✅ 正确，但**不完整**。实际 `applications` 表的索引包括：

| 索引 | 来源 |
|-----|------|
| 主键索引（id） | `primaryKey` |
| `uix_applications_token` | `uniqueIndex:uix_applications_token` |
| `idx_applications_user_id`（单列） | `index` |
| `uix_application_user_id_sort_key`（联合） | `uniqueIndex:uix_application_user_id_sort_key` |

#### 原报告 3.475 行：`applications.user_id` 索引缺失

**原说法**：
> "`applications.user_id` 索引缺失 → 查询变慢 → 删除也变慢"

**校准**：✅ 说法正确，但需要澄清：

- `applications.user_id` **有索引**（单列普通索引 + 联合索引）
- 如果升级失败导致索引未创建，确实会变慢
- 但由于有 `index` 声明，AutoMigrate 会自动创建

---

## 四、可直接执行的数据库排查命令

### 4.1 SQLite 数据库排查

#### 4.1.1 检查所有表和索引

```bash
# 连接数据库
sqlite3 gotify.db

# 查看所有表
.tables

# 查看某张表的结构（包含索引信息）
.schema applications
.schema messages
.schema users
.schema clients
.schema plugin_confs
```

#### 4.1.2 检查 messages 表的索引状态（最关键）

```sql
-- 查看 messages 表的完整结构和索引
.schema messages

-- 查看 messages 表上的所有索引（应只看到主键相关，没有 application_id）
PRAGMA index_list(messages);

-- 查看每个索引的具体列
PRAGMA index_info(idx_messages_id);  -- 如果存在的话
```

**预期输出分析**：

| 检查项 | 预期结果 | 说明 |
|-------|---------|------|
| `PRAGMA index_list(messages)` 行数 | 1-2 行 | 主键索引 + 可能的额外 id 索引 |
| 是否有 `application_id` 相关索引 | ❌ 不应有 | 模型未声明 |

#### 4.1.3 检查 applications 表的索引

```sql
-- 查看 applications 表结构
.schema applications

-- 查看所有索引
PRAGMA index_list(applications);

-- 查看每个索引的详细信息
PRAGMA index_info(uix_applications_token);
PRAGMA index_info(idx_applications_user_id);  -- 单列 user_id 索引
PRAGMA index_info(uix_application_user_id_sort_key);  -- 联合索引
```

**预期输出分析**（联合索引）：

```
PRAGMA index_info(uix_application_user_id_sort_key);
-- 应输出类似：
-- seqno  cid  name
-- 0      X    user_id   (priority:1)
-- 1      Y    sort_key  (priority:2)
```

#### 4.1.4 验证查询是否使用索引

```sql
-- 查看按应用查询消息的执行计划
EXPLAIN QUERY PLAN
SELECT * FROM messages WHERE application_id = 1 ORDER BY id DESC LIMIT 100;

-- 查看按用户查询应用的执行计划
EXPLAIN QUERY PLAN
SELECT * FROM applications WHERE user_id = 1 ORDER BY sort_key;
```

**预期分析**：

| 查询 | `EXPLAIN QUERY PLAN` 输出 | 说明 |
|-----|--------------------------|------|
| `messages WHERE application_id = ?` | `SCAN TABLE messages` | ⚠️ 全表扫描！无索引 |
| `applications WHERE user_id = ?` | `SEARCH TABLE applications USING INDEX ...` | ✅ 使用索引 |

### 4.2 MySQL 数据库排查

#### 4.2.1 检查表和索引

```sql
-- 查看所有表
SHOW TABLES;

-- 查看 messages 表结构和索引
DESCRIBE messages;
SHOW INDEX FROM messages;

-- 查看 applications 表结构和索引
DESCRIBE applications;
SHOW INDEX FROM applications;
```

#### 4.2.2 检查 messages.application_id 索引

```sql
-- 查看 messages 上的索引
SHOW INDEX FROM messages WHERE Key_name LIKE '%application%';

-- 如果没有结果，说明确实没有 application_id 索引
```

#### 4.2.3 检查 applications 表的索引

```sql
-- 查看所有索引
SHOW INDEX FROM applications;

-- 查看联合索引的列顺序
SHOW INDEX FROM applications WHERE Key_name = 'uix_application_user_id_sort_key';
```

**预期输出**：
```
+---------------+------------+-----------------------------------+--------------+-------------+
| Table         | Non_unique | Key_name                          | Seq_in_index | Column_name |
+---------------+------------+-----------------------------------+--------------+-------------+
| applications  |          0 | uix_application_user_id_sort_key  |            1 | user_id     |
| applications  |          0 | uix_application_user_id_sort_key  |            2 | sort_key    |
+---------------+------------+-----------------------------------+--------------+-------------+
```

#### 4.2.4 验证查询执行计划

```sql
-- 查看消息查询的执行计划
EXPLAIN SELECT * FROM messages WHERE application_id = 1 ORDER BY id DESC LIMIT 100;

-- 查看应用查询的执行计划
EXPLAIN SELECT * FROM applications WHERE user_id = 1 ORDER BY sort_key;
```

**关键列关注**：
- `type`：`ALL` = 全表扫描，`ref`/`range` = 使用索引
- `key`：实际使用的索引名
- `rows`：预计扫描的行数
- `Extra`：`Using index` = 覆盖索引，`Using filesort` = 额外排序

### 4.3 排查清单一键执行

#### 4.3.1 SQLite 一键排查脚本

```bash
#!/bin/bash

DB_PATH=${1:-gotify.db}

echo "=== 消息存储索引排查报告 ==="
echo "数据库: $DB_PATH"
echo ""

echo "=== 1. 所有表 ==="
sqlite3 $DB_PATH ".tables"
echo ""

echo "=== 2. messages 表索引（关键检查） ==="
sqlite3 $DB_PATH "PRAGMA index_list(messages);"
echo ""
echo "⚠️  注意：messages.application_id 应该没有索引"
echo ""

echo "=== 3. applications 表索引 ==="
sqlite3 $DB_PATH "PRAGMA index_list(applications);"
echo ""

echo "=== 4. 联合索引 uix_application_user_id_sort_key 详情 ==="
sqlite3 $DB_PATH "PRAGMA index_info(uix_application_user_id_sort_key);"
echo ""

echo "=== 5. messages 表查询执行计划（验证是否全表扫描） ==="
sqlite3 $DB_PATH "EXPLAIN QUERY PLAN SELECT * FROM messages WHERE application_id = 1 ORDER BY id DESC LIMIT 100;"
echo ""

echo "=== 6. applications 表查询执行计划（验证是否使用索引） ==="
sqlite3 $DB_PATH "EXPLAIN QUERY PLAN SELECT * FROM applications WHERE user_id = 1 ORDER BY sort_key;"
echo ""

echo "=== 7. 数据量统计 ==="
sqlite3 $DB_PATH "SELECT 'messages' as table_name, COUNT(*) as count FROM messages UNION ALL SELECT 'applications', COUNT(*) FROM applications UNION ALL SELECT 'users', COUNT(*) FROM users;"
echo ""

echo "=== 排查完成 ==="
```

**使用方法**：
```bash
chmod +x check_indexes.sh
./check_indexes.sh /path/to/gotify.db
```

#### 4.3.2 MySQL 一键排查 SQL

```sql
-- 消息存储索引排查报告
-- 执行方式：mysql -u user -p database < check_indexes.sql

SELECT '=== 1. 所有表 ===' as info;
SHOW TABLES;

SELECT '=== 2. messages 表索引（关键检查） ===' as info;
SHOW INDEX FROM messages;

SELECT '=== 3. applications 表索引 ===' as info;
SHOW INDEX FROM applications;

SELECT '=== 4. 联合索引详情 ===' as info;
SHOW INDEX FROM applications WHERE Key_name = 'uix_application_user_id_sort_key';

SELECT '=== 5. messages 表查询执行计划 ===' as info;
EXPLAIN SELECT * FROM messages WHERE application_id = 1 ORDER BY id DESC LIMIT 100;

SELECT '=== 6. applications 表查询执行计划 ===' as info;
EXPLAIN SELECT * FROM applications WHERE user_id = 1 ORDER BY sort_key;

SELECT '=== 7. 数据量统计 ===' as info;
SELECT 'messages' as table_name, COUNT(*) as count FROM messages
UNION ALL
SELECT 'applications', COUNT(*) FROM applications
UNION ALL
SELECT 'users', COUNT(*) FROM users;
```

---

## 五、校准后的完整结论

### 5.1 索引状态总结

| 关键路径 | 模型声明 | 实际索引 | 查询性能影响 |
|---------|---------|---------|-------------|
| `messages.application_id` | ❌ 无 | ❌ 无 | ⚠️ 按应用查询消息可能全表扫描 |
| `applications.user_id` | ✅ `index` + 联合索引 | ✅ 单列索引 + 联合索引 | ✅ 按用户查询应用使用索引 |
| `applications.(user_id, sort_key)` | ✅ 联合唯一索引 | ✅ 联合唯一索引 | ✅ 覆盖排序 |

### 5.2 需要修正的原报告结论

| 原结论 | 修正后结论 |
|-------|-----------|
| "新增 `application_id` 索引" | ❌ **错误**：不存在此索引，消息按应用查询可能走全表扫描 |
| "依赖 `messages.application_id` 索引" | ❌ **错误**：查询不依赖此索引（因为不存在） |
| "`application_id` 索引变更" | ❌ **错误**：不存在此索引，变更无从谈起 |
| "新增 `applications.user_id` 索引" | ✅ **正确**，且实际上是**两个索引**（单列 + 联合） |
| "检查索引完整性应包含 uix_..." | ✅ **正确**，但还要检查单列 `user_id` 索引 |

### 5.3 隐藏的性能风险

由于 `messages.application_id` **没有索引**，以下查询可能随数据量增长而变慢：

| 查询函数 | SQL 条件 | 索引使用情况 |
|---------|---------|-------------|
| `GetMessagesByApplicationSince` | `WHERE application_id = ?` | ⚠️ 可能全表扫描 |
| `DeleteMessagesByApplication` | `WHERE application_id = ?` | ⚠️ 可能全表扫描 |
| `GetMessagesByUserSince` | `JOIN applications + WHERE user_id = ?` | ✅ 应用表有索引，但消息表过滤走全表 |

**建议**：如果消息量较大（>10万条），考虑手动添加索引：

```sql
-- SQLite
CREATE INDEX idx_messages_application_id ON messages(application_id);

-- MySQL
CREATE INDEX idx_messages_application_id ON messages(application_id);
```

---

## 六、附录：GORM 索引声明语法速查

### 6.1 索引声明语法

```go
// 单列普通索引
Field string `gorm:"index"`
Field string `gorm:"index:idx_name"`

// 单列唯一索引
Field string `gorm:"uniqueIndex"`
Field string `gorm:"uniqueIndex:idx_name"`

// 联合索引（priority 决定列顺序，数字小在前）
Field1 string `gorm:"index:idx_name,priority:1"`
Field2 string `gorm:"index:idx_name,priority:2"`

// 联合唯一索引
Field1 string `gorm:"uniqueIndex:uix_name,priority:1"`
Field2 string `gorm:"uniqueIndex:uix_name,priority:2"`

// 索引长度（MySQL/SQLite）
Field string `gorm:"uniqueIndex:idx_name,length:255"`

// 同一字段多个索引（分别创建）
Field string `gorm:"index;uniqueIndex:uix_name"`
```

### 6.2 本项目中的索引命名规则

| 索引类型 | 命名模式 | 示例 |
|---------|---------|------|
| 自动普通索引 | `idx_<table>_<field>` | `idx_applications_user_id` |
| 显式命名索引 | 自定义 | 本项目未使用 |
| 唯一索引 | `uix_<table>_<fields>` | `uix_applications_token` |
| 联合唯一索引 | `uix_<table>_<fields>` | `uix_application_user_id_sort_key` |

---

## 七、校准总结

### 7.1 主要修正

1. **重大修正**：`messages.application_id` **没有索引**，原报告中关于此索引的所有说法均错误
2. **补充修正**：`applications.user_id` 有**两个索引**（单列 + 联合），原报告只提到了联合索引
3. **风险澄清**：消息按应用查询可能走全表扫描，这是一个潜在性能问题

### 7.2 原报告中仍然正确的结论

- `fillMissingSortKeys` 的迁移逻辑和兼容边界 ✅
- 无独立 schema_version 时的迁移判断机制 ✅
- `since` 游标分页机制 ✅
- 查询与清理接口的权限校验路径 ✅
- 外键缺失导致的孤立消息问题 ✅

### 7.3 建议的后续行动

1. 使用本报告中的排查脚本验证实际数据库索引状态
2. 如果消息量大，评估是否需要手动添加 `messages.application_id` 索引
3. 下次模型变更时，关注索引声明的完整性
