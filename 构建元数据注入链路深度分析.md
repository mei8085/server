# 构建元数据注入链路深度分析报告

## 1. 概述

本报告分析 Gotify Server 项目中 **构建版本（Version）、提交散列（Commit）、构建日期（BuildDate）** 三类元数据从注入到展示的完整链路。

项目存在 **三种注入入口** 和 **四个消费出口**，各出口消费的字段数量不同，commit 和 buildDate 在不同层级被截断。

---

## 2. 三种注入入口对比

### 2.1 入口 A：Tag 发布构建（生产发布）

- **触发条件**：GitHub Actions 检测到 `refs/tags/v*` 格式的 tag 推送
- **入口位置**：`.github/workflows/build.yml` 第 29-34 行
- **数据来源**：
  - `Version`：从 git tag 名中剥离 `v` 前缀（如 `v2.5.0` → `2.5.0`）
  - `Commit`：`git rev-parse --verify HEAD`（40 位完整 SHA-1）
  - `BuildDate`：`date "+%F-%T"`（格式 `YYYY-MM-DD-HH:MM:SS`）
  - `Mode`：固定字符串 `prod`
- **注入形式**：
  ```bash
  LD_FLAGS="-w -s \
    -X main.Version=$VERSION \
    -X main.BuildDate=$(date "+%F-%T") \
    -X main.Commit=$(git rev-parse --verify HEAD) \
    -X main.Mode=prod"
  ```
- **总跳数**：从 CI 环境变量到最终 UI 展示共 **7 跳**

### 2.2 入口 B：Master 分支构建（开发镜像）

- **触发条件**：GitHub Actions 检测到 master 分支推送
- **入口位置**：`Makefile` 第 120 行（`build-docker-multiarch-master` 目标）
- **数据来源**：
  - `Version`：`master-<短commit>`（如 `master-ae9512b`）
  - `Commit`：`git rev-parse --verify HEAD`（40 位完整 SHA-1）
  - `BuildDate`：`date "+%F-%T"`
  - `Mode`：固定字符串 `prod`
- **注入形式**：直接在 Makefile 中内联 LD_FLAGS，不经过环境变量传递
  ```makefile
  --build-arg LD_FLAGS="-w -s \
    -X main.Version=master-$(shell git rev-parse --short HEAD) \
    -X main.BuildDate=$(shell date "+%F-%T") \
    -X main.Commit=$(shell git rev-parse --verify HEAD) \
    -X main.Mode=prod"
  ```
- **总跳数**：从 Makefile 变量到最终 UI 展示共 **6 跳**（少了 CI 环境变量这一跳）

### 2.3 入口 C：本地默认值（开发调试）

- **触发条件**：本地直接 `go run app.go` 或不带 ldflags 的 `go build`
- **入口位置**：`app.go` 第 16-25 行（包级变量默认值）
- **数据值**：
  - `Version`：`"unknown"`
  - `Commit`：`"unknown"`
  - `BuildDate`：`"unknown"`
  - `Mode`：`mode.Dev`（即 `"dev"`）
- **总跳数**：从 Go 源码变量到 UI 展示共 **5 跳**（无 CI、无 ldflags 注入）

---

## 3. 共享链路（前 4 跳）

无论哪种入口，最终都会到达 Go 包级变量，之后的链路是共享的。

### 第 N 跳：Go 包级变量

- **载体**：Go 二进制文件 `.data` 段中的字符串符号
- **位置**：`app.go` 第 16-25 行
- **代码**：
  ```go
  var (
      Version   = "unknown"
      Commit    = "unknown"
      BuildDate = "unknown"
      Mode      = mode.Dev
  )
  ```
- **说明**：
  - Tag/Master 入口：变量值被链接器 `-X` 参数在编译期覆盖
  - 本地入口：使用源码中的默认字符串字面量

### 第 N+1 跳：封装为 `model.VersionInfo` 结构体

- **载体**：堆上分配的 `*model.VersionInfo` 指针对象
- **位置**：`app.go` 第 28 行
- **代码**：
  ```go
  vInfo := &model.VersionInfo{Version: Version, Commit: Commit, BuildDate: BuildDate}
  ```
- **结构体定义**：`model/version.go` 第 6-22 行
  ```go
  type VersionInfo struct {
      Version   string `json:"version"`
      Commit    string `json:"commit"`
      BuildDate string `json:"buildDate"`
  }
  ```
- **作用**：将三个独立变量封装为带 JSON tag 的统一结构体，便于下游消费

### 第 N+2 跳：传入 `router.Create()`

- **载体**：函数参数（指针传递）
- **位置**：`app.go` 第 49 行
- **代码**：
  ```go
  engine, closeable := router.Create(db, vInfo, conf)
  ```
- **接收端**：`router/router.go` 第 28 行 `Create` 函数签名

---

## 4. 四个出口详细分析

从 `router.Create` 开始，vInfo 分发给四个出口，各出口消费的字段不同。

### 4.1 出口 1：启动日志（Startup Log）

- **出口位置**：`app.go` 第 31 行
- **代码**：
  ```go
  fmt.Println("Starting Gotify version", vInfo.Version+"@"+BuildDate)
  ```
- **消费字段**：`Version` + `BuildDate`（**注意**：BuildDate 直接从包级变量取，而非 vInfo）
- **Commit 是否在此停止**：✅ **是**，Commit 字段完全不出现
- **输出示例**：
  ```
  Starting Gotify version 2.5.0@2024-01-15-10:30:00
  ```
- **跳数**：
  - Tag 入口：第 5 跳到达启动日志
  - Master 入口：第 4 跳到达
  - 本地入口：第 2 跳到达

### 4.2 出口 2：`GET /version` API

- **出口位置**：`router/router.go` 第 172-174 行
- **代码**：
  ```go
  g.GET("version", func(ctx *gin.Context) {
      ctx.JSON(200, vInfo)
  })
  ```
- **消费字段**：`Version` + `Commit` + `BuildDate`（全部三个字段）
- **Commit 是否在此停止**：❌ 否，三个字段完整输出
- **认证要求**：无，公开接口
- **响应示例**：
  ```json
  {
      "version": "2.5.0",
      "commit": "ae9512b6b6feea56a110d59a3353ea3b9c293864",
      "buildDate": "2024-01-15-10:30:00"
  }
  ```
- **典型消费者**：排障工具、自动化脚本、第三方客户端

### 4.3 出口 3：`GET /gotifyinfo` API

- **出口位置**：`router/router.go` 第 187-189 行
- **代码**：
  ```go
  g.GET("gotifyinfo", func(ctx *gin.Context) {
      ctx.JSON(200, &model.GotifyInfo{Version: vInfo.Version, Oidc: conf.OIDC.Enabled, Register: conf.Registration})
  })
  ```
- **结构体定义**：`model/gotifyinfo.go`
  ```go
  type GotifyInfo struct {
      Version  string `json:"version"`
      Register bool   `json:"register"`
      Oidc     bool   `json:"oidc"`
  }
  ```
- **消费字段**：仅 `Version`（外加 register/oidc 两个功能开关）
- **Commit 和 BuildDate 在哪层停止**：
  - ✅ **`model.GotifyInfo` 结构体层** — Commit 和 BuildDate 在构造 GotifyInfo 时被丢弃
  - 只取了 `vInfo.Version` 一个字段，Commit 和 BuildDate 完全不进入该接口
- **认证要求**：无，公开接口
- **响应示例**：
  ```json
  {
      "version": "2.5.0",
      "register": true,
      "oidc": false
  }
  ```
- **设计目的**：登录前页面判断功能开关（是否显示注册按钮、是否显示 OIDC 登录），无需暴露详细构建信息

### 4.4 出口 4：页头展示（UI Header）

这是最复杂的出口，经过多步传递。

#### 第 1 步：vInfo 传入 ui.Register

- **位置**：`router/router.go` 第 117 行
- **代码**：
  ```go
  ui.Register(g, *vInfo, conf.Registration, conf.OIDC.Enabled)
  ```
- **消费字段**：`Version` + `Commit` + `BuildDate`（三个字段以值拷贝方式传入）

#### 第 2 步：封装为 uiConfig 并 JSON 序列化

- **位置**：`ui/serve.go` 第 18-29 行
- **代码**：
  ```go
  type uiConfig struct {
      Register bool              `json:"register"`
      Version  model.VersionInfo `json:"version"`
      OIDC     bool              `json:"oidc"`
  }
  
  uiConfigBytes, err := json.Marshal(uiConfig{Version: version, Register: register, OIDC: oidcEnabled})
  ```
- **消费字段**：全部三个字段（嵌套在 version 键下）

#### 第 3 步：替换 index.html 中的 `%CONFIG%` 占位符

- **位置**：`ui/serve.go` 第 31-37 行
- **HTML 模板**：`ui/index.html` 第 38 行
  ```html
  <script>window.config = %CONFIG%;</script>
  ```
- **替换逻辑**：
  ```go
  replaceConfig := func(content string) string {
      return strings.Replace(content, "%CONFIG%", string(uiConfigBytes), 1)
  }
  ```
- **最终 HTML 效果**：
  ```html
  <script>window.config = {"register":true,"version":{"version":"2.5.0","commit":"ae95...","buildDate":"2024-01-15-10:30:00"},"oidc":false};</script>
  ```
- **消费字段**：全部三个字段均在 `window.config.version` 对象中

#### 第 4 步：前端 config.ts 读取 window.config

- **位置**：`ui/src/config.ts` 第 16-22 行
- **代码**：
  ```typescript
  const config: IConfig = {
      url: 'unset',
      register: false,
      version: {commit: 'unknown', buildDate: 'unknown', version: 'unknown'},
      oidc: false,
      ...window.config,
  };
  ```
- **类型定义**：`ui/src/types.ts` 第 77-81 行
  ```typescript
  export interface IVersion {
      version: string;
      commit: string;
      buildDate: string;
  }
  ```
- **消费字段**：全部三个字段都在 config 对象中可用

#### 第 5 步：Layout.tsx 解构出 version 字符串

- **位置**：`ui/src/layout/Layout.tsx` 第 77 行
- **代码**：
  ```tsx
  const {version} = config.get('version');
  ```
- **⚠️ 关键截断点**：
  - 这里只解构了 `version` 字符串字段
  - `commit` 和 `buildDate` 虽然在 `config.get('version')` 返回的对象里，但**被直接丢弃**
  - 传入 Header 组件的只有 `version: string` 一个 props

#### 第 6 步：Header.tsx 渲染为超链接

- **位置**：`ui/src/layout/Header.tsx` 第 88、114-124 行
- **代码**：
  ```tsx
  const Header = ({ version, ... }) => {
      // ...
      <a href={
          version.startsWith('master-')
              ? `https://github.com/gotify/server/commit/${version.replace('master-', '')}`
              : `https://github.com/gotify/server/releases/tag/v${version}`
      }>
          <Typography variant="button" color="inherit">
              @{version}
          </Typography>
      </a>
  }
  ```
- **消费字段**：仅 `version` 字符串
- **用户可见内容**：页面顶部 Gotify Logo 旁显示 `@<version>` 形式的超链接
  - Release 版本：如 `@2.5.0`，链接到 GitHub Release 页面
  - Master 版本：如 `@master-ae9512b`，链接到对应 commit 页面（**利用 version 字符串中嵌入的短 commit**）

#### 页头出口总结

| 层级 | 字段可达性 |
|---|---|
| `window.config.version` | ✅ version + commit + buildDate 三字段完整 |
| `config.get('version')` 返回值 | ✅ 三字段完整 |
| `Layout.tsx` 解构后 | ⚠️ 仅 version 字符串，commit/buildDate 被丢弃 |
| `Header.tsx` props | ❌ 仅 version 字符串 |
| 最终用户可见 DOM | ❌ 仅 version 字符串 |

> **特别说明**：master 构建场景下，commit 信息是通过 Version 字符串本身（`master-<short commit>`）间接传递的，并非通过独立的 commit 字段传递。

---

## 5. 字段截断点总览

### Commit 字段的停止位置

| 出口 | Commit 是否出现 | 停止层级 |
|---|---|---|
| 启动日志 | ❌ 否 | 包级变量 → 日志输出 之间（直接未使用） |
| `GET /version` | ✅ 是 | 不停止，完整输出 |
| `GET /gotifyinfo` | ❌ 否 | `model.GotifyInfo` 结构体构造层（只取 Version） |
| 页头展示 | ❌ 否（间接） | `Layout.tsx` 解构层（只取 version 字符串字段） |

### BuildDate 字段的停止位置

| 出口 | BuildDate 是否出现 | 停止层级 |
|---|---|---|
| 启动日志 | ✅ 是（在 @ 之后） | 不停止，直接拼接输出 |
| `GET /version` | ✅ 是 | 不停止，完整输出 |
| `GET /gotifyinfo` | ❌ 否 | `model.GotifyInfo` 结构体构造层（只取 Version） |
| 页头展示 | ❌ 否 | `Layout.tsx` 解构层（只取 version 字符串字段） |

---

## 6. 浏览器用户实际可见的字段

**结论：浏览器用户只能看到 Version 一个字段。**

虽然 `window.config` 全局对象中包含了完整的 `version`、`commit`、`buildDate` 三个字段（可以通过浏览器 DevTools 控制台查看），但：

1. **React UI 代码只消费了 `version` 字符串**
2. 页头只渲染了 `@<version>` 超链接
3. Commit 和 BuildDate 从未被任何 React 组件读取或展示
4. 前端没有调用 `/version` API 来获取详细构建信息

> 开发者可以在浏览器控制台通过 `window.config.version.commit` 查看 commit，但这不是面向普通用户的展示。

---

## 7. 全链路跳数汇总表

### 7.1 Tag 发布入口（7 跳）

| 跳数 | 载体 | 位置 |
|---|---|---|
| 1 | GitHub Actions `$GITHUB_ENV` 环境变量 | `.github/workflows/build.yml` |
| 2 | Makefile `LD_FLAGS` 变量 → `go build -ldflags` | `Makefile` |
| 3 | Go 二进制 `.data` 段包级变量 | `app.go` |
| 4 | `*model.VersionInfo` 堆对象 | `app.go` |
| 5A | 启动日志 stdout | `app.go`（出口1） |
| 5B | Gin 路由闭包环境 → `/version` API JSON 响应 | `router/router.go`（出口2） |
| 5C | Gin 路由闭包环境 → `/gotifyinfo` API JSON 响应 | `router/router.go`（出口3） |
| 5D | `ui.Register` 函数参数 → uiConfig JSON → HTML 字符串 | `ui/serve.go` |
| 6D | 浏览器 `window.config` 全局对象 | `ui/index.html` |
| 7D | React Props → DOM 渲染（仅 version） | `ui/src/layout/Header.tsx`（出口4） |

> 注：跳 5 之后分化为四个并行出口，各出口跳数不同。

### 7.2 Master 构建入口（6 跳）

少了 CI 环境变量这一跳，直接从 Makefile 内联的 LD_FLAGS 开始。

### 7.3 本地默认入口（5 跳）

直接从 Go 源码默认值开始，无 CI、无 ldflags。

---

## 8. 设计观察

1. **信息分层披露**：越面向用户的出口，暴露的信息越少
   - `/version`（排障用）→ 完整三字段
   - `/gotifyinfo`（登录前用）→ 仅 version
   - UI 页头（用户可见）→ 仅 version

2. **master 版本的巧妙设计**：
   - Master 构建的 Version 字符串本身包含短 commit（`master-ae9512b`）
   - 这样即使页头只展示 Version，用户也能间接知道 commit 信息
   - 同时 Header 组件的链接逻辑也利用了这一点

3. **默认值兜底**：
   - 所有字段默认值都是 `"unknown"`
   - 保证本地开发无 ldflags 时程序也能正常运行
   - 前端 config.ts 也有对应的默认值，避免 `window.config` 不存在时报错

4. **冗余但安全**：
   - `window.config.version` 里有完整的 commit 和 buildDate
   - 但 UI 代码实际上没用到
   - 属于「数据传了但没消费」的状态，留作未来扩展
