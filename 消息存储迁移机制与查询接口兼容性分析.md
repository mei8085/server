# 消息存储迁移机制与查询接口兼容性分析

## 一、无独立 Schema 版本号时的迁移判断机制

### 1.1 迁移判断的两层架构

系统**未使用独立的 schema_version 表**，而是通过以下两层机制联合判断是否需要迁移：

| 机制层次 | 判断依据 | 触发条件 | 覆盖范围 |
|---------|---------|---------|---------|
| GORM AutoMigrate | 表/列/索引是否存在 | 首次启动或版本升级 | 表结构变更（新增表、新增列、新增索引） |
| 显式 fillMissingSortKeys | sort_key 是否为 NULL/空 | 存在缺失 sort_key 的记录 | 数据完整性补全 |

### 1.2 第一层：GORM AutoMigrate 的隐式版本判断

**执行入口**：`database/database.go:83`

```go
if err := db.AutoMigrate(
    new(model.User), 
    new(model.Application), 
    new(model.Message), 
    new(model.Client), 
    new(model.PluginConf)
); err != nil {
    return nil, err
}
```

**AutoMigrate 的判断逻辑**（GORM 框架层）：

1. **表存在性检查**：`Migrator().HasTable()`
   - 若表不存在 → 创建新表（含所有列、索引、约束）
   - 若表已存在 → 进入列检查

2. **列存在性检查**：`Migrator().HasColumn()`
   - 若列不存在 → `AddColumn()` 添加新列
   - 若列已存在 → 不修改（类型变更不会自动处理）

3. **索引存在性检查**：`Migrator().HasIndex()`
   - 若索引不存在 → `CreateIndex()` 创建
   - 若索引已存在 → 不修改

**关键配置**：`DisableForeignKeyConstraintWhenMigrating: true`

- 位置：`database/database.go:38`
- 含义：迁移时不创建外键约束
- 风险：删除关联表记录时无数据库级保护

### 1.3 第二层：fillMissingSortKeys 的显式数据完整性检查

**执行入口**：`database/database.go:93`

```go
if err := db.Transaction(fillMissingSortKeys, &sql.TxOptions{Isolation: sql.LevelSerializable}); err != nil {
    return nil, err
}
```

**判断逻辑**：`database/database.go:100-108`

```go
func fillMissingSortKeys(db *gorm.DB) error {
    missingSort := int64(0)
    // 检查是否存在缺失 sort_key 的应用记录
    if err := db.Model(new(model.Application)).
        Where("sort_key IS NULL OR sort_key = ''").
        Count(&missingSort).Error; err != nil {
        return err
    }

    if missingSort == 0 {
        return nil  // 无需迁移，直接返回
    }
    
    // ... 执行数据补全
}
```

**判断依据**：
- 条件 `sort_key IS NULL OR sort_key = ''`
- 零记录 → 已迁移或全新库 → 跳过
- 有记录 → 需要补全 → 执行迁移

---

## 二、两层机制的兼容边界与升级风险

### 2.1 AutoMigrate 的兼容边界

**覆盖范围**：

| 变更类型 | AutoMigrate 支持 | 说明 |
|---------|-----------------|------|
| 新增表 | ✅ 完全支持 | 通过 `HasTable` 判断 |
| 新增列 | ✅ 完全支持 | 通过 `HasColumn` 判断，新增列使用默认值（零值） |
| 新增索引 | ✅ 完全支持 | 通过 `HasIndex` 判断 |
| 修改列类型 | ❌ 不支持 | GORM 不会修改已有列的类型，需手动 SQL |
| 删除列 | ❌ 不支持 | 为防止数据丢失，AutoMigrate 从不删除 |
| 重命名列 | ❌ 不支持 | 会被视为"删除旧列 + 新增新列"，旧数据丢失 |
| 重命名表 | ❌ 不支持 | 同上 |
| 唯一约束变更 | ⚠️ 部分支持 | 新增唯一索引可创建，但旧数据若有重复会失败 |

**边界条件示例**：

```go
// 示例：Application 模型演进
type Application struct {
    ID        uint
    Token     string `gorm:"type:varchar(180)"`  // 旧：varchar(100)
    Name      string
    SortKey   string `gorm:"type:bytes"`          // 新增列
}

// AutoMigrate 行为：
// ✅ SortKey 列会被创建（默认值为空）
// ❌ Token 的 varchar 长度不会从 100 扩大到 180
```

### 2.2 fillMissingSortKeys 的兼容边界

**覆盖范围**：

| 场景 | fillMissingSortKeys 行为 | 结果 |
|-----|-------------------------|------|
| 全新数据库（无应用） | `missingSort = 0` | 直接返回，无操作 |
| 升级前已有应用（无 SortKey） | 所有应用 sort_key = NULL | 执行补全，按用户分组生成 a0, a1, a2... |
| 部分应用有 SortKey，部分没有 | 缺失的被补全，已有保留 | 增量补全，不破坏已有排序 |
| SortKey 为空字符串'' | 被视为缺失，重新生成 | 可能改变已有排序顺序 |
| 应用被删除后重建 | 新应用获得新的 SortKey | 正常行为 |

**排序键生成算法**：`database/database.go:116-130`

```go
sortKey := ""
currentUser := uint(math.MaxUint)
for _, app := range apps {
    if currentUser != app.UserID {
        sortKey = ""           // 切换用户，重置基准
        currentUser = app.UserID
    }
    // fracdex 分数索引：在 "" 和 "" 之间生成 a0, a1, a2...
    sortKey, err = fracdex.KeyBetween(sortKey, "")
    app.SortKey = sortKey
}
```

**隐含假设**：
- 应用顺序由 `Order("user_id, sort_key, id ASC")` 决定
- 首次迁移时，排序按 `id ASC`（即创建顺序）确定

### 2.3 升级风险分析

#### 风险 1：列类型变更不生效

**场景**：
- 旧版：`Token varchar(100)`
- 新版：`Token varchar(180)`（为了支持更长的 token）

**AutoMigrate 行为**：
- 检测到列已存在 → 不做任何修改
- 新 token 长度为 150 → 写入时数据库报错

**排查方法**：
```sql
-- 检查实际列类型
DESCRIBE applications;  -- MySQL
PRAGMA table_info(applications);  -- SQLite
```

#### 风险 2：重命名列导致数据丢失

**场景**：
- 旧版：`Description string`
- 新版：`Desc string`（重命名）

**AutoMigrate 行为**：
- 无法识别"重命名"，认为是：
  - 删除 `Description` 列（AutoMigrate 不会执行）
  - 新增 `Desc` 列（会执行）
- 结果：旧 `Description` 数据被孤立，新 `Desc` 列为空

**排查方法**：
```sql
-- 检查表结构
SELECT column_name FROM information_schema.columns WHERE table_name = 'applications';
```

#### 风险 3：唯一索引冲突导致迁移失败

**场景**：
- 旧版无 `uniqueIndex:uix_application_user_id_sort_key`
- 新版新增组合唯一索引 `(user_id, sort_key)`

**迁移前数据状态**：
```
user_id | sort_key
--------+----------
1       | a0
1       | a0        ← 重复！
2       | a0
```

**AutoMigrate 行为**：
- 创建唯一索引失败 → 返回 error → 应用启动失败

**排查方法**：
```sql
-- 检查重复数据
SELECT user_id, sort_key, COUNT(*) 
FROM applications 
GROUP BY user_id, sort_key 
HAVING COUNT(*) > 1;
```

#### 风险 4：fillMissingSortKeys 排序顺序非预期

**场景**：
- 旧版应用有自定义展示顺序（通过其他方式）
- 升级后首次运行 fillMissingSortKeys

**排序结果**：
- 按 `id ASC`（创建时间顺序）生成 a0, a1, a2...
- 用户原有的自定义排序丢失

**排查方法**：
```sql
-- 检查 sort_key 是否按创建顺序分配
SELECT id, name, sort_key FROM applications ORDER BY user_id, id;
```

#### 风险 5：外键缺失导致数据不一致

**配置**：`DisableForeignKeyConstraintWhenMigrating: true`

**场景**：
- 创建消息时 `application_id = 999`（不存在的应用）
- 数据库无外键约束 → 插入成功
- 查询时 JOIN 应用表 → 消息无法被检索到

**排查方法**：
```sql
-- 查找孤立消息
SELECT m.id, m.application_id 
FROM messages m 
LEFT JOIN applications a ON m.application_id = a.id 
WHERE a.id IS NULL;
```

### 2.4 旧库升级排查清单

| 检查项 | SQL 排查命令（MySQL/SQLite 通用） | 预期结果 | 异常处理 |
|-------|----------------------------------|---------|---------|
| 表完整性 | `SELECT name FROM sqlite_master WHERE type='table' ORDER BY name;` | users, applications, messages, clients, plugin_confs | 确认是否有遗漏表 |
| 列完整性 | `PRAGMA table_info(messages);` | 含 id, application_id, message, title, priority, extras, date | 对比模型定义 |
| 索引完整性 | `PRAGMA index_list(applications);` | 含 uix_applications_token, uix_application_user_id_sort_key | 对比 gorm tag |
| 重复数据 | `SELECT user_id, sort_key, COUNT(*) FROM applications GROUP BY user_id, sort_key HAVING COUNT(*) > 1;` | 空结果 | 手动修复重复 |
| 孤立数据 | `SELECT COUNT(*) FROM messages m LEFT JOIN applications a ON m.application_id = a.id WHERE a.id IS NULL;` | 0 | 清理或修复 |
| SortKey 状态 | `SELECT COUNT(*) FROM applications WHERE sort_key IS NULL OR sort_key = '';` | 0 | 等待迁移完成 |

---

## 三、升级后查询与清理接口的行为影响

### 3.1 查询接口影响分析

#### 3.1.1 按应用查询：`GET /application/{id}/message`

**数据库实现**：`database/message.go:65-76`

```go
func (d *GormDatabase) GetMessagesByApplicationSince(appID uint, limit int, since uint) ([]*model.Message, error) {
    db := d.DB.Where("application_id = ?", appID).
        Order("messages.id desc").
        Limit(limit)
    if since != 0 {
        db = db.Where("messages.id < ?", since)
    }
    return messages, db.Find(&messages).Error
}
```

**升级影响分析**：

| 升级场景 | 对查询的影响 | 风险等级 |
|---------|-------------|---------|
| 新增 `messages` 表索引 | ✅ 无影响，查询更快 | 低 |
| 新增 `application_id` 索引 | ✅ 无影响，查询更快 | 低 |
| `application_id` 列类型变更 | ⚠️ 若缩小范围可能失败 | 中 |
| 外键约束缺失导致孤立消息 | ❌ 孤立消息不会被此接口返回 | 中（但这是正确行为） |
| `Order("messages.id desc")` 依赖自增 ID | ✅ 自增主键机制不变 | 低 |

**API 行为不变性**：
- 请求参数：`limit`, `since` 语义不变
- 响应结构：`PagedMessages` 不变
- 排序规则：始终按 ID 倒序

#### 3.1.2 按用户查询：`GET /message`

**数据库实现**：`database/message.go:39-51`

```go
func (d *GormDatabase) GetMessagesByUserSince(userID uint, limit int, since uint) ([]*model.Message, error) {
    db := d.DB.Joins("JOIN applications ON applications.user_id = ?", userID).
        Where("messages.application_id = applications.id").
        Order("messages.id desc").
        Limit(limit)
    if since != 0 {
        db = db.Where("messages.id < ?", since)
    }
    return messages, db.Find(&messages).Error
}
```

**关键依赖**：`applications.user_id` 索引

```go
// model/application.go:23
UserID uint `gorm:"index;uniqueIndex:uix_application_user_id_sort_key,priority:1" json:"-"`
```

**升级影响分析**：

| 升级场景 | 对查询的影响 | 风险等级 |
|---------|-------------|---------|
| 新增 `applications.user_id` 索引 | ✅ 无影响，JOIN 更快 | 低 |
| 新增 `applications` 表 | ✅ 旧库升级会自动创建 | 低 |
| `user_id` 列类型变更 | ⚠️ 若与 messages.application_id 不匹配 | 高 |
| 应用被删除但消息保留 | ❌ 通过 JOIN 过滤，消息不可见 | 中（与外键缺失风险相关） |
| 应用 SortKey 变更 | ✅ 查询不依赖 SortKey，无影响 | 低 |

**JOIN 语义不变性**：
- 始终返回"用户所有应用的消息"
- 孤立消息（`application_id` 指向不存在的应用）不会被返回
- 这是设计使然，而非 bug

#### 3.1.3 按时间窗口查询：`since` 游标机制

**游标过滤**：`database/message.go:44, 69`

```go
if since != 0 {
    db = db.Where("messages.id < ?", since)
}
```

**分页构建**：`api/message.go:100-119`

```go
func buildWithPaging(ctx *gin.Context, paging *pagingParams, messages []*model.Message) *model.PagedMessages {
    if len(messages) > paging.Limit {
        useMessages = messages[:len(messages)-1]
        since = useMessages[len(useMessages)-1].ID  // 下一页游标
        // 构建 next URL
    }
    return &model.PagedMessages{
        Paging:   model.Paging{Size: len(useMessages), Limit: paging.Limit, Next: next, Since: since},
        Messages: toExternalMessages(useMessages),
    }
}
```

**升级影响分析**：

| 升级场景 | 对时间窗口的影响 | 风险等级 |
|---------|-----------------|---------|
| 消息 ID 分配机制变更 | ❌ 游标依赖 ID 单调性，若 ID 不再递增则分页混乱 | 高 |
| 消息被删除（已读过的） | ✅ since 游标指向 ID，删除不影响后续翻页 | 低 |
| 消息被删除（未读过的） | ✅ 游标机制天然免疫 offset 偏移问题 | 低 |
| 数据库重置导致 ID 重新从 1 开始 | ❌ 客户端保存的 since 游标失效 | 高 |

**游标不变性**：
- 只要 `messages.id` 保持自增且单调递增
- 只要 `Order("messages.id desc")` 排序规则不变
- 分页行为就是可预测的

### 3.2 历史清理接口影响分析

#### 3.2.1 单条删除：`DELETE /message/{id}`

**权限校验流程**：`api/message.go:307-326`

```go
func (a *MessageAPI) DeleteMessage(ctx *gin.Context) {
    withID(ctx, "id", func(id uint) {
        // 1. 查询消息（使用 GetMessageByID）
        msg, err := a.DB.GetMessageByID(id)
        if msg == nil {
            ctx.AbortWithError(404, ...)
            return
        }
        
        // 2. 查询应用（使用 GetApplicationByID）
        app, err := a.DB.GetApplicationByID(msg.ApplicationID)
        
        // 3. 校验归属
        if app != nil && app.UserID == auth.GetUserID(ctx) {
            // 4. 执行删除
            a.DB.DeleteMessageByID(id)
        }
    })
}
```

**升级影响分析**：

| 升级场景 | 对删除的影响 | 风险等级 |
|---------|-------------|---------|
| `GetMessageByID` 查询路径变更 | ✅ 仅依赖主键，稳定 | 低 |
| `GetApplicationByID` 查询路径变更 | ✅ 仅依赖主键，稳定 | 低 |
| 应用被删除但消息存在 | ❌ `GetApplicationByID` 返回 nil → 删除失败（404） | 中 |
| 外键约束启用 | ✅ 数据库级保护与应用层校验一致 | 低 |

**与查询接口的关联**：
- 删除前**必须先查询**确认消息存在且归属正确
- 使用与读接口相同的 `GetMessageByID`、`GetApplicationByID`
- 这意味着：若查询接口能找到某条消息，删除接口也能找到它

#### 3.2.2 应用维度清理：`DELETE /application/{id}/message`

**数据库实现**：`database/message.go:84-86`

```go
func (d *GormDatabase) DeleteMessagesByApplication(applicationID uint) error {
    return d.DB.Where("application_id = ?", applicationID).Delete(&model.Message{}).Error
}
```

**与查询的对应关系**：

| 操作 | 查询接口 | 删除接口 | 条件 |
|-----|---------|---------|------|
| 检索应用消息 | `GetMessagesByApplicationSince` | `DeleteMessagesByApplication` | `application_id = ?` |

**升级影响分析**：

| 升级场景 | 对清理的影响 | 风险等级 |
|---------|-------------|---------|
| `application_id` 索引变更 | ✅ 不影响 DELETE 条件，但可能影响性能 | 低 |
| 外键约束启用 | ✅ CASCADE 删除与应用层删除一致 | 低 |
| 应用被删除后再清理 | ❌ 应用不存在 → 权限校验失败（404） | 中 |
| 孤立消息清理 | ❌ 此接口无法清理孤立消息（需直接 SQL） | 低 |

#### 3.2.3 用户维度清理：`DELETE /message`

**数据库实现**：`database/message.go:89-94`

```go
func (d *GormDatabase) DeleteMessagesByUser(userID uint) error {
    // 1. 查询用户所有应用（与 GetMessagesByUser 相同路径）
    app, _ := d.GetApplicationsByUser(userID)
    
    // 2. 逐个应用删除（复用 DeleteMessagesByApplication）
    for _, app := range app {
        d.DeleteMessagesByApplication(app.ID)
    }
    return nil
}
```

**与查询的对称设计**：

| 查询路径 | 删除路径 |
|---------|---------|
| `GetMessagesByUser` → JOIN applications → 聚合消息 | `DeleteMessagesByUser` → GetApplicationsByUser → 逐个删除 |
| 依赖 `applications.user_id` | 依赖 `GetApplicationsByUser` |

**升级影响分析**：

| 升级场景 | 对清理的影响 | 风险等级 |
|---------|-------------|---------|
| `GetApplicationsByUser` 返回顺序变更 | ✅ 不影响最终结果（全删） | 低 |
| 新增应用字段 | ✅ 删除不依赖这些字段 | 低 |
| 孤立消息 | ❌ 无法通过此接口清理（与查询一致，它们"不可见"） | 低 |
| `applications.user_id` 索引缺失 | ⚠️ 查询变慢 → 删除也变慢 | 中 |

### 3.3 升级后接口行为总结表

| 接口 | 路径 | 依赖的数据库特性 | 升级风险 | 风险说明 |
|-----|------|-----------------|---------|---------|
| **按应用查询** | `GET /application/{id}/message` | `messages.application_id` 索引 | 低 | 仅依赖单列索引，稳定 |
| **按用户查询** | `GET /message` | `JOIN applications` + `user_id` 索引 | 中 | 若应用表异常会影响消息可见性 |
| **时间窗口** | `?since=X` 参数 | `messages.id` 自增单调性 | 高 | 若 ID 分配机制变了，分页全乱 |
| **单条删除** | `DELETE /message/{id}` | `GetMessageByID` + `GetApplicationByID` | 中 | 若应用被删除，消息无法删除（但查询也查不到） |
| **应用清理** | `DELETE /application/{id}/message` | `messages.application_id` 条件 | 低 | 与查询条件对称 |
| **用户清理** | `DELETE /message` | `GetApplicationsByUser` + 循环删除 | 中 | 与查询路径对称，孤立消息不可见也不可删 |

### 3.4 升级后的不变性保证

**始终不变的行为**：

1. **排序不变**：所有查询始终按 `messages.id desc`
2. **分页不变**：`since` 游标机制不依赖数据量，只依赖 ID 顺序
3. **权限不变**：删除前必先查询归属，与查询接口使用同一验证路径
4. **一致性**：查询能看到的消息 → 清理接口也能处理
5. **孤立消息**：查询不可见 → 删除也不可达（设计一致性）

**可能变化的性能**：

1. **查询性能**：新增索引可能使查询变快
2. **删除性能**：索引变更可能影响 DELETE 效率
3. **启动时间**：首次升级时 `fillMissingSortKeys` 可能耗时较长

---

## 四、迁移决策流程图

```
                    应用启动
                       │
                       ▼
              ┌─────────────────┐
              │  打开数据库连接  │
              └────────┬────────┘
                       │
                       ▼
              ┌─────────────────┐
              │  GORM AutoMigrate │
              └────────┬────────┘
                       │
         ┌─────────────┼─────────────┐
         ▼             ▼             ▼
    表不存在？     列不存在？     索引不存在？
         │             │             │
         ▼             ▼             ▼
    CreateTable    AddColumn     CreateIndex
         │             │             │
         └─────────────┼─────────────┘
                       │
                       ▼
              ┌─────────────────┐
              │ fillMissingSortKeys │
              └────────┬────────┘
                       │
              ┌────────┴────────┐
              │ sort_key 缺失？  │
              └────────┬────────┘
                       │
           ┌───────────┴───────────┐
           │ 是（需要迁移）          │ 否（已迁移）
           ▼                       ▼
    按用户分组生成排序键       ┌─────────┐
           │                  │ 直接启动 │
           ▼                  └────┬────┘
    Save 更新数据库                  │
           │                         │
           └───────────┬─────────────┘
                       │
                       ▼
              ┌─────────────────┐
              │   应用启动完成    │
              └─────────────────┘
```

---

## 五、升级检查清单（运维视角）

### 5.1 升级前检查

```bash
# 1. 备份数据库
mysqldump gotify > gotify_backup.sql  # MySQL
# 或
sqlite3 gotify.db ".backup gotify_backup.db"  # SQLite

# 2. 检查列类型是否匹配模型
# 对比 model/*.go 中的 gorm tag 与实际表结构

# 3. 检查是否有重复数据（可能导致唯一索引创建失败）
SELECT user_id, sort_key, COUNT(*) 
FROM applications 
GROUP BY user_id, sort_key 
HAVING COUNT(*) > 1;

# 4. 检查孤立消息
SELECT COUNT(*) 
FROM messages m 
LEFT JOIN applications a ON m.application_id = a.id 
WHERE a.id IS NULL;
```

### 5.2 升级中监控

```bash
# 1. 观察启动日志，关注：
# - "Migrating X application sort keys" 表示 fillMissingSortKeys 正在执行
# - 无报错则 AutoMigrate 成功

# 2. 首次启动可能较慢（sort_key 迁移）
```

### 5.3 升级后验证

```sql
-- 1. 验证所有表存在
SELECT name FROM sqlite_master WHERE type='table';

-- 2. 验证 sort_key 已填充
SELECT COUNT(*) FROM applications WHERE sort_key IS NULL OR sort_key = '';
-- 应返回 0

-- 3. 验证索引已创建
PRAGMA index_list(applications);

-- 4. 验证 API 可访问
# GET /message 应正常返回
# GET /application/{id}/message 应正常返回
```

### 5.4 回滚方案

1. **数据库回滚**：使用升级前的备份恢复
2. **应用回滚**：使用旧版本二进制重新部署
3. **注意**：AutoMigrate 不会删除列，回滚后新增列会被忽略但不会报错
