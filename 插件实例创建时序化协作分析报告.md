# 插件实例创建时序化协作分析报告

## 1. 主链路总览

插件实例创建是一个跨多层协作的完整流程，涉及**前端UI层**、**API层**、**认证层**、**插件管理层**和**数据库层**的深度交互。

### 1.1 全链路时序总览

```
 用户
  ↓ 点击插件列表页面
[前端: PluginUI]
  ↓ T0. 触发动作: 页面加载/刷新
  ↓ 依赖: Client Token (Cookie)
  ├─ PluginStore.refresh()
  └─ GET /plugin
      ↓
[认证中间件]
  ↓ T1. 认证校验
  ↓ 依赖: 从请求提取 Token
  ├─ Token 有效性验证
  ├─ 查询用户/客户端信息
  └─ RegisterUser/RegisterClient 注入 Context
      ↓
[API层: PluginAPI]
  ↓ T2. API 处理
  ↓ 依赖: auth.GetUserID(ctx)
  ├─ 从 Context 提取 UserID
  └─ 调用 Manager 获取插件列表
      ↓
[插件管理层: Manager]
  ↓ T3. 插件实例创建（系统启动时）
  ↓ 依赖: 完整 User 对象
  ├─ loadPlugins() - 加载 .so 插件
  ├─ compat.Wrap() - 兼容封装
  ├─ GetUsers() - 获取所有用户
  └─ initializeForUser() - 为每个用户创建实例
      ↓
[兼容层: compat]
  ↓ T4. 实例创建与能力注入
  ↓ 依赖: UserContext
  ├─ NewPluginInstance(ctx)
  ├─ 类型断言检测能力接口
  └─ SetMessageHandler/SetStorageHandler 注入
      ↓
[数据库层]
  ↓ T5. 持久化
  ↓ 依赖: PluginConf 结构体
  ├─ 生成 Plugin Token
  ├─ Messenger 插件生成 Application Token
  └─ 保存 PluginConf 记录
      ↓
[结果回传]
  ↓ T6. 原路返回
  ├─ Manager → API: []PluginConf
  ├─ API → 前端: JSON 数组
  └─ 前端: 渲染插件列表表格
```

---

## 2. 分阶段时序协作详解

### 阶段 0: 前端触发 - 插件列表加载

#### 2.0.1 触发动作详情

| 项 | 详情 |
|----|------|
| **发起者** | 用户访问 /plugins 页面 |
| **触发入口** | `ui/src/plugin/Plugins.tsx:20` |
| **触发代码** | `React.useEffect(() => void pluginStore.refresh(), [])` |
| **前置条件** | 用户已登录，持有有效 Client Token |
| **依赖认证信息** | Cookie 中的 `gotify-client-token` (Client Token) |

#### 2.0.2 前端状态机流转

```
  [初始状态: 未加载]
      ↓ pluginStore.refresh()
  [加载中状态]
      ↓ axios GET /plugin
  ┌───────────────┐     ┌──────────────┐
  │ 请求成功: 200 │ →   │ 已加载状态   │ → 渲染表格
  └───────────────┘     └──────────────┘
         ↓
  ┌───────────────┐     ┌──────────────┐
  │ 请求失败: 401 │ →   │ 跳转登录页   │
  └───────────────┘     └──────────────┘
         ↓
  ┌───────────────┐     ┌──────────────┐
  │ 请求失败: 500 │ →   │ 显示错误提示 │
  └───────────────┘     └──────────────┘
```

#### 2.0.3 PluginStore 调用链

**文件位置**: `ui/src/plugin/PluginStore.ts`

```typescript
// T0.1: MobX 状态触发 refresh
public refresh = async (): Promise<void> => {
    // 调用 BaseStore.refresh → requestItems
}

// T0.2: 实际 HTTP 请求发送
protected requestItems = (): Promise<IPlugin[]> =>
    axios.get<IPlugin[]>(`${config.get('url')}plugin`)
        .then((response) => response.data)

// T0.3: axios 拦截器自动注入认证
// - Cookie: gotify-client-token (自动携带)
// - Header: X-Gotify-Key 或 Authorization: Bearer (配置决定)
```

#### 2.0.4 失败回退机制

| 失败场景 | HTTP 状态 | 回退行为 | 用户感知 |
|----------|----------|---------|---------|
| Token 无效/过期 | 401 Unauthorized | 清除本地 Token，跳转到登录页 | 需重新登录 |
| 用户无权限 | 403 Forbidden | 显示权限错误提示，保持当前页面 | Snackbar 提示 |
| 服务端异常 | 500 Internal Error | 显示加载失败，提供重试按钮 | Snackbar 提示 + 重试按钮 |
| 网络超时 | - | 显示网络错误，提供重试按钮 | Snackbar 提示 + 重试按钮 |

---

### 阶段 1: 认证层 - Token 校验与上下文注入

#### 2.1.1 认证中间件执行时序

**文件位置**: `auth/authentication.go:84-106`

```
T1.0 收到 HTTP 请求
     ↓
T1.1 读取 Token (多来源尝试)
     ├─ 尝试 1: Query 参数 ?token=xxx
     ├─ 尝试 2: Header X-Gotify-Key
     ├─ 尝试 3: Header Authorization: Bearer xxx
     └─ 尝试 4: Cookie gotify-client-token
     ↓ 无 Token → 401 Unauthorized

T1.2 认证类型判断
     ├─ 分支 A: Client Token (C 前缀)
     │   ├─ DB: GetClientByToken(token)
     │   ├─ 存在 → RegisterClient(ctx, client)
     │   └─ 不存在 → 继续下一种认证
     │
     ├─ 分支 B: Basic Auth (用户名/密码)
     │   ├─ DB: GetUserByName(name)
     │   ├─ 密码比对 (bcrypt)
     │   ├─ 通过 → RegisterUser(ctx, user)
     │   └─ 失败 → 继续下一种认证
     │
     └─ 分支 C: Application Token (A 前缀)
         ├─ DB: GetApplicationByToken(token)
         ├─ 存在 → RegisterApplication(ctx, app)
         └─ 不存在 → 认证失败

T1.3 权限检查 (RequireClient)
     ├─ 检查是否已注册 Client 或 User
     ├─ 通过 → ctx.Next() 进入 API 处理
     └─ 失败 → 401/403 中止请求
```

#### 2.1.2 交接点: 上下文注入

| 交接项 | 详情 |
|--------|------|
| **发起方** | 认证中间件 (`auth/authentication.go`) |
| **接收方** | Gin Context (后续所有 Handler) |
| **传递数据** | `authentication` 结构体 (包含 user/client/app) |
| **注入方式** | `ctx.Set(authKey, &authentication{...})` |
| **文件位置** | `auth/util.go:17-29` |

**注入代码**:
```go
// 注册用户 (Basic Auth 场景)
func RegisterUser(ctx *gin.Context, user *model.User) {
    ctx.Set(authKey, &authentication{user: user})
}

// 注册客户端 (Token 场景)
func RegisterClient(ctx *gin.Context, client *model.Client) {
    ctx.Set(authKey, &authentication{client: client})
}

// 注册应用 (Application Token 场景)
func RegisterApplication(ctx *gin.Context, app *model.Application) {
    ctx.Set(authKey, &authentication{app: app})
}
```

#### 2.1.3 认证层失败回退

| 失败点 | 回退机制 | 返回结果 |
|--------|---------|---------|
| Token 格式无效 | 跳过该认证方式，尝试下一种 | 最终全部失败返回 401 |
| Token 数据库查询失败 | `return authStateSkip, err` | 500 Internal Error |
| 密码验证失败 | 跳过 Basic Auth，继续其他方式 | 最终 401 |
| 权限检查不通过 (非 Client/User) | `ctx.AbortWithError(401/403)` | 立即中止，返回错误 |
| 客户端已过期 | 检查 ElevatedUntil，未通过则 403 | 403 Forbidden |

---

### 阶段 2: API 层 - 用户身份提取与业务分发

#### 2.2.1 GetPlugins 接口时序

**文件位置**: `api/plugin.go:63-86`

```
T2.0 进入 GetPlugins Handler
     ↓
T2.1 提取 UserID
     ├─ 调用: auth.GetUserID(ctx)
     ├─ 实现: getInfo(ctx).GetUserID()
     └─ 从 authentication 结构体提取
     ↓ 提取失败 → Panic (认证层应保证存在)

T2.2 查询插件配置
     ├─ DB: GetPluginConfByUser(userID)
     └─ 返回: []*model.PluginConf
     ↓ 查询失败 → 500 Internal Error

T2.3 组装外部模型
     对每个 PluginConf:
     ├─ Manager.Instance(conf.ID) → 获取插件实例
     ├─ Manager.PluginInfo(conf.ModulePath) → 获取元信息
     ├─ 转换为 PluginConfExternal 模型
     └─ 填充 Capabilities 字段
     ↓

T2.4 返回响应
     └─ ctx.JSON(200, result)
```

#### 2.2.2 交接点: UserID 传递

| 交接项 | 详情 |
|--------|------|
| **发起方** | 认证中间件 (已注入 authentication) |
| **接收方** | `PluginAPI.GetPlugins` |
| **提取接口** | `auth.GetUserID(ctx) uint` |
| **文件位置** | `auth/util.go` |
| **依赖假设** | 认证层已保证 UserID 存在，否则 Panic |

**提取代码**:
```go
func GetUserID(ctx *gin.Context) uint {
    return getInfo(ctx).GetUserID()
}

// authentication 结构体方法
func (a *authentication) GetUserID() uint {
    switch {
    case a.user != nil:
        return a.user.ID
    case a.client != nil:
        return a.client.UserID
    case a.app != nil:
        return a.app.UserID
    default:
        panic("unauthenticated")
    }
}
```

#### 2.2.3 API 层失败回退

| 失败点 | 回退机制 | 返回结果 |
|--------|---------|---------|
| UserID 提取失败 (Panic) | Gin Recovery 捕获 | 500 Internal Error |
| 数据库查询 PluginConf 失败 | `successOrAbort(ctx, 500, err)` | 500 Internal Error |
| 插件实例获取失败 | 跳过该插件，不加入结果 | 部分成功 |
| 插件元信息获取失败 | 使用默认占位信息 | UNKNOWN 插件名 |

---

### 阶段 3: 插件管理层 - 实例创建与初始化

#### 2.3.1 系统启动时插件加载时序

**文件位置**: `plugin/manager.go:56-101`

```
T3.0 NewManager 初始化
     ↓
T3.1 启动消息处理 goroutine
     go func() {
         for {
             message := <-manager.messages
             // 处理消息: 存 DB + Notifier 广播
         }
     }()
     ↓

T3.2 加载插件文件 (loadPlugins)
     遍历插件目录:
     ├─ plugin.Open(path) - 加载 .so 动态库
     ├─ compat.Wrap(plugin) - 兼容封装
     │   ├─ 查找 GetGotifyPluginInfo 符号
     │   ├─ 查找 NewGotifyPluginInstance 符号
     │   └─ 创建 PluginV1 包装对象
     └─ 存入 manager.plugins map
     ↓ 任一插件失败 → 整个 Manager 创建失败

T3.3 获取所有用户
     DB: GetUsers() → []*model.User
     ↓ 查询失败 → 返回 error

T3.4 为每个用户初始化插件
     for _, user := range users {
         initializeForUser(*user)
     }
     ↓ 任一用户失败 → 整个 Manager 创建失败
```

#### 2.3.2 单用户插件初始化时序

**文件位置**: `plugin/manager.go:281-313`

```
T3.5 构建 UserContext
     userCtx := compat.UserContext{
         ID:    user.ID,
         Name:  user.Name,
         Admin: user.Admin,
     }
     ↓

T3.6 遍历所有插件创建实例
     for _, p := range manager.plugins {
         initializeSingleUserPlugin(userCtx, p)
     }
     ↓

T3.7 更新应用标记
     对用户所有 Application:
     ├─ 检查是否为插件关联应用
     └─ 设置 Internal = true/false
```

#### 2.3.3 单插件实例化详细流程

**文件位置**: `plugin/manager.go:315-364`

```
T3.8 调用插件构造函数
     instance := p.NewPluginInstance(userCtx)
     ↓ 进入 compat 层 (见阶段 4)

T3.9 创建 PluginConf 数据库记录
     若不存在则创建:
     ├─ 生成 Plugin Token (P 前缀)
     │   auth.GenerateNotExistingToken(
     │       auth.GeneratePluginToken,
     │       m.pluginConfExists)
     │
     ├─ Messenger 能力插件:
     │   ├─ 生成 Application Token (A 前缀)
     │   ├─ 创建 Application 记录
     │   └─ 关联 ApplicationID 到 PluginConf
     │
     ├─ Configurer 能力插件:
     │   └─ 序列化默认配置到 Config 字段
     │
     └─ DB: CreatePluginConf(pluginConf)
     ↓

T3.10 存储实例引用
     manager.instances[pluginConf.ID] = instance
     ↓

T3.11 根据能力注入 Handler
     ├─ [Messenger] SetMessageHandler(redirectToChannel{
     │     ApplicationID: pluginConf.ApplicationID,
     │     UserID:        pluginConf.UserID,
     │     Messages:      manager.messages,
     │ })
     │
     ├─ [Storager] SetStorageHandler(dbStorageHandler{
     │     pluginID: pluginConf.ID,
     │     db:       manager.db,
     │ })
     │
     ├─ [Configurer] 初始化配置
     │   └─ 验证当前配置，失败则重置为默认
     │
     └─ [Webhooker] RegisterWebhook(
          basePath: pluginConf.Token + "/",
          mux:      mux.Group(..., requirePluginEnabled),
        )
     ↓

T3.12 启用插件实例
     if pluginConf.Enabled {
         err := instance.Enable()
         if err != nil {
             log.Printf("启用失败，禁用插件")
             pluginConf.Enabled = false
             DB.UpdatePluginConf(pluginConf)
         }
     }
```

#### 2.3.4 交接点: UserContext 传递

| 交接项 | 详情 |
|--------|------|
| **发起方** | `Manager.initializeForUser` |
| **接收方** | `Plugin.NewPluginInstance` (compat 层) |
| **传递数据** | `compat.UserContext{ID, Name, Admin}` |
| **传递方式** | 函数参数传递 |
| **边界保证** | 仅暴露必要的用户信息，不传递完整 User 对象 |

#### 2.3.5 管理层失败回退

| 失败点 | 回退机制 | 影响范围 |
|--------|---------|---------|
| 插件 .so 加载失败 | 返回 error，整个 Manager 创建失败 | 系统启动失败 |
| compat.Wrap 符号查找失败 | 返回 error，整个 Manager 创建失败 | 系统启动失败 |
| GetUsers 查询失败 | 返回 error，整个 Manager 创建失败 | 系统启动失败 |
| 单用户初始化失败 | 返回 error，整个 Manager 创建失败 | 系统启动失败 |
| 单插件实例化失败 | 继续处理下一个插件，记录日志 | 仅该插件不可用 |
| Application 创建失败 | 返回 error，整个初始化失败 | 系统启动失败 |
| PluginConf 创建失败 | 返回 error，整个初始化失败 | 系统启动失败 |
| instance.Enable() 失败 | 禁用插件，更新 DB，不返回 error | 仅该插件被禁用 |
| 配置验证失败 | 重置为默认配置，禁用插件，记录日志 | 仅该插件受影响 |

---

### 阶段 4: 兼容层 - 适配器模式实例化

#### 2.4.1 PluginV1 实例创建时序

**文件位置**: `plugin/compat/v1.go:35-67`

```
T4.0 调用插件构造函数
     instance := c.Constructor(papiv1.UserContext{
         ID:    ctx.ID,
         Name:  ctx.Name,
         Admin: ctx.Admin,
     })
     ↓ 调用外部插件代码

T4.1 创建适配器
     compat := &PluginV1Instance{
         instance: instance,
     }
     ↓

T4.2 类型断言检测能力
     ├─ if _, ok := instance.(papiv1.Displayer); ok
     │   └─ compat.displayer = displayer
     │
     ├─ if _, ok := instance.(papiv1.Messenger); ok
     │   └─ compat.messenger = messenger
     │
     ├─ if _, ok := instance.(papiv1.Configurer); ok
     │   └─ compat.configurer = configurer
     │
     ├─ if _, ok := instance.(papiv1.Storager); ok
     │   └─ compat.storager = storager
     │
     └─ if _, ok := instance.(papiv1.Webhooker); ok
         └─ compat.webhooker = webhooker
     ↓

T4.3 返回统一接口
     return compat (as PluginInstance)
```

#### 2.4.2 能力接口动态检测

| 能力接口 | 断言检测位置 | 后续操作 |
|----------|-------------|---------|
| `papiv1.Displayer` | `v1.go:46-48` | `GetDisplay()` 转发调用 |
| `papiv1.Messenger` | `v1.go:50-52` | `SetMessageHandler()` 注入 Handler |
| `papiv1.Configurer` | `v1.go:54-56` | `DefaultConfig()` / `ValidateAndSetConfig()` |
| `papiv1.Storager` | `v1.go:58-60` | `SetStorageHandler()` 注入 Handler |
| `papiv1.Webhooker` | `v1.go:62-64` | `RegisterWebhook()` 注册路由 |

#### 2.4.3 交接点: Handler 适配层

**MessageHandler 适配器** (`v1.go:146-158`):
```go
type PluginV1MessageHandler struct {
    WrapperHandler MessageHandler // 内部接口
}

func (c *PluginV1MessageHandler) SendMessage(msg papiv1.Message) error {
    // 转换: papiv1.Message → compat.Message
    return c.WrapperHandler.SendMessage(compat.Message{
        Message:  msg.Message,
        Priority: msg.Priority,
        Title:    msg.Title,
        Extras:   msg.Extras,
    })
}
```

**StorageHandler 适配器** (`v1.go:171-183`):
```go
type PluginV1StorageHandler struct {
    WrapperHandler StorageHandler // 内部接口
}

func (c *PluginV1StorageHandler) Save(b []byte) error {
    return c.WrapperHandler.Save(b) // 直接转发
}

func (c *PluginV1StorageHandler) Load() ([]byte, error) {
    return c.WrapperHandler.Load() // 直接转发
}
```

#### 2.4.4 兼容层失败处理

兼容层为纯内存操作，无 IO 失败场景：
- **类型断言失败**: 对应的能力字段为 nil，`Supports()` 不包含该能力
- **外部插件构造 panic**: Go plugin 机制会 panic，向上传递到 Manager 层
- **方法调用转发失败**: 若能力字段为 nil，调用对应方法返回零值或空操作

---

### 阶段 5: 数据库层 - Token 生成与持久化

#### 2.5.1 Token 生成时序

**文件位置**: `auth/token.go`

```
T5.0 调用 GenerateNotExistingToken
     generateToken: GeneratePluginToken / GenerateApplicationToken
     tokenExists: m.pluginConfExists / m.applicationExists
     ↓

T5.1 循环生成直到唯一
     for {
         token := generateToken()  // 生成 P/A 前缀 + 22 随机字符
         if !tokenExists(token) {
             return token
         }
     }
     ↓ 理论上可能无限循环（概率极低）

T5.2 Token 格式
     ├─ Plugin Token: P + 22 随机字符 (例: Pabc123...)
     ├─ Application Token: A + 22 随机字符 (例: Axyz789...)
     └─ Client Token: C + 22 随机字符 (例: Cdef456...)
```

#### 2.5.2 PluginConf 持久化

**模型结构** (`model/pluginconf.go`):
```go
type PluginConf struct {
    ID            uint    // 主键
    UserID        uint    // 关联用户
    ModulePath    string  // 插件模块路径
    Token         string  // Plugin Token (P 前缀)
    ApplicationID uint    // Messenger 插件关联应用
    Enabled       bool    // 是否启用
    Config        []byte  // YAML 配置
    Storage       []byte  // Storager 插件存储
}
```

#### 2.5.3 数据库失败回退

| 操作 | 失败回退 | 影响 |
|------|---------|------|
| `CreatePluginConf` | 返回 error 到上层 | Manager 初始化失败，系统启动中止 |
| `UpdatePluginConf` (启用失败) | 日志记录，不返回 error | 插件被标记为禁用 |
| `UpdatePluginConf` (配置重置) | 日志记录，不返回 error | 使用默认配置 |
| `GetPluginConfByID` | 返回 error | API 调用返回 500 |

---

### 阶段 6: 结果回传 - 原路返回链路

#### 2.6.1 回传时序

```
T6.0 数据库返回 PluginConf
     ↓ Manager.initializeSingleUserPlugin
T6.1 实例存入 manager.instances map
     ↓ Manager.initializeForUser
T6.2 所有用户插件初始化完成
     ↓ 返回 Manager 对象给上层
T6.3 系统启动完成，等待 API 请求
     ↓ (用户访问插件列表时)
T6.4 API: GetPluginConfByUser(userID)
     ↓ Manager.Instance() + Manager.PluginInfo()
T6.5 转换为 PluginConfExternal
     ↓ ctx.JSON(200, result)
T6.6 前端 axios 收到响应
     ↓ PluginStore 状态更新
T6.7 React 组件 observer 检测到变化
     ↓ 重新渲染表格
T6.8 用户看到插件列表
```

#### 2.6.2 前端渲染时序

**文件位置**: `ui/src/plugin/Plugins.tsx:18-55`

```
React.useEffect → pluginStore.refresh()
     ↓ (异步等待)
MobX observable 状态更新
     ↓
observer() 包装组件触发重渲染
     ↓
plugins.map() 遍历每个插件
     ↓
渲染 <Row> 组件，包含:
- ID
- Enabled Switch (绑定 changeEnabledState)
- Name
- Token (可复制)
- Settings 按钮链接
```

---

## 3. 关键交接点汇总表

| 编号 | 交接点名称 | 发起模块 | 接收模块 | 传递数据 | 认证依赖 | 失败回退 |
|------|-----------|---------|---------|---------|---------|---------|
| J1 | 前端 Token 发送 | PluginStore | 认证中间件 | Client Token | Cookie / Header | 401 → 跳转登录 |
| J2 | 认证信息注入 | 认证中间件 | Gin Context | authentication 结构体 | Token 有效性验证 | 401/403 中止 |
| J3 | UserID 提取 | Gin Context | PluginAPI | UserID (uint) | 认证层保证存在 | Panic → 500 |
| J4 | UserContext 构建 | Manager | compat 层 | UserContext{ID, Name, Admin} | DB User 对象 | 初始化失败 → 系统启动中止 |
| J5 | 插件实例返回 | compat 层 | Manager | PluginInstance 接口 | 无 (纯内存) | 类型断言失败 → 能力缺失 |
| J6 | MessageHandler 注入 | Manager | 插件实例 | redirectToChannel | ApplicationID、UserID | 无 (Messenger 能力可选) |
| J7 | StorageHandler 注入 | Manager | 插件实例 | dbStorageHandler | PluginID | 无 (Storager 能力可选) |
| J8 | Webhook 路由注册 | Manager | Gin Router | gin.RouterGroup | Plugin Token | 无 (Webhooker 能力可选) |
| J9 | PluginConf 持久化 | Manager | 数据库 | PluginConf 结构体 | 事务外单条插入 | 失败 → 系统启动中止 |
| J10 | 插件列表返回 | PluginAPI | 前端 | []PluginConfExternal | User 身份校验 | 500 → 前端重试 |

---

## 4. 失败场景完整链路

### 4.1 场景: 插件启用失败

**触发点**: `manager.go:353-361` - `instance.Enable()` 返回 error

```
1. 插件初始化时 Enabled = true
   ↓
2. 调用 instance.Enable() → 返回 error
   ↓
3. 回退动作 1: log.Printf() 记录错误日志
   ↓
4. 回退动作 2: pluginConf.Enabled = false
   ↓
5. 回退动作 3: DB.UpdatePluginConf(pluginConf)
   ↓
6. 结果: 插件被静默禁用，不影响其他插件
   ↓
7. 用户感知: 前端插件列表显示 Enabled = false
```

### 4.2 场景: 插件配置损坏

**触发点**: `manager.go:374` - YAML 解析失败或 ValidateAndSetConfig 失败

```
1. 读取 PluginConf.Config (损坏的 YAML)
   ↓
2. yaml.Unmarshal 到默认配置结构 → 失败
   ↓ 或
3. instance.ValidateAndSetConfig(config) → 失败
   ↓
4. 回退动作 1: pluginConf.Enabled = false
   ↓
5. 回退动作 2: 生成注释说明的新配置内容
   ↓
6. 回退动作 3: 写入默认配置 + 注释 + 原配置(被注释)
   ↓
7. 回退动作 4: DB.UpdatePluginConf(pluginConf)
   ↓
8. 回退动作 5: instance.ValidateAndSetConfig(默认配置)
   ↓
9. 结果: 配置重置，插件禁用，用户可重新配置后启用
```

### 4.3 场景: 前端认证过期

**触发点**: 前端发送请求时 Cookie 中的 Token 已过期

```
1. 用户访问 /plugins 页面
   ↓
2. React.useEffect 触发 pluginStore.refresh()
   ↓
3. axios 发送 GET /plugin (携带过期 Token)
   ↓
4. 认证中间件验证 Token 无效
   ↓
5. 返回 401 Unauthorized
   ↓
6. 前端 axios 拦截器检测到 401
   ↓
7. 回退动作 1: 清除本地 Token 和用户信息
   ↓
8. 回退动作 2: react-router 跳转到 /login
   ↓
9. 结果: 用户需要重新登录
```

---

## 5. 时序图汇总

```
┌─────────┐     ┌─────────────┐     ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  用户   │     │  前端UI     │     │  认证中间件  │     │  PluginAPI  │     │   Manager   │
└────┬────┘     └──────┬──────┘     └──────┬──────┘     └──────┬──────┘     └──────┬──────┘
     │                 │                    │                    │                    │
     │ 访问 /plugins   │                    │                    │                    │
     │ ──────────────> │                    │                    │                    │
     │                 │                    │                    │                    │
     │                 │ useEffect 触发     │                    │                    │
     │                 │ refresh()          │                    │                    │
     │                 │ ──────────────────>│                    │                    │
     │                 │ 携带 Client Token  │                    │                    │
     │                 │                    │ Token 校验         │                    │
     │                 │                    │ RegisterClient()   │                    │
     │                 │                    │ ────────────────> │                    │
     │                 │                    │ (注入 Context)     │                    │
     │                 │                    │                    │ GetUserID()        │
     │                 │                    │                    │ ─────────────────> │
     │                 │                    │                    │                    │ GetPluginConfByUser
     │                 │                    │                    │                    │ Instance()
     │                 │                    │                    │                    │ PluginInfo()
     │                 │                    │                    │                    │ < 返回插件列表 >
     │                 │                    │                    │ JSON 200          │
     │                 │                    │                    │ <────────────────── │
     │                 │ 渲染插件列表        │                    │                    │
     │                 │ <────────────────── │                    │                    │
     │ 看到插件列表     │                    │                    │                    │
     │ <────────────── │                    │                    │                    │
     │                 │                    │                    │                    │
```

---

## 6. 总结

### 6.1 协作核心特征

1. **分层认证传递**: Token → authentication 结构体 → UserID → UserContext → PluginConf，逐层收敛
2. **能力可选注入**: 5种能力接口动态检测，按需注入 Handler，不强制插件实现所有能力
3. **失败隔离设计**: 单插件启用/配置失败不影响其他插件，仅系统级失败才中止启动
4. **适配器解耦**: compat 层作为防腐层，隔离外部插件 API 版本差异

### 6.2 关键设计决策

| 决策 | 优点 | 代价 |
|------|------|------|
| 启动时预创建所有用户所有插件实例 | 运行时无延迟，响应快 | 启动慢，内存占用高 |
| 插件实例按用户完全隔离 | 安全，配置独立 | 实例数 = 插件数 × 用户数 |
| 静默失败策略 (启用失败仅禁用) | 系统容错性好 | 问题发现滞后 |
| Token 循环生成直到唯一 | 保证全局唯一 | 理论上可能无限循环 |

### 6.3 潜在优化点

1. **懒加载实例**: 按需创建插件实例，而非启动时全量创建
2. **实例池复用**: 相同配置的用户可共享实例（需考虑安全隔离）
3. **失败通知**: 插件启用失败时应通过某种方式通知管理员
4. **重试机制**: Token 生成、数据库操作可增加有限重试
