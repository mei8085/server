# 事务边界划定与并发冲突处理分析报告

## 一、项目架构概述

本项目（Gotify Server v2）是一个基于 Go 语言开发的消息推送服务，采用经典的分层架构：

```
┌─────────────────────────────────────────────────────┐
│  API 层 (api/*.go)                                  │
│  - HTTP 请求处理、参数校验、权限认证                │
│  - 通过接口（Interface）调用 Database 层            │
│  - 不直接参与事务管理                               │
├─────────────────────────────────────────────────────┤
│  Database 层 (database/*.go)                        │
│  - 数据访问层，封装所有数据库 CRUD 操作             │
│  - ✅ 事务边界在此层划定（核心结论）                 │
│  - 使用 GORM v1.31.1 作为 ORM 框架                  │
├─────────────────────────────────────────────────────┤
│  插件管理器 (plugin/manager.go)                     │
│  - 插件生命周期管理                                 │
│  - 使用 sync.RWMutex 进行内存级并发控制             │
│  - 调用 Database 层，不管理数据库事务               │
├─────────────────────────────────────────────────────┤
│  数据库 (MySQL / PostgreSQL / SQLite3)              │
│  - 实际数据存储，执行 ACID 保证                     │
└─────────────────────────────────────────────────────┘
```

技术栈关键依赖：
- **Web 框架**：Gin v1.12.0
- **ORM**：GORM v1.31.1
- **数据库驱动**：MySQL / PostgreSQL / SQLite3
- **Go 版本**：1.25.0

---

## 二、事务边界的划定

### 2.1 事务管理机制

项目使用 **GORM** 提供的声明式事务 API，通过 `db.Transaction()` 方法划定事务边界：

```go
// GORM 事务的典型用法
return d.DB.Transaction(func(tx *gorm.DB) error {
    // 事务内的所有数据库操作使用 tx（而不是 d.DB）
    // 返回 nil → GORM 自动 Commit
    // 返回 error → GORM 自动 Rollback
}, &sql.TxOptions{Isolation: sql.LevelSerializable})
```

GORM `Transaction` 方法内部逻辑：
1. 调用 `db.Begin()` 开启事务
2. 执行传入的闭包函数
3. 如果闭包返回 `nil`，调用 `tx.Commit()` 提交
4. 如果闭包返回 `error` 或发生 panic，调用 `tx.Rollback()` 回滚

**核心设计原则**：
- ✅ 事务边界完全在 **Database 层** 内部划定
- ✅ API 层不感知事务的存在，仅通过接口调用 Database 层
- ✅ 采用 **"瘦接口 + 胖实现"** 模式，接口定义在 `api/` 包，实现在 `database/` 包

### 2.2 数据库连接池与基础配置

**代码位置**：[database/database.go:59-82](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/database/database.go#L59-L82)

```go
sqldb, _ := db.DB()
sqldb.SetMaxOpenConns(10)          // 默认最大 10 个连接

if dialect == "sqlite3" {
    sqldb.SetMaxOpenConns(1)       // SQLite 强制单连接，避免并发写入冲突
}

if dialect == "mysql" {
    sqldb.SetConnMaxLifetime(9 * time.Minute)  // 规避 MySQL wait_timeout
}
```

这为后续并发控制奠定了基础设施。

---

### 2.3 已使用事务保护的场景（完整梳理）

#### ✅ 场景 1：创建应用（CreateApplication）— 先读后写 + Serializable

**代码位置**：[database/application.go:39-55](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/database/application.go#L39-L55)

```go
func (d *GormDatabase) CreateApplication(application *model.Application) error {
    return d.DB.Transaction(func(tx *gorm.DB) error {
        if application.SortKey == "" {
            sortKey := ""
            // 步骤 1：查询该用户最后一个 sort_key（读）
            err := tx.Model(&model.Application{}).Select("sort_key").
                Where("user_id = ?", application.UserID).
                Order("sort_key DESC").Limit(1).Find(&sortKey).Error
            if err != nil && err != gorm.ErrRecordNotFound {
                return err  // → 触发 Rollback
            }
            // 步骤 2：通过分数索引算法生成新 sort_key（计算）
            application.SortKey, err = fracdex.KeyBetween(sortKey, "")
            if err != nil {
                return err  // → 触发 Rollback
            }
        }
        // 步骤 3：插入应用记录（写）
        return tx.Create(application).Error  // error → Rollback, nil → Commit
    }, &sql.TxOptions{Isolation: sql.LevelSerializable})
}
```

**事务边界分析**：
- **划定层次**：Database 层方法内部
- **隔离级别**：`Serializable`（最高级别，防止幻读）
- **覆盖操作**：1 次 SELECT + 1 次 INSERT
- **一致性保障**：保证同一用户下 sort_key 连续且不冲突
- **调用方**：[api/application.go:92-111](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/api/application.go#L92-L111) `CreateApplication` → 插件管理器 `createPluginConf`

---

#### ✅ 场景 2：批量更新客户端 Token 活性 — 多记录一致性

**代码位置**：[database/client.go:68-83](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/database/client.go#L68-L83)

```go
func (d *GormDatabase) UpdateClientTokensLastUsedAndExpiresAt(tokens []string, t *time.Time) error {
    return d.DB.Transaction(func(tx *gorm.DB) error {
        var clients []*model.Client
        // 步骤 1：批量查询客户端（读）
        if err := tx.Where("token IN (?)", tokens).Find(&clients).Error; err != nil {
            return err
        }
        // 步骤 2：逐个更新 last_used 和 expires_at（写）
        for _, c := range clients {
            c.LastUsed = t
            c.PopulateExpiresAt()  // 根据 ExpiresAfterInactivitySeconds 计算
            if err := tx.Save(c).Error; err != nil {
                return err  // 任何一条失败 → 全部回滚
            }
        }
        return nil  // 全部成功 → 整体提交
    })  // 默认隔离级别（由数据库决定，通常是 Read Committed）
}
```

**事务边界分析**：
- **划定层次**：Database 层方法内部
- **隔离级别**：默认（未显式指定）
- **覆盖操作**：1 次批量 SELECT + N 次 UPDATE
- **一致性保障**：要么所有客户端的时间戳同时更新，要么都不更新
- **调用方**：[router/router.go:70-78](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/router/router.go#L70-L78) 后台 5 分钟定时任务

---

#### ✅ 场景 3：初始化数据迁移 — 全表批量更新

**代码位置**：[database/database.go:94-100](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/database/database.go#L94-L100)

```go
// 迁移 1：填充缺失的应用 sort_key
if err := db.Transaction(fillMissingSortKeys, 
    &sql.TxOptions{Isolation: sql.LevelSerializable}); err != nil {
    return nil, err
}

// 迁移 2：填充缺失的 created_at 时间戳
if err := db.Transaction(
    func(tx *gorm.DB) error { return fillMissingCreatedAt(tx, now()) }, 
    &sql.TxOptions{Isolation: sql.LevelSerializable}); err != nil {
    return nil, err
}
```

其中 `fillMissingSortKeys` 在 [database/database.go:120-151](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/database/database.go#L120-L151) 中遍历所有应用并重新计算 sort_key，使用 Serializable 保证迁移期间不会有并发写入干扰。

---

### 2.4 ⚠️ 未使用事务保护的多表操作（风险点梳理）

以下操作涉及 **2 张或更多表**，但 **未包裹在事务中**，存在数据不一致风险。

#### ❌ 风险点 1：删除应用（DeleteApplicationByID）

**代码位置**：[database/application.go:57-61](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/database/application.go#L57-L61)

```go
func (d *GormDatabase) DeleteApplicationByID(id uint) error {
    d.DeleteMessagesByApplication(id)  // 语句 1：DELETE messages WHERE application_id = ?
    return d.DB.Where("id = ?", id).   // 语句 2：DELETE applications WHERE id = ?
        Delete(&model.Application{}).Error
}
```

**涉及表**：`messages` + `applications`（2 张表）

**风险场景**：
```
时间线：
  T1: DELETE messages WHERE application_id = 123   ✓ 成功，消息已删除
  T2: 网络中断 / 数据库连接断开 / 磁盘满
  T3: DELETE applications WHERE id = 123           ✗ 失败
结果：messages 表中 app=123 的消息全没了，但 applications 表中 app=123 还存在 → "孤儿应用"
```

**正确做法**：
```go
func (d *GormDatabase) DeleteApplicationByID(id uint) error {
    return d.DB.Transaction(func(tx *gorm.DB) error {
        if err := tx.Where("application_id = ?", id).Delete(&model.Message{}).Error; err != nil {
            return err
        }
        return tx.Where("id = ?", id).Delete(&model.Application{}).Error
    })
}
```

---

#### ❌ 风险点 2：删除用户（DeleteUserByID）— 最严重

**代码位置**：[database/user.go:54-69](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/database/user.go#L54-L69)

```go
func (d *GormDatabase) DeleteUserByID(id uint) error {
    apps, _ := d.GetApplicationsByUser(id)          // 忽略错误
    for _, app := range apps {
        d.DeleteApplicationByID(app.ID)             // 内部又删 messages + applications
    }
    clients, _ := d.GetClientsByUser(id)            // 忽略错误
    for _, client := range clients {
        d.DeleteClientByID(client.ID)               // DELETE clients
    }
    pluginConfs, _ := d.GetPluginConfByUser(id)     // 忽略错误
    for _, conf := range pluginConfs {
        d.DeletePluginConfByID(conf.ID)             // DELETE plugin_confs
    }
    return d.DB.Where("id = ?", id).                // 最后：DELETE users
        Delete(&model.User{}).Error
}
```

**涉及表**：`messages` → `applications` → `clients` → `plugin_confs` → `users`（5 张表）

**三大问题**：
1. **无事务**：任何一步中断都会导致部分删除、部分残留
2. **吞掉错误**：`apps, _ := ...` 用 `_` 接收 error，出错了完全不知道
3. **嵌套非事务调用**：`DeleteApplicationByID` 本身也是非事务的（见风险点 1）

**风险场景**：删用户时删到一半断电，数据库中出现：
- 该用户的 `applications` 被删了，但 `users` 表记录还在
- 该用户的部分 `clients` 被删了，另一部分还在
- `plugin_confs` 完全没动

---

#### ❌ 风险点 3：删除用户所有消息（DeleteMessagesByUser）

**代码位置**：[database/message.go:88-95](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/database/message.go#L88-L95)

```go
func (d *GormDatabase) DeleteMessagesByUser(userID uint) error {
    app, _ := d.GetApplicationsByUser(userID)    // 忽略错误
    for _, app := range app {
        d.DeleteMessagesByApplication(app.ID)    // 逐条删除，忽略错误
    }
    return nil                                    // 永远返回 nil
}
```

**问题**：忽略所有中间错误，调用方永远认为成功。

---

#### ❌ 风险点 4：插件初始化创建 Application + PluginConf

**代码位置**：[plugin/manager.go:399-424](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/plugin/manager.go#L399-L424)

```go
func (m *Manager) createPluginConf(...) (*model.PluginConf, error) {
    // ...
    if compat.HasSupport(instance, compat.Messenger) {
        app := &model.Application{...}
        if err := m.db.CreateApplication(app); err != nil {   // 步骤 1：INSERT applications
            return nil, err
        }
        pluginConf.ApplicationID = app.ID
    }
    if err := m.db.CreatePluginConf(pluginConf); err != nil { // 步骤 2：INSERT plugin_confs
        return nil, err                                        // 此时 application 已写入！
    }
    return pluginConf, nil
}
```

**涉及表**：`applications` + `plugin_confs`（2 张表）

**风险**：步骤 1 成功插入 application，步骤 2 失败插入 plugin_conf → 产生"无主 application"。

**更严重的问题**：`CreateApplication` 内部已经是一个 Serializable 事务，但外部的"创建应用 + 创建插件配置"这个更大的业务单元并没有事务包裹。

---

#### ❌ 风险点 5：用户删除流程中的回调 + 数据库操作

**代码位置**：[api/user.go:330-354](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/api/user.go#L330-L354)

```go
func (a *UserAPI) DeleteUserByID(ctx *gin.Context) {
    withID(ctx, "id", func(id uint) {
        // ... 校验 admin 数量 ...
        if err := a.UserChangeNotifier.fireUserDeleted(id); err != nil {  // 步骤 A：执行回调
            ctx.AbortWithError(500, err)
            return
        }
        successOrAbort(ctx, 500, a.DB.DeleteUserByID(id))                 // 步骤 B：删数据库
    })
}
```

`fireUserDeleted` 会调用 `pluginManager.RemoveUser` → 禁用插件实例（内存操作）。

**风险**：步骤 A 成功（插件已禁用，内存状态已变），步骤 B 失败（数据库用户未删）→ 内存状态与数据库不一致。

---

### 2.5 事务边界划定层次总结

| 层次 | 是否划定事务 | 说明 |
|------|:-----------:|------|
| `api/` 层 (API Handler) | ❌ 从不 | 通过接口调用 Database 层，完全不感知事务 |
| `plugin/` 层 (插件管理器) | ❌ 从不 | 调用 Database 层接口，使用内存锁管理插件实例 |
| `database/` 层 (DAO) | ✅ **唯一划定层** | 所有事务在此层的方法内部划定 |
| 数据库引擎 | ✅ 实际执行 | MySQL/PostgreSQL/SQLite 执行 ACID |

**设计哲学**：**"事务靠近数据"** — 事务边界尽可能靠近数据访问层，避免业务逻辑层被事务基础设施污染，同时保证数据库操作的原子性。

---

## 三、并发写入冲突处理机制

### 3.1 三级并发控制策略总览

项目采用 **"应用层内存锁 → 数据库连接限制 → 隔离级别 + 唯一索引 → 错误捕获转换"** 的纵深防御策略：

```
┌───────────────────────────────────────────────────────────┐
│  L4 应用层错误捕获（API 层）                               │
│  ├─ 捕获 gorm.ErrDuplicatedKey                            │
│  └─ 转换为 400 Bad Request + 友好消息                      │
├───────────────────────────────────────────────────────────┤
│  L3 数据库隔离与约束                                       │
│  ├─ 唯一索引（Unique Index）→ 抛出 Duplicate Key          │
│  ├─ Serializable 隔离级别 → 防止幻读                     │
│  └─ SQLite 单连接限制 → 写入完全串行化                     │
├───────────────────────────────────────────────────────────┤
│  L2 连接池配置                                            │
│  ├─ MySQL/PG: MaxOpenConns = 10                          │
│  └─ SQLite: MaxOpenConns = 1（强制串行写入）              │
├───────────────────────────────────────────────────────────┤
│  L1 内存同步原语                                          │
│  ├─ sync.Mutex / sync.RWMutex                             │
│  └─ 保护内存中的插件实例 map、连接 map 等                  │
└───────────────────────────────────────────────────────────┘
```

---

### 3.2 L1：内存级并发控制（sync 包）

对于非数据库的内存数据结构，使用 `sync` 包原语保护：

| 模块 | 锁类型 | 保护对象 | 代码位置 |
|------|:------:|---------|---------|
| Plugin Manager | `sync.RWMutex` | `instances` map、`plugins` map | [plugin/manager.go:48](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/plugin/manager.go#L48) |
| Stream Handler | `sync.RWMutex` | 客户端连接注册表 | [api/stream/stream.go:42](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/api/stream/stream.go#L42) |
| DecayMap | `sync.Mutex` | 带 TTL 的泛型缓存 Map | [decaymap/decaymap.go:29](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/decaymap/decaymap.go#L29) |
| Stream Once | `sync.Mutex` | 单次执行标记 | [api/stream/once.go:29](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/api/stream/once.go#L29) |
| Plugin 消息通道 | `chan` | 插件发送消息的串行化队列 | [plugin/manager.go:51](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/plugin/manager.go#L51) |

**典型用法**（Plugin Manager 启用/禁用插件）：
```go
// [plugin/manager.go:117-148]
func (m *Manager) SetPluginEnabled(pluginID uint, enabled bool) error {
    // ... 先检查状态（读，不加锁）...

    m.mutex.Lock()          // 写操作前加写锁
    defer m.mutex.Unlock()

    if enabled {
        err = instance.Enable()   // 内存操作：调用插件 Enable
    } else {
        err = instance.Disable()  // 内存操作：调用插件 Disable
    }
    // ... 更新数据库 plugin_conf.enabled ...
    return m.db.UpdatePluginConf(conf)
}
```

**设计要点**：锁仅保护内存状态，数据库操作在锁外执行（降低锁持有时间）。

---

### 3.3 L2：连接池级别的并发控制

**代码位置**：[database/database.go:64-74](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/database/database.go#L64-L74)

| 数据库 | MaxOpenConns | 并发策略 |
|--------|:------------:|---------|
| MySQL | 10 | 允许 10 个并发连接，依靠 MVCC + 行锁处理冲突 |
| PostgreSQL | 10 | 同上 |
| SQLite3 | **1** | **完全串行化写入**，从连接层面杜绝并发写 |

SQLite 特殊处理的原因：
> Sqlite cannot handle concurrent writes, so we limit sqlite to one connection.  
> — [database/database.go:71-72](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/database/database.go#L71-L72)

SQLite 本身使用 **文件级锁**（BEGIN EXCLUSIVE），多个连接写入会频繁返回 `SQLITE_BUSY` 错误。限制为单连接相当于在应用层实现了"数据库级写锁"。

---

### 3.4 L3：数据库级别的并发控制

#### 机制 A：唯一索引（Unique Index）

在模型定义中通过 GORM tag 声明唯一索引，由数据库保证唯一性：

| 表 | 唯一索引字段 | 代码位置 |
|----|------------|---------|
| `applications` | `token`（全局唯一） | [model/application.go:22](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/model/application.go#L22) |
| `applications` | `(user_id, sort_key)`（联合唯一） | [model/application.go:24-25](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/model/application.go#L24-L25) |
| `users` | `name`（用户名唯一） | [model/user.go:8](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/model/user.go#L8) |
| `clients` | `token`（全局唯一） | [model/client.go:22](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/model/client.go#L22) |
| `plugin_confs` | `token`（全局唯一） | [model/pluginconf.go:10](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/model/pluginconf.go#L10) |

**冲突产生原理**（以 `CreateApplication` 为例）：

```
两个请求同时创建应用，使用同一个用户 ID=5：

  请求 A (Serializable 事务)              请求 B (Serializable 事务)
  ─────────────────────────               ─────────────────────────
  BEGIN ISOLATION LEVEL SERIALIZABLE
  SELECT sort_key FROM applications
    WHERE user_id = 5 ORDER BY sort_key
    DESC LIMIT 1 → 得到 "a1"
                                          BEGIN ISOLATION LEVEL SERIALIZABLE
                                          SELECT sort_key FROM applications
                                            WHERE user_id = 5 ORDER BY sort_key
                                            DESC LIMIT 1 → 也得到 "a1"!
  计算 fracdex.KeyBetween("a1", "")
    → 得到新 sort_key "a2"
  INSERT INTO applications (sort_key, ...)
    VALUES ("a2", ...) → 成功
                                          计算 fracdex.KeyBetween("a1", "")
                                            → 也得到 sort_key "a2"!
                                          INSERT INTO applications (sort_key, ...)
                                            VALUES ("a2", ...) → 💥 唯一索引冲突!
  COMMIT
                                          ROLLBACK (抛出 ErrDuplicatedKey)
```

即使使用了 Serializable 隔离级别，在 PostgreSQL/MySQL 的 MVCC 实现中，Serializable 并不一定阻塞读。唯一索引作为 **最后一道防线**，保证 sort_key 不会重复。

---

#### 机制 B：Serializable 隔离级别

**使用场景**：
1. `CreateApplication` — sort_key 生成（先读后写）
2. `fillMissingSortKeys` — 数据迁移期间全表更新
3. `fillMissingCreatedAt` — 数据迁移期间全表更新

Serializable 是 SQL 标准定义的最高隔离级别，保证：
- ✅ 不脏读（不会读到未提交数据）
- ✅ 不可重复读（同一事务内两次读同一行结果相同）
- ✅ 不幻读（同一事务内两次范围查询结果相同）

---

### 3.5 L4：应用层冲突"收场"方式

当数据库层面的冲突（如唯一索引冲突）发生后，项目采用以下链路处理：

#### 完整的冲突处理链路

```
┌──────────────┐     gorm.ErrDuplicatedKey      ┌──────────────┐
│  数据库引擎   │ ─────────────────────────────→ │  GORM 框架    │
│  (抛出错误)   │                                │  (翻译错误)   │
└──────────────┘                                └──────┬───────┘
                                                       │ error
                                                       ▼
                                              ┌────────────────┐
                                              │ Database 层    │
                                              │ (直接向上传递) │
                                              └───────┬────────┘
                                                      │ error
                                                      ▼
                                             ┌──────────────────┐
                                             │ API 层 Handler   │
                                             │ handleApplication│
                                             │     Error()      │
                                             └────────┬─────────┘
                                                      │
                                                      ▼
                    ┌───────────────────────────────────────────────────────┐
                    │ HTTP 响应：                                            │
                    │   400 Bad Request                                      │
                    │   {"error": "Bad Request", "errorCode": 400,          │
                    │    "errorDescription": "sort key is not unique"}      │
                    └───────────────────────────────────────────────────────┘
```

**关键代码**：

1. **GORM 配置开启错误翻译**（[database/database.go:39](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/database/database.go#L39)）：
   ```go
   gormConfig := &gorm.Config{
       TranslateError: true,  // ← 将数据库特定错误翻译为 gorm.ErrDuplicatedKey 等通用错误
   }
   ```

2. **API 层转换错误**（[api/application.go:485-491](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/api/application.go#L485-L491)）：
   ```go
   func handleApplicationError(ctx *gin.Context, err error) {
       if errors.Is(err, gorm.ErrDuplicatedKey) {
           ctx.AbortWithError(400, errors.New("sort key is not unique"))
       } else {
           ctx.AbortWithError(500, err)
       }
   }
   ```

3. **测试验证**（[api/application_test.go:655-668](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/api/application_test.go#L655-L668)）：
   ```go
   func (s *ApplicationSuite) Test_UpdateApplication_duplicateSortKey() {
       user := s.db.User(5)
       user.App(1) // sortKey=a0
       user.App(2) // sortKey=a1
       
       s.withFormData("name=new_name&sortKey=a0")  // 把 app2 的 sortKey 改为 a0
       test.WithUser(s.ctx, 5)
       s.ctx.Params = gin.Params{{Key: "id", Value: "2"}}
       
       s.a.UpdateApplication(s.ctx)
       
       // 断言：返回 400 和友好错误消息
       assert.EqualError(s.T(), s.ctx.Errors[0].Err, "sort key is not unique")
       assert.Equal(s.T(), 400, s.recorder.Code)
   }
   ```

#### 用户层冲突处理（用户名重复）

**代码位置**：[api/user.go:188-239](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/api/user.go#L188-L239)

```go
func (a *UserAPI) CreateUser(ctx *gin.Context) {
    user := model.CreateUserExternal{}
    if err := ctx.Bind(&user); err == nil {
        // 先查询是否已存在（应用层面"乐观检查"）
        existingUser, err := a.DB.GetUserByName(internal.Name)
        if success := successOrAbort(ctx, 500, err); !success {
            return
        }
        if existingUser == nil {
            // 不存在 → 尝试创建
            if success := successOrAbort(ctx, 500, a.DB.CreateUser(internal)); !success {
                return
            }
            // ... 触发回调 ...
        } else {
            // 应用层面已检查到重复 → 直接返回 400
            ctx.AbortWithError(400, errors.New("username already exists"))
        }
    }
}
```

**策略**：先 SELECT 检查是否存在，不存在才 INSERT。但这不是原子操作！并发创建同名用户仍然会触发数据库唯一索引冲突（抛出 `gorm.ErrDuplicatedKey`），此时会被 `successOrAbort` 转为 500 错误，**存在缺陷**：数据库层唯一索引冲突没有被正确捕获并转换为 400。

---

### 3.6 并发冲突收场策略对照表

| 冲突场景 | 检测层 | 失败时响应 | 重试？ |
|---------|--------|-----------|:------:|
| 创建应用时 sort_key 冲突 | 数据库唯一索引 → API 层捕获 | HTTP 400: "sort key is not unique" | ❌ 无重试，需用户操作 |
| 更新应用时 sort_key 冲突 | 数据库唯一索引 → API 层捕获 | HTTP 400: "sort key is not unique" | ❌ 无重试 |
| 创建用户时用户名重复 | 应用层 SELECT 预检查 | HTTP 400: "username already exists" | ❌ 无重试 |
| Token 生成冲突 | 循环重试（`GenerateNotExistingToken`） | 内部重试，对用户透明 | ✅ 循环生成直到唯一 |
| 插件启用状态竞态 | 应用层 `conf.Enabled == enabled` 检查 | 返回 `ErrAlreadyEnabledOrDisabled` | ❌ 幂等，不视为错误 |
| SQLite 并发写入 | 连接池限制 MaxOpenConns=1 | 排队等待，不触发错误 | ✅ 自动排队 |
| Serializable 隔离级别下的序列化失败 | 数据库层 | 向上传递为 error（可能 500） | ❌ 无重试 |

---

## 四、代码调用链路追踪（典型场景）

### 4.1 创建应用的事务链路

```
HTTP POST /application
  │
  ▼
[api/application.go:92] CreateApplication()
  │  生成 token: auth.GenerateNotExistingToken()
  │  构造 model.Application 对象
  │
  ▼
[database/application.go:39] CreateApplication()
  │  ┌────────────────────────────────────────────┐
  │  │ db.Transaction() Serializable 事务开始     │
  │  │   tx: SELECT sort_key ... (读最后一个 key) │
  │  │   计算 fracdex.KeyBetween()                │
  │  │   tx: INSERT INTO applications ...         │
  │  └────────────────────────────────────────────┘
  │         ↓ nil → Commit / error → Rollback
  ▼
  返回 HTTP 200 + 应用对象
  或 HTTP 400 "sort key is not unique"
```

### 4.2 删除用户的非事务链路（⚠️ 有风险）

```
HTTP DELETE /user/:id
  │
  ▼
[api/user.go:330] DeleteUserByID()
  │
  ├─▶ [plugin/manager.go:184] RemoveUser()  ← 回调 A: 内存操作
  │       禁用插件，从 instances map 删除
  │
  ▼
[database/user.go:54] DeleteUserByID()  ← 无事务包裹！
  │
  ├─ ① d.GetApplicationsByUser(id)
  ├─ ② for each app:
  │      d.DeleteApplicationByID(app.ID)
  │        ├─ d.DeleteMessagesByApplication(id)  ← DELETE messages
  │        └─ DELETE applications                ← DELETE applications
  │
  ├─ ③ d.GetClientsByUser(id)
  ├─ ④ for each client: DELETE clients
  │
  ├─ ⑤ d.GetPluginConfByUser(id)
  ├─ ⑥ for each conf: DELETE plugin_confs
  │
  └─ ⑦ DELETE users
```

每一步都可能失败，且无回滚机制。

---

## 五、改进建议

### 5.1 高优先级：补充缺失的事务

| 风险点 | 建议 | 涉及文件 |
|--------|------|---------|
| `DeleteApplicationByID` | 用 `db.Transaction` 包裹删除消息和删除应用 | [database/application.go:57-61](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/database/application.go#L57-L61) |
| `DeleteUserByID` | 用 `db.Transaction` 包裹全部 5 张表删除操作，传递 error 不要吞掉 | [database/user.go:54-69](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/database/user.go#L54-L69) |
| `DeleteMessagesByUser` | 用 `db.Transaction` 包裹并正确传递 error | [database/message.go:88-95](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/database/message.go#L88-L95) |
| `plugin.createPluginConf` | 新增 Database 层方法 `CreateApplicationWithPluginConf`，包裹在事务中 | [plugin/manager.go:399-424](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/plugin/manager.go#L399-L424) |
| `UserAPI.DeleteUserByID` | 将 `fireUserDeleted` 回调放入事务成功后执行，或使用 saga 模式补偿 | [api/user.go:345-349](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/api/user.go#L345-L349) |

### 5.2 中优先级：完善并发冲突处理

1. **`CreateUser` 中捕获 `ErrDuplicatedKey`**：当前只做了应用层 SELECT 检查，未捕获数据库唯一索引冲突
2. **考虑冲突重试机制**：对于 Serializable 隔离级别下的序列化失败，可考虑指数退避重试
3. **用户创建的原子性**：`GetUserByName` + `CreateUser` 是非原子的，建议在 `CreateUser` 内部捕获冲突并返回语义化错误

### 5.3 低优先级：架构优化

1. **引入 Unit of Work 模式**：对于跨多个 DAO 方法的业务操作，提供工作单元接口统一管理事务
2. **Database 层接口暴露事务能力**：考虑新增 `WithTransaction(fn func(tx Database) error)` 方法，让上层在必要时控制更大粒度的事务

---

## 六、总结

### 6.1 核心结论

1. **事务边界划定层**：**Database 层（`database/*.go`）** 是项目中唯一划定事务边界的地方，所有 `db.Transaction()` 调用均在此层。API 层和 Plugin 层不直接参与事务管理。

2. **事务使用现状**：
   - ✅ 已正确使用事务：`CreateApplication`（Serializable）、`UpdateClientTokensLastUsedAndExpiresAt`（默认隔离）、初始化数据迁移
   - ❌ 缺失事务：`DeleteApplicationByID`、`DeleteUserByID`（最严重）、`DeleteMessagesByUser`、插件创建流程

3. **并发冲突收场机制**：采用"纵深防御"策略 — 内存锁保护内存数据 → 连接池限制 → 数据库唯一索引 + 高隔离级别 → API 层错误转换为友好响应。**最终"收场"方式是返回 HTTP 400 错误给用户，不做自动重试**。

4. **SQLite 特殊处理**：通过 `SetMaxOpenConns(1)` 在应用层实现写入完全串行化，规避 SQLite 的并发写入缺陷。

### 6.2 关键代码速查表

| 功能 | 文件 | 行号 |
|------|------|-----|
| 数据库初始化 & 连接池配置 | [database/database.go](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/database/database.go) | 27-103 |
| 创建应用事务（Serializable） | [database/application.go](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/database/application.go) | 39-55 |
| 批量更新客户端事务 | [database/client.go](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/database/client.go) | 68-83 |
| 删除应用（缺事务 ⚠️） | [database/application.go](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/database/application.go) | 57-61 |
| 删除用户（缺事务 ⚠️） | [database/user.go](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/database/user.go) | 54-69 |
| 唯一索引冲突错误处理 | [api/application.go](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/api/application.go) | 485-491 |
| Plugin Manager 内存锁 | [plugin/manager.go](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/plugin/manager.go) | 48, 131-132 |
| 应用模型唯一索引定义 | [model/application.go](file:///d:/fz/0601-1/solo-dogfeeding/code/76-server/model/application.go) | 22-25 |
