# 插件系统架构分析报告

## 1. 系统整体架构

### 1.1 架构分层

Gotify 插件系统采用三层架构设计：

```
┌─────────────────────────────────────────────────────────────┐
│                     前端层 (Frontend)                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐  │
│  │ PluginStore  │  │ PluginUI     │  │ MessageStore     │  │
│  └──────────────┘  └──────────────┘  └──────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                              ↓ HTTP/REST
┌─────────────────────────────────────────────────────────────┐
│                      API层 (api/plugin.go)                   │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  PluginAPI: GetPlugins/Enable/Disable/Config/Display │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                   插件管理层 (plugin/)                       │
│  ┌──────────────────────────────────────────────────────┐  │
│  │            Manager: 实例管理、生命周期                 │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │         兼容层 (compat/): 接口抽象、适配器              │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                    认证层 (auth/)                            │
│  ┌──────────────────────────────────────────────────────┐  │
│  │    中间件: Token验证、用户/客户端/应用认证              │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

---

## 2. 插件实例创建流程

### 2.1 流程图

```
系统启动
    ↓
[Manager.NewManager]
    ↓
1. 加载插件 (loadPlugins)
   ├─ 读取插件目录 .so 文件
   ├─ Go plugin.Open 加载原生插件
   └─ compat.Wrap() 封装为兼容接口
       ├─ 查找 GetGotifyPluginInfo 符号
       └─ 查找 NewGotifyPluginInstance 符号
    ↓
2. 为所有用户初始化插件实例
   └─ [initializeForUser]
       ├─ 创建 UserContext {ID, Name, Admin}
       └─ 对每个插件执行 initializeSingleUserPlugin
           ├─ 调用 Plugin.NewPluginInstance(ctx)
           │   └─ PluginV1Instance 适配器包装
           ├─ 创建 PluginConf 数据库记录
           │   ├─ 生成 Plugin Token
           │   ├─ Messenger 插件自动创建 Application
           │   └─ 生成 Application Token
           ├─ 根据能力设置 Handler
           │   ├─ Messenger: SetMessageHandler
           │   ├─ Storager: SetStorageHandler
           │   ├─ Configurer: 初始化配置
           │   └─ Webhooker: 注册 Webhook 路由
           └─ 如已启用则调用 instance.Enable()
```

### 2.2 关键代码路径

| 阶段 | 文件位置 | 关键函数 |
|------|---------|---------|
| 插件加载 | `plugin/manager.go:219-254` | `loadPlugins()` |
| 兼容封装 | `plugin/compat/wrap.go:12-35` | `Wrap()` |
| 用户初始化 | `plugin/manager.go:281-313` | `initializeForUser()` |
| 单插件初始化 | `plugin/manager.go:315-364` | `initializeSingleUserPlugin()` |
| 实例创建 | `plugin/compat/v1.go:35-67` | `NewPluginInstance()` |

---

## 3. 插件前端模块分析

### 3.1 前端架构

前端采用 **MobX + React** 状态管理模式：

```
PluginStore (MobX Store)
    ├─ 状态: plugins 列表
    ├─ 操作:
    │   ├─ requestItems() - 获取插件列表
    │   ├─ requestConfig(id) - 获取配置
    │   ├─ requestDisplay(id) - 获取显示信息
    │   ├─ changeConfig(id, newConfig) - 更新配置
    │   └─ changeEnabledState(id, enabled) - 启用/禁用
    └─ ↓ HTTP 请求
        ↓
    PluginAPI (后端)
        └─ ↓ 响应
            ↓
    PluginUI (React 组件)
        ├─ Plugins.tsx - 插件列表
        └─ PluginDetailView.tsx - 插件详情
```

### 3.2 PluginStore 核心实现 (`ui/src/plugin/PluginStore.ts`)

```typescript
class PluginStore extends BaseStore<IPlugin> {
    // 获取 YAML 配置
    requestConfig(id: number): Promise<string>
    
    // 获取 Displayer 插件的显示内容
    requestDisplay(id: number): Promise<string>
    
    // 更新配置 (YAML 格式)
    changeConfig(id: number, newConfig: string): Promise<void>
    
    // 启用/禁用插件
    changeEnabledState(id: number, enabled: boolean): Promise<void>
}
```

### 3.3 前端认证机制

前端通过以下方式传递认证信息：
- **Cookie**: `gotify-client-token` 存储客户端 Token
- **Header**: `X-Gotify-Key` 或 `Authorization: Bearer <token>`
- **Query Param**: `?token=<token>`

所有插件相关 API 调用均通过 `axios` 自动携带认证信息。

---

## 4. 消息 UI 层交互

### 4.1 消息流向

```
插件 (Messenger 能力)
    ↓ 调用 SendMessage()
MessageHandler (redirectToChannel)
    ↓ 写入 channel
Manager.messages 通道
    ↓ goroutine 消费
    ├─ 创建 Message 数据库记录
    └─ Notifier.Notify() 广播消息
        ↓ WebSocket
前端 MessagesStore
    ↓
UI 消息展示
```

### 4.2 关键组件交互

| 组件 | 职责 | 文件位置 |
|------|------|---------|
| `redirectToChannel` | 将插件消息转发到内部通道 | `plugin/messagehandler.go:10-36` |
| `MessageWithUserID` | 封装消息与用户关联 | `plugin/messagehandler.go:17-20` |
| `MessagesStore` | 前端消息状态管理 | `ui/src/message/MessagesStore.ts` |
| `WebSocketStore` | WebSocket 连接管理 | `ui/src/message/WebSocketStore.ts` |

---

## 5. 认证模块分析

### 5.1 认证中间件架构

```
HTTP 请求
    ↓
Auth 中间件
    ├─ 读取 Token 来源:
    │   ├─ Cookie (gotify-client-token)
    │   ├─ Header (X-Gotify-Key)
    │   ├─ Header (Authorization: Bearer)
    │   └─ Query Param (token)
    ↓
认证类型判断:
    ├─ 用户认证 (Basic Auth) → RegisterUser()
    ├─ 客户端认证 (Token) → RegisterClient()
    └─ 应用认证 (Token) → RegisterApplication()
    ↓
权限检查:
    ├─ RequireClient - 需要客户端
    ├─ RequireAdmin - 需要管理员
    ├─ RequireElevatedClient - 需要提升权限
    └─ RequireApplicationToken - 需要应用 Token
```

### 5.2 插件相关认证点

| API 端点 | 认证要求 | 说明 |
|---------|---------|------|
| `GET /plugin` | RequireClient | 获取当前用户的插件列表 |
| `POST /plugin/{id}/enable` | RequireClient | 启用插件（需验证是插件所有者） |
| `POST /plugin/{id}/disable` | RequireClient | 禁用插件 |
| `GET /plugin/{id}/config` | RequireClient | 获取插件配置 |
| `POST /plugin/{id}/config` | RequireClient | 更新插件配置 |
| `GET /plugin/{id}/display` | RequireClient | 获取插件显示信息 |
| `/{pluginToken}/**` | 插件自有 | Webhook 端点，插件自定义认证 |

---

## 6. 兼容层接口边界详解

### 6.1 兼容层架构设计

兼容层采用 **适配器模式 (Adapter Pattern)** 实现，支持多版本插件 API：

```
原生 Go 插件 (.so)
    ↓
[compat.Wrap()] 版本检测与封装
    ↓
compat.Plugin 统一接口
    ├─ PluginInfo() Info
    ├─ NewPluginInstance(ctx UserContext) PluginInstance
    └─ APIVersion() string
    ↓
compat.PluginInstance 实例接口
    ├─ Enable() / Disable()
    ├─ Supports() Capabilities
    ├─ [Messenger] SetMessageHandler()
    ├─ [Configurer] DefaultConfig() / ValidateAndSetConfig()
    ├─ [Storager] SetStorageHandler()
    ├─ [Webhooker] RegisterWebhook()
    └─ [Displayer] GetDisplay()
```

### 6.2 核心接口定义

#### 6.2.1 Plugin 接口 (`plugin/compat/plugin.go:4-8`)

```go
type Plugin interface {
    PluginInfo() Info          // 获取插件元信息
    NewPluginInstance(ctx UserContext) PluginInstance  // 创建实例
    APIVersion() string        // API 版本标识
}
```

**边界说明**:
- 仅暴露插件必要的元信息和实例创建方法
- `UserContext` 作为唯一参数，隔离外部用户数据
- 返回 `PluginInstance` 抽象接口，隐藏具体实现

#### 6.2.2 PluginInstance 接口 (`plugin/compat/instance.go:26-49`)

```go
type PluginInstance interface {
    Enable() error
    Disable() error
    
    // Displayer 能力
    GetDisplay(location *url.URL) string
    
    // Configurer 能力
    DefaultConfig() interface{}
    ValidateAndSetConfig(c interface{}) error
    
    // Messenger 能力
    SetMessageHandler(h MessageHandler)
    
    // Webhooker 能力
    RegisterWebhook(basePath string, mux *gin.RouterGroup)
    
    // Storager 能力
    SetStorageHandler(handler StorageHandler)
    
    Supports() Capabilities
}
```

**边界说明**:
- 采用能力接口组合模式，插件按需实现
- 所有外部依赖通过参数注入（MessageHandler, StorageHandler, gin.RouterGroup）
- 不直接暴露数据库、HTTP 上下文等内部资源

#### 6.2.3 V1 适配器 (`plugin/compat/v1.go:11-183`)

`PluginV1Instance` 作为适配器，将外部 `papiv1` 接口转换为内部统一接口：

```go
type PluginV1Instance struct {
    instance   papiv1.Plugin    // 外部插件实例
    messenger  papiv1.Messenger // 类型断言后的能力接口
    configurer papiv1.Configurer
    storager   papiv1.Storager
    webhooker  papiv1.Webhooker
    displayer  papiv1.Displayer
}
```

**适配器职责**:
- 类型断言检测插件实现的能力
- 转发方法调用到外部插件实例
- 转换消息格式（Message → papiv1.Message）
- 转换 Handler 接口（MessageHandler → papiv1.MessageHandler）

### 6.3 兼容层边界原则

1. **单向依赖**: 仅插件依赖核心系统，核心系统不依赖特定插件
2. **接口隔离**: 每个能力为独立接口，插件按需实现
3. **依赖注入**: 所有外部资源通过 Handler 接口注入，不直接暴露
4. **版本透明**: 多版本 API 并存，对外提供统一接口

---

## 7. 认证信息在插件生命周期中的传递路径

### 7.1 认证信息传递全景图

```
用户登录
    ↓ [auth] 生成 Client Token
前端存储 (Cookie/LocalStorage)
    ↓
HTTP 请求携带 Token
    ↓ [auth middleware]
Gin Context 注册用户/客户端信息
    ↓
[PluginAPI] 提取 UserID: auth.GetUserID(ctx)
    ↓
[Manager.initializeForUser]
    ↓
UserContext 构建: {ID, Name, Admin}
    ↓ [PluginV1.NewPluginInstance]
传递给插件构造函数
    ↓
PluginConf 数据库记录 {UserID, Token, ApplicationID}
    ↓
插件实例生命周期内可用
    ├─ Messenger: 使用关联 Application Token 发消息
    ├─ Webhooker: 使用 Plugin Token 作为路由前缀
    └─ 所有操作均在 UserID 权限范围内
```

### 7.2 详细传递路径

#### 路径 1: 用户上下文传递

```
1. 认证中间件
   文件: auth/authentication.go:122-141
   操作: Basic Auth 验证 → 获取 User 对象 → RegisterUser(ctx, user)

2. API 层提取用户
   文件: api/plugin.go:64
   操作: userID := auth.GetUserID(ctx)

3. 插件管理器初始化
   文件: plugin/manager.go:281-286
   操作:
     userCtx := compat.UserContext{
         ID:    user.ID,
         Name:  user.Name,
         Admin: user.Admin,
     }

4. 传递给插件实例
   文件: plugin/compat/v1.go:36-40
   操作: c.Constructor(papiv1.UserContext{...})
```

#### 路径 2: Plugin Token 生成与使用

```
1. Token 生成
   文件: plugin/manager.go:403
   操作: Token: auth.GenerateNotExistingToken(
            auth.GeneratePluginToken, m.pluginConfExists)

2. 存储到数据库
   模型: model.PluginConf.Token

3. Webhook 路由注册
   文件: plugin/manager.go:349-351
   操作: mux.Group(pluginConf.Token+"/", requirePluginEnabled(...))

4. 前端使用
   类型: IPlugin.token
   用途: 构建 Webhook URL，展示给用户
```

#### 路径 3: Application Token 关联

```
1. Messenger 插件自动创建 Application
   文件: plugin/manager.go:409-419
   操作:
     app := &model.Application{
         Token:  auth.GenerateNotExistingToken(...),
         Name:   info.String(),
         UserID: userID,
         Internal: true,
     }

2. Token 存储关联
   模型: PluginConf.ApplicationID → Application.ID

3. 消息发送时使用
   文件: plugin/messagehandler.go:23-35
   操作: 使用关联的 ApplicationID 创建消息
   效果: 消息来源标记为该插件应用
```

### 7.3 认证信息生命周期

| 阶段 | 认证信息状态 | 说明 |
|------|-------------|------|
| 插件加载 | 无用户上下文 | 仅加载插件元信息，不创建实例 |
| 用户初始化 | UserContext 构建 | 为每个用户创建独立实例 |
| 实例启用 | 全量认证信息可用 | Token、ApplicationID 均就绪 |
| Webhook 调用 | 插件自定义认证 | 使用 Plugin Token 路由，插件可实现自有认证 |
| 消息发送 | Application Token 关联 | 消息自动关联到插件应用 |
| 用户删除 | 清理所有插件实例 | 删除用户关联的所有配置和 Token |

---

## 8. 关键设计模式与架构决策

### 8.1 使用的设计模式

| 模式 | 应用位置 | 作用 |
|------|---------|------|
| **适配器模式 (Adapter)** | compat/v1.go | 统一不同版本插件 API 接口 |
| **能力接口 (Capability Interface)** | compat/instance.go | 插件按需实现功能，接口隔离 |
| **依赖注入 (DI)** | SetMessageHandler/SetStorageHandler | 解耦插件与核心系统实现 |
| **观察者模式 (Observer)** | Notifier 机制 | 消息广播通知 |
| **工厂模式 (Factory)** | NewPluginInstance | 创建插件实例 |

### 8.2 重要架构决策

1. **按用户隔离插件实例**: 每个用户拥有独立的插件实例，配置、数据完全隔离
2. **能力驱动设计**: 插件不继承基类，通过实现能力接口声明功能
3. **插件无状态化**: 状态通过 StorageHandler 持久化，插件本身不持状态
4. **Token 三级体系**: User → Client/Application → Plugin，分层授权

---

## 9. 总结

Gotify 插件系统通过精心设计的兼容层实现了：

1. **高度解耦**: 插件与核心系统通过清晰的接口边界交互
2. **版本兼容**: 适配器模式支持多版本插件 API 并存
3. **安全隔离**: 用户认证信息通过受控路径传递，插件无法直接访问认证系统
4. **能力扩展**: 插件通过实现能力接口扩展功能，不影响核心系统

该架构既保证了插件的灵活性，又维持了系统的安全性和可维护性。
